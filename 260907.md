# 26.09.07
KANT 25일차, ML


## 오늘 느낀 점
- 다양한 테이블을 합쳐서 세부적인 조건과 함께 원하는 데이터를 출력하는 쿼리를 작성할 수 있게 됐다.


## 오늘 배운 것

```
### [ 학습목표 ]
- SELECT, FROM을 이용한 기본 쿼리문을 작성할 수 있다
- DISTINCT로 중복값을 제거할 수 있다
- WHERE, CASE WHEN으로 조건에 따른 데이터를 조회할 수 있다
- ORDER BY로 결과값을 정렬할 수 있다
- SQL 집계함수(COUNT, SUM, AVG, MIN, MAX)를 활용할 수 있다
- GROUP BY로 특정 기준에 따라 데이터를 그룹화할 수 있다
- HAVING과 WHERE의 차이점을 이해하고 적절히 사용할 수 있다
- SQL 테이블 결합: UNION 함수를 숙지하고 활용할 수 있다
- SQL 테이블 결합: JOIN 함수를 숙지하고 활용할 수 있다
- UNION과 JOIN 함수의 차이점을 이해하고 응용할 수 있다
- JOIN의 종류별 결과 차이를 설명할 수 있다
- JOIN으로 다중 테이블을 조건에 맞게 조회할 수 있다
```

## 목차

- [26.09.07](#260907)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [SQL \& 데이터베이스 교안 -\> 2장 1강](#sql--데이터베이스-교안---2장-1강)
    - [`AS`](#as)
    - [키워드 검색](#키워드-검색)
    - [ORDER BY](#order-by)
      - [연습문제 18. 매장별 주문 현황 정리](#연습문제-18-매장별-주문-현황-정리)
    - [SQL 통계(집계)](#sql-통계집계)
    - [GROUP BY](#group-by)
    - [관계형 데이터베이스 테이블 조작하기](#관계형-데이터베이스-테이블-조작하기)
    - [`JOIN`](#join)
      - [INNER JOIN](#inner-join)
      - [OUTER JOIN](#outer-join)
    - [UNION이랑 JOIN은 같은 것 아닌가요? 둘 다 테이블을 합치잖아요.](#union이랑-join은-같은-것-아닌가요-둘-다-테이블을-합치잖아요)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### SQL & 데이터베이스 교안 -> 2장 1강

```sql
SELECT [ALL|DISTINCT]
--distinct -> 중복없이 조회
```

WHERE 조건식
- 비교연산자 `>,<,>=,<=,=`
- 논리 연산자 `AND, OR, NOT`
- IN : 포함여부
  - NOT IN - 미포함시 TRUE
- BETWEEN A AND B : "A에서 B 사이"
- IS NULL : 값이 빈 상태인지 체크
  - IS NOT NULL : 값이 있는 상태인지 체크
  - "NULL은 값이 없는 상태이므로 비교, 산술 등의 연산이 불가"


```sql
-- NULL + 100 -> NULL
SELECT NULL + 100;

-- NULL이 아닌 데이터만 가져오기
select * from orders
where shipped_date IS NOT NULL;

-- 특정 범위 가격 조건 - 부등호
select * from products
where list_price >= 500 and list_price <= 1000;

-- 특정 범위 가격 조건 - BETWEEN
select * from products
where list_price BETWEEN 500 and 1000;

-- 기존 컬럼값에 대한 조건을 부여하여 새로운 컬럼 생성하기
select order_id, product_id, list_price,
case when list_price < 100 THEN '저가'
	when list_price < 500 THEN '중가'
	ELSE '고가'
END AS price_grade
from order_items;
```

### `AS`
- 별칭 / 생략가능
- 컬럼명 뒤에 -> 다른 컬럼으로 변경
- 테이블명 뒤에


### 키워드 검색
LIKE
1. LIKE "키워드" : 키워드에 정확히 매칭
2. LIKE "키워드%" : 키워드로 시작하는 패턴
3. LIKE "%키워드" : 키워드로 끝나는 패턴
4. LIKE "%키워드%" : 키워드가 포함된 패턴


```sql
SELECT * FROM customers
WHERE state = 'NY' 
```


### ORDER BY
- ORDER BY 컬럼명1, 컬럼명2 
  - 순차적으로 정렬 진행 : 1차정렬 -> 2차정렬
- ORDER BY product_name ASC, list_price DESC
  - 상품명으로 오름차 정렬 후 가격으로 내림차 정렬


#### 연습문제 18. 매장별 주문 현황 정리
- orders 테이블을 매장(store_id) 오름차순 -> 주문일(order_date) 내림차순 순서로 정렬해 조회하세요.

```sql
select * from orders
order by store_id, order_date DESC;
```


### SQL 통계(집계)
1. 집계 함수
   1. COUNT (컬럼명[컬럼명]) : 레코드 갯수 (NULL 아닌 갯수)
      1. `*` : 전체 컬럼에 대한 레코드 갯수
   2. SUM (컬럼명) : 합계
   3. AVG (컬럼명) : 평균
   4. MIN (컬럼명) : 최소값
   5. MAX (컬럼명) : 최대값

```sql
SELECT COUNT(*) AS "총 주문건수" FROM orders;

SELECT
	AVG(list_price) 평균,
	MAX(list_price) 최대값,
	MIN(list_price) 최소값
FROM products;



```

### GROUP BY
1. 특정 컬럼 기준으로 그룹을 만든다 (컬럼 동일값 기준)
2. 집계 함수와 함께 쓰이는 경우가 많다
3. 레코드 갯수는 그룹핑된 결과 갯수 만큼 조회
4. 반드시 GROUP BY에 명시된 컬럼만 사용가능
5. WHERE 절 사용불가능 / 조건입력은 HAVING 절 사용

```sql
select brand_id, COUNT(*) "상품 갯수"
from products
group by brand_id;

select brand_id, count(*) "상품 갯수"
from products
group by brand_id having count(*) >= 100;

-- brand_id 별로 통계값 출력하기
SELECT
	AVG(list_price) 평균,
	MAX(list_price) 최대값,
	MIN(list_price) 최소값
FROM products GROUP BY brand_id ORDER BY brand_id;

select category_id, AVG(list_price) 평균
from products
group by category_id
having avg(list_price) >= 500;
```



### 관계형 데이터베이스 테이블 조작하기
RDBMS : 관계형 데이터베이스 -> Relation (관계-테이블) 합치기

- 집합 (중복X)
  - 합집합 UNION -> 중복은 제거하고 합친다
    - UNION ALL -> 중복허용 합집합
    - UNION 실행 시, 필드 갯수는 동일해야 한다 (*필드 이름은 달라도 된다)
  - 교집합 INTERSECT
  - 차집합 EXCEPT


```sql
-- 2016년 1월 17일 이전
select order_id, order_date from orders
where order_date <= '2016-01-17' order by order_date;

-- 2016년 1월 15일 이후
select order_id, order_date from orders
where order_date >= '2016-01-15' order by order_date;

-- 공통 레코드 조회 (교집합-INTERSECT)
select order_id, order_date from orders
where order_date <= '2016-01-17'
INTERSECT
select order_id, order_date from orders
where order_date >= '2016-01-15'
ORDER BY order_date;

-- 공통 레코드 조회 (교집합-INTERSECT) 묶어서 불러오기
select * from (
(select order_id, order_date from orders
where order_date <= '2016-01-17')
INTERSECT
(select order_id, order_date from orders
where order_date >= '2016-01-15')) o
ORDER BY o.order_date;

-- 레코드 합치기 (합집합 = UNION) // 1615개
select * from (
	select order_id, order_date from orders
	where order_date <= '2016-01-17'
	union
	select order_id, order_date from orders
	where order_date >= '2016-01-15'
) o order by o.order_date;

-- 레코드 합치기 (합집합 중복허용 = UNION ALL) // 3236개
select * from (
	select order_id, order_date from orders
	where order_date <= '2016-01-17'
	union all
	select order_id, order_date from orders
	where order_date >= '2016-01-15'
) o order by o.order_date;
```

### `JOIN`
두 테이블에서 공통 값을 기준으로 테이블을 하나로 만들어 조회할 수 있도록 결합


```sql
select * from orders, stores
where orders.store_id = stores.store_id; -- 주문이 발생한 매장 정보

```

#### INNER JOIN
- "동등조인(교집합)" / "내부조인"
- INNER 키워드 -> 생략가능함

```sql
-- INNER JOIN / 동등조인 (내부조인)
select * from orders o
	inner join stores s on o.store_id = s.store_id;

-- stores 기준에서 orders 테이블의 정보를 결합
select distinct s.store_id from stores s
inner join orders o on s.store_id = o.store_id;
```

#### OUTER JOIN
- LEFT OUTER JOIN
  - 주 테이블을 반드시 모두 출력한다
  - 보조 테이블은 있는 것만 출력한다
- RIGHT OUTER JOIN
  - 보조 테이블을 반드시 모두 출력한다
  - 주 테이블은 있는 것만 출력한다
- FULL OUTER JOIN
  - 양쪽 모두 주 테이블로 모두 출력한다

```sql
-- LEFT OUTER JOIN - stores가 주테이블로 모든 데이터가 출력, orders는 보조 테이블로 있는 것만 나온다
select * from stores s 
left join orders o on s.store_id = o.store_id
order by s.store_id;

-- 세 개의 테이블 LEFT JOIN
select 
o.order_id, o.order_date,
s.store_name,
f.first_name staff_first_name,
f.last_name staff_last_name,
c.first_name, c.last_name
from orders o
left join customers c on o.customer_id = c.customer_id
left join stores s on o.store_id = s.store_id
left join staffs f on o.staff_id = f.staff_id;

-- FULL OUTER JOIN
select * from stores s
full outer join orders o on s.store_id = o.store_id
order by s.store_id;

-- FULL OUTER JOIN + WHERE
select * from stores s
full outer join orders o on s.store_id = o.store_id
where o.order_id = 17
order by s.store_id DESC;
```


### UNION이랑 JOIN은 같은 것 아닌가요? 둘 다 테이블을 합치잖아요.

- UNION은 **행을 추가**하는 방식이에요. 같은 형태의 데이터를 위아래로 쌓아요.
- JOIN은 **열을 추가**하는 방식이에요. 서로 다른 테이블의 정보를 좌우로 연결해요.
- 예를 들어, 2023년과 2024년 데이터를 하나의 테이블로 통합하려면 UNION을, 주문 데이터에 고객 이름을 붙이고 싶다면 JOIN을 써요.





## 메모할 만한 것들


## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


