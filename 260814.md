# 26.08.14
KANT 10일차, python


## 오늘 느낀 점
- 무거운 데이터를 제너레이터를 이용해 메모리 효율적으로 조회하고 활용 가능하다는 점이 멋있는 것 같다.
- 파이썬 모듈을 활용하여 보다 체계적으로 기능을 파일 단위로 관리하고 사용하면 편리할 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
- iter()와 next()로 데이터를 수동 순회하고 StopIteration 예외 원인을 설명할 수 있다.
- yield 키워드로 제너레이터를 구현하고 지연 평가 개념을 적용할 수 있다.
- 제너레이터 표현식을 이해하고 리스트 컴프리헨션과의 메모리 차이를 비교할 수 있다.
- 파이썬 표준 라이브러리를 가져와서 필요한 기능을 호출할 수 있다.
- as 키워드로 모듈에 별칭을 부여하고 활용할 수 있다.
- datetime 모듈로 현재 시간을 원하는 형식으로 출력할 수 있다.
```

## 목차

- [26.08.14](#260814)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 1. 이터러블(Iterable)

- 반복 가능한 객체
- 리스트, 문자열, 딕셔너리처럼 내부 요소를 하나씩 꺼내어 순회할 수 있는 모든 객체
  - 내부 매직 메서드 `__iter__(...)` 정의되어 있다면 반복 가능!


### 2. 이터레이터(Iterator)
반복을 하려면
- 이터레이터 객체 생성(__iter__ 호출(iter(...)))
- 순서를 한칸씩 이동하면서 요소 조회(__next__ 호출(next(...)))

```python
fruits = ['Apple', 'Orange', 'Melon', 'Banana']

it = iter(fruits) # __iter__(...) 호출

next(it) # 차례로 하나씩 포인터가 이동하면서 꺼낸다
# 끝까지 도달하면 StopIteration에 걸린다.
```

### 3. 제너레이터(Generator)
- 함수의 실행을 정지하고 원할 때 다시 재개하는 형태

```
### yield 키워드를 사용한 함수의 일시 정지 및 상태 보존

**함수의 일시 정지**
---
제어 흐름 중에 yield 키워드를 만나면 함수는 그 시점의 연산 결과값을 함수 밖으로 반환함과 동시에, 자신의 실행 상태(지역 변수 값, 포인터 위치 등)를 메모리에 고스란히 얼려둔 채로 잠시 대기합니다.


**상태 보존과 재개**
---
외부에서 다시 데이터를 요청(next())하면 함수는 처음부터 다시 실행되는 것이 아니라, 얼어있던 상태를 깨워 yield 바로 다음 줄부터 실행을 이어가는 독특한 동작 방식을 취합니다.
```

```python
def generate_nums():
  
  ### next()
  print("1번째")
  yield 10
  ### 함수의 실행 일시중지 ###

  ### next()
  print("2번째")
  yield 20
  ### 함수의 실행 일시중지 ###

  ### next()
  print("3번째")
  yield 30

gen = generate_nums()

next(gen) # 1번째
next(gen) # 2번째
next(gen) # 3번째
```

#### generator를 쓰면 뭐가 좋나요? - '메모리 효율적' + '안전한 실행흐름 보장'
호출될 때마다 값을 하나씩 생성하여 내보내는 '지연 평가(Lazy Evaluation)' 동작 방식

- 리스트, 튜플 등은 한 번에 모든 정보를 메모리에 올린다.
- generator를 사용하면, 호출할 때마다 출력하는 만큼씩만 메모리를 할당한다.
- 무한하거나 무한에 가까운 데이터를 다루더라도 출력 단위만큼씩만 메모리 로드가 이루어지므로 안전한 실행 흐름을 보존

#### 동기 vs. 비동기

- 비동기: 순서대로 작업을 시작하지만 결과는 먼저 처리된 순서대로 나오는 방식


### 4. 제너레이터 표현식
- 참고: 리스트 컴프리헨션 -> 생성 결과가 처음부터 메모리에 적재
- 리스트 대신 튜플로 리스트 컴프리헨션과 동일하게 사용하면 된다. -> 메모리 소비가 적다.
- 대용량 데이터 처리

```python
# 짝수 50개를 가지고 있는 제너레이터
# generator object
gen = (i for i in range(1, 101) if i % 2 == 0)
```

### 5. 독립 소스 파일 모듈(Module)
- 모듈 - 파이썬 파일 하나
- 다른 파일에서 모듈을 불러와서 사용하는 경우 많다
- 모듈명은 파일 이름
- `__name__` : 모듈 이름


#### import, from, as 키워드
- import : 모듈 불러오기
- 불러온 모듈에서 마침표(.)를 통해서 접근
- from 모듈명/패키지명 import ... -> 특정 객체 불러오기
- from 모듈명 import * -> 모듈의 전체 함수 불러오기


### 6. 디렉터리형 패키지(Package)
- 패키지는 모듈을 모아 놓은 디렉터리
- `from 패키지명.하위패키지 import ...`
- 전체 패키지 경로: 절대 경로 접근 예시-`chatper06.mod1`
- 상대 경로 접근법(`..`): 바로 위 패키지 / (`.`): 현재 모듈이 있는 패키지


### 7. 파이썬 표준 라이브러리
[파이썬 공식 독스 - 파이썬 3.13(한국어)](https://docs.python.org/ko/3.13)
- 파이썬이 가지고 있는 내장 모듈
- 자주 쓰는 내장모듈 위주로



## 메모할 만한 것들

### 제너레이터의 확장: 코루틴

```
제너레이터가 yield를 사용하여 함수 실행을 일시 정지하고 원하는 시점에 다시 깨우는 이 독특한 동작 방식은 파이썬의 동시성 프로그래밍인 '코루틴'의 기반이 됩니다. 일반 함수가 호출되면 멈춤 없이 한 방향으로만 실행되고 종료되는 반면, 코루틴은 실행 도중 제어권을 외부로 넘겼다 복귀받으며 서로 협력하듯 주도적으로 실행 흐름을 주고받는 고도의 함수 동작 방식입니다.
```


## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


