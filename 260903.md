# 26.09.03
KANT 23일차, ML


## 오늘 느낀 점
- SQL 쿼리의 문법에 대해서만 알고 있었는데, 데이터 무결성을 위한 시스템이 어떤 방식으로 구축되었는지 알 수 있어서 좋았다.
- 데이터베이스 정규화는 개념이 다소 한번에 이해되지는 않고, 저대로 항상 구현할 수 있을 것 같지도 않지만, 기본적으로 가장 이상적인 테이블 구성의 형태로 이해했다. 실무에서는 반정규화와 같이 어느 정도 타협이 필요할 것 같다.

## 오늘 배운 것

```
### [ 학습목표 ]
- 주요 데이터 타입을 구분할 수 있다
- 스키마 개념을 이해한다
- PK/FK 개념과 제약 규칙을 설명할 수 있다
- 참조 무결성의 의미를 이해한다
- 정규화(1NF~3NF)의 개념과 목적을 설명할 수 있다
- DDL과 DML의 차이를 구분할 수 있다
- CREATE TABLE 문법으로 테이블을 생성할 수 있다
- INSERT INTO 문법으로 데이터를 입력할 수 있다
- 여러 행을 한 번에 입력할 수 있다
```

## 목차

- [26.09.03](#260903)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [SQL](#sql)
      - [DDL 데이터 정의어](#ddl-데이터-정의어)
      - [데이터 무결성 제약조건](#데이터-무결성-제약조건)
      - [REFERENCES 무결성 제약조건 위반](#references-무결성-제약조건-위반)
      - [SERIAL - 자동증감하는 정수형 자료형](#serial---자동증감하는-정수형-자료형)
      - [multi primary key + CHECK 제한조건 + DEFAULT 기본값](#multi-primary-key--check-제한조건--default-기본값)
      - [REFERENCES](#references)
    - [데이터베이스 정규화](#데이터베이스-정규화)
      - [항상 정규화가 정답인 것은 아니다!](#항상-정규화가-정답인-것은-아니다)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


### SQL
1. DDL (Data Definition Language)
   1. CREATE
   2. ALTER
   3. DROP
   4. TRUNCATED TABLE [테이블명] -> 테이블 구조를 유지하고 전체 데이터 초기화.
      1. *TRUNCATE 하더라도, 자동증감(AUTO_INCREMENT) 카운터는 리셋되지 않는다.
2. DML (Data Manipulation Language)
   1. INSERT
   2. UPDATE
   3. DELETE
   4. SELECT 조회
   5. DQL 쿼리
3. DCL (Data Control Language)
   1. [권한]
      1. GRANT 권한부여
      2. REVOKE 권한취소
   2. [트랜잭션] (TCL, Transaction Control Langauge)
      1. COMMIT - 데이터를 DB에 영구적으로 반영
      2. ROLLBACK - COMMIT 이전에 반영 내용 되돌리기 / COMMIT 이후는 사용불가 (영구반영)

#### DDL 데이터 정의어
1. CREATE TABLE 테이블명 (컬럼명1 데이터타입, 컬럼명2 데이터타입, ...)
   1. 컬럼명, 데이터타입, [제약조건 (선택적 작성)]
   2. 기본키 제약조건
      1. PRIMARY KEY - 중복허용X, NULL 허용X
      2. NOT NULL - NULL 허용X, 필수항목(컬럼)
      3. UNIQUE - 중복허용X (NULL은 중복 가능함 // NOT NULL과 같이 쓰는 이유.)
      4. CHECK - 특정 조건에 해당하는지 체크 // 비교,논리연산자 AND OR NOT 등등 사용가능
      5. DEFAULT - 기본값 (직접 지정/내장 함수 호출/내장 상수) // 예시. 현재 날짜와 시간 CURRENT_TIMESTAMP
2. DROP 대상
   1. DROP TABLE 테이블명;
3. FOREIGN KEY
   1. REFERENCES - 부모쪽(외래키X) 테이블에 없는 참조값을 자식테이블(외래키O)이 사용할 수 없다.


```sql
-- 체크 제약조건의 활용

create table CLUB_MEMBERS (
	user_seq INT,
	club_name VARCHAR(50),
	user_name VARCHAR(45) not NULL,
	age INT CHECK(age <= 25),
	created_at TIMESTAMPTZ,
	primary key(user_seq, club_name)
);
```

#### 데이터 무결성 제약조건
1. 도메인 무결성 제약조건 // 자료형 (DDL)
2. 개체 무결성 제약조건 // 기본키 (PRIMARY KEY)
3. 참조 무결성 제약조건 // 외래키 (REFERENCES)


#### REFERENCES 무결성 제약조건 위반
1. 외래키 제약조건 위반 (부모쪽 테이블에 없는 참조값을 자식테이블이 사용하려고 시도함)
2. 부모 레코드에 자식 레코드가 있으면 부모 레코드를 삭제할 수 없다
3. ON DELETE
   1. CASCADE -> 부모 레코드가 삭제 -> 자식 레코드도 함께 삭제
   2. SET NULL -> 부모 레코드가 삭제 -> 자식 레코드의 참조 컬럼의 값을 NULL로 교체
```
SQL Error [23503]: ERROR: insert or update on table "club_members" violates foreign key constraint "club_members_user_seq_fkey"
  세부 정보: Key (user_seq)=(3) is not present in table "members".
```
```sql
-- ON DELETE CASCADE
-- 삭제시 무결성 위반 에러가 발생하지 않고, 규칙에 따라 자식 레코드를 함께 삭제한다.
create table CLUB_MEMBERS (
	user_seq INT references MEMBERS(seq) on delete cascade,
	club_name VARCHAR(40),
	created_at TIMESTAMPTZ default current_timestamp
);
```

#### SERIAL - 자동증감하는 정수형 자료형
- 별도로 INSERT 항목에 포함하지 않아도 자동으로 올라간다.

#### multi primary key + CHECK 제한조건 + DEFAULT 기본값
```sql
create table CLUB_MEMBERS (
	user_seq INT,
	club_name VARCHAR(50),
	user_name VARCHAR(45) not NULL,
	age INT CHECK(age <= 25),
	created_at TIMESTAMPTZ default current_timestamp,
	primary key(user_seq, club_name)
);
```

- 묶어서 지정한 조합 단위로 primary key 제한조건을 체크한다 (NOT NULL UNIQUE)

```
SQL Error [23505]: ERROR: duplicate key value violates unique constraint "club_members_pkey"
  세부 정보: Key (user_seq, club_name)=(1, 전자기타) already exists.
```

#### REFERENCES
```sql
CREATE TABLE 테이블명 (
    컬럼명 자료형 [제약조건 등...] REFERENCES 테이블명(컬럼명)
)
```


### 데이터베이스 정규화
데이터 중복과 이상현상을 개선하기 위해서 테이블의 구조를 단계적으로 개선하는 과정

1. 제1 정규형 (1NF) - 원자값 유지
- 하나의 컬럼에는 하나의 값만 넣는다.

|학번|이름|수강과목|
|---|---|---|
101|김철수|데이터베이스,알고리즘
102|이영희|자바

|학번|이름|수강과목|
|---|---|---|
101|김철수|데이터베이스
102|김철수|알고리즘
103|이영희|자바

2. 제2 정규형 (2NF)

학번(PK)|과목코드(PK)|학생이름|과목명|성적
---|---|---|---|---
101|CS01|김철수|데이터베이스|A
101|CS02|김철수|알고리즘|B+
102|CS01|이영희|데이터베이스|A+

학생 테이블 (기본키: 학번)
학번|학생이름
---|---
101|김철수
102|이영희

과목 테이블 (기본키: 과목코드)
과목코드|과목명
---|---
CS01|데이터베이스
CS02|알고리즘

수강신청 테이블 (기본키: (학번, 과목코드))
학번|과목코드|성적
---|---|---
101|CS01|A
101|CS02|B+
102|CS01|A+


3. 제3 정규형 (3NF)

학번(PK)|학생이름|학과코드|학과위치
---|---|---|---
101|김철수|D01|공학관 3층
102|이영희|D02|IT관 2층
103|박민수|D01|공학관 3층

**학생 테이블 (기본키: 학번, 외래키: 학과코드)**
학번|학생이름|학과코드(FK)
---|---|---
101|김철수|D01
102|이영희|D02
103|박민수|D01

**학과 테이블 (기본키: 학과코드)**
학과코드|학과위치
---|---
D01|공학관 3층
D02|IT관 2층


#### 항상 정규화가 정답인 것은 아니다!
완벽함을 버리고 속도와 확장가능성을 얻는 트레이드오프
- 경우에 따라서 반 정규화가 맞기도 하다
- 완벽한 정합성 -> 데이터 쿼리 복잡도 증가, 처리속도 지연
- 현대 개발 방법론과 서비스 기준으로는 빠른 스케일업을 위한 모듈화가 중요해짐


## 메모할 만한 것들

Docker PostgresSQL 서버 진입 터미널커맨드
```
postgre 도커 내부 컨테이너 접근
docker exec -it postgres bash

postgreSQL 터미널 접근
psql -U postgres

테이블 명세 확인
\d 테이블명;
```


## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


