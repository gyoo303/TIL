# 26.09.15
KANT 31일차, 프론트엔드 / LLM


## 오늘 느낀 점
- 보다 구체적인 기능 동작/실행으로 이어질 수 있는 HTML 문법을 배워서 재미있었다.
- CSS를 통해서 웹사이트를 요소 단위로 꾸미는 방법을 배웠고, Cascading이라는 이름과 같이 자식(하위) 요소에 대해 스타일 정의가 일괄적으로 적용되는 점을 이해할 수 있었다. 선택자 우선순위는 반드시 기억해 두자.

## 오늘 배운 것

```
### [ 학습목표 ]
Frontend (HTML)
- 하이퍼링크와 멀티미디어 태그의 삽입 방식 및 필수 속성을 설명할 수 있다.
- 다양한 폼(Form) 서식 요소를 활용하여 사용자 데이터 입력 화면을 배치할 수 있다.
- 시멘틱 마크업의 개념과 검색엔진 및 웹 접근성 향상 측면의 필요성을 설명할 수 있다.
- HTML5 레이아웃 태그를 활용하여 영역이 명확히 분할된 웹 페이지를 설계할 수 있다.
- <div>와 시멘틱 태그의 차이를 이해하고 사용 기준을 명확히 구분하여 적용할 수 있다.
```

## 목차

- [26.09.15](#260915)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
      - [양식(Form) 태그](#양식form-태그)
    - [시맨틱 태그 - 의미를 가지고 있는 태그](#시맨틱-태그---의미를-가지고-있는-태그)
      - [레이아웃을 구성하는 태그](#레이아웃을-구성하는-태그)
    - [CSS \& 스타일링](#css--스타일링)
      - [CSS (Cascading Style Sheet)](#css-cascading-style-sheet)
      - [웹 문서에 스타일을 정의한다.](#웹-문서에-스타일을-정의한다)
      - [주석 -\> `/* ... */`](#주석-----)
      - [선택자 (selector)](#선택자-selector)
      - [스타일 적용 우선순위 (selector)](#스타일-적용-우선순위-selector)
      - [(참고만) 스타일 우선순위 강제 적용](#참고만-스타일-우선순위-강제-적용)
      - [복합 관계 선택자](#복합-관계-선택자)
  - [메모해 둘 것](#메모해-둘-것)
    - [W3Schools 및 공식 레퍼런스 활용](#w3schools-및-공식-레퍼런스-활용)
    - [기타 다양한 입력 양식을 만드는 `<input>` 타입들](#기타-다양한-입력-양식을-만드는-input-타입들)
    - [HTML 태그 레퍼런스\_목록을 구조화하는 리스트 태그 계열](#html-태그-레퍼런스_목록을-구조화하는-리스트-태그-계열)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


**어제(9.14) HTML 강의에서 계속됨**

#### 양식(Form) 태그
- input : 사용자 입력을 위한 태그
  - target 
    - `__self`, `_parent`, `_top`, `_blank`
  - action : 제출 처리 URL (*서버URL)
  - method : 전송 방식
    - GET (기본값) - 읽기/검색
      - 전송 데이터는 URL에 포함 
      - **쿼리 스트링** -> `_?이름=값&이름=값&이름=값 ...`
    - POST - 쓰기, 서버에 변경을 가하는 요청
      - 회원가입, 글작성, 상품등록
      - 요청 전문의 body에 쿼리 스트링 형태로 담아서 전송
      - URL-encoded (16진수 인코딩, default) or JSON -> "content-type" 
  - type : 입력 유형
    - text (기본값)
    - password 비밀번호
    - radio 여러개 중 1개 선택
    - checkbox 여러개 중 여러개 선택
    - submit 제출 (reset-입력항목 초기화, button-버튼)
  - name
    - 전송할 값의 이름
    - radio, checkbox 그룹 명칭
  - value
    - 입력값
    - type="text" / "password" / "number"
      - 사용자 입력 -> 값 할당
  - button
    - type
      - submit : 양식 폼 제출
      - reset : 양식 폼 입력 취소
      - button : 일반 버튼
      - email
      - number
      - color
      - range 범위
      - date 날짜
      - time 시간
      - datetime 날짜+시간
    - `<button ...>버튼명</button>`
  - 태그와 입력 요소 연결
    - 입력 요소를 태그로 감싼다 -> label 태그를 주로 사용
    - for(label) -> id 속성(입력요소)을 연결시킨다
      - `<label for="hobby_python">파이썬 공부</label>`
  - `textarea` -> 여러 줄 텍스트 입력
  - `required` -> 필수입력항목 지정
  - `placeholder` -> 안내문구
  - `readonly` -> 읽기 전용 (데이터 전송o)
  - `disabled` -> 기능 비활성화 (데이터 전송x)
  - `select` : 여러개 중 한개 또는 여러개
    - `option` (하위)선택항목
      - `selected` : "선택됨" 상태
    - `multiple` : 여러개 선택 *마우스 드래그
    - `size` : 한번에 보일 갯수


```html
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>팀스파르타</title>
    </head>
    <body>
        <h1>회원 가입</h1>
        <form method = "POST" action="sign_up.html">
            이메일 <input type="email" name="email" value="user01@test.org" required> <br>
            비밀번호 <input type="password" name="password" required> <br>
            비밀번호 확인 <input type="password" name="confirm_password" required> <br>
            회원이름 <input type="name" name="username" required> <br>
            성별
                <label>
                    <input type="radio" name="gender" value="FEMALE" checked>여성
                </label>
                <label>
                    <input type="radio" name="gender" value="MALE">남성<br>
                </label>
            취미
                <input type="checkbox" id="hobby_python" name="hobby" value="python">
                <label for="hobby_python">파이썬 공부</label>

                <input type="checkbox" id="hobby_machine" name="hobby" value="machine">
                <label for="hobby_machine">머신러닝 공부</label>
                
                <input type="checkbox" id="hobby_reading" name="hobby" value="reading">
                <label for="hobby_reading">독서</label>
            
                <br>

            자기소개<br><textarea name="introduction" rows="5" cols="100" placeholder="두 줄 이상 입력하세요."></textarea><br>

            태어난 년도
            <select name="birth_year" multiple size="2">
                <option value="2020">2020년</option>
                <option value="2019">2019년</option>
                <option value="2018">2018년</option>
                <option value="2017" selected>2017년</option>
                <option value="2016">2016년</option>
            </select>
            
            
            <br>
            <!-- <input type="submit" value="회원가입"> -->
            <button type="submit">회원가입</button>
        </form>
    </body>
</html>
```


### 시맨틱 태그 - 의미를 가지고 있는 태그
- (예) `<form>`, `<table>`, `<video>`
  - 비시맨틱 태그: `<div>`, `<span>`
- 필요한 이유
  - 검색엔진 최적화
  - 웹 접근성
  - 코드 가독성

#### 레이아웃을 구성하는 태그
- `<header>` : 상단 공통 영역 (1개)
- `<footer>` : 하단 공통 영역 (1개)
- `<main>` : 본문 (1개)
- `<section>` : 내용 구분
- `<article>` : 섹션별 내용
- `<mav>` : 주 메뉴
- `<aside>` : 왼쪽, 오른쪽 영역 정의


### CSS & 스타일링

#### CSS (Cascading Style Sheet)
- Cascading -> "연쇄", "상속"
- 상위 요소에 대해 정의한 내용은 자식 요소에게도 적용된다.

#### 웹 문서에 스타일을 정의한다.

- 스타일 적용 기본 구조
    - 선택자(selector)
      - `{속성(property): 값(value); 속성:값; ...}`
      - 스타일 속성
        - color, background, width, height, ...
- 적용방법
  - 인라인 스타일
    - 태그의 `style` 속성에 직접 적용
    - `<li style="color: orange; background:black;">감</li>`
  - 내부 스타일 시트
    - `<style> ... </style>` 태그 안에 스타일을 정의
    - ```<style>
        li {
            color: blue;
            background: gray;
        } </style>
  - **외부 스타일 시트** (가장 보편적)
    - 외부 css 파일 연동 (`<link ... >`)


#### 주석 -> `/* ... */`
- 설명
- 해석 배제 // stylesheet에 읽기 좋게 작성

#### 선택자 (selector)
- 전체 선택자 (`*`)
- 태그 선택자 (`태그 이름`)
- 클래스 선택자 (`.클래스명`)
  - 여러 개의 요소를 선택 가능 / 한 요소에 여러 클래스명 지정 가능
    - `<li class="kia discontinued">모하비</li>`
- 아이디 선택자 (`#아이디명`)
  - 아이디 명은 하나만 지정 가능, 유일한 이름으로 지정

#### 스타일 적용 우선순위 (selector)
- tag < class < id < style 속성
  - 단, 요소에 대해 직접 style 속성 명시는 좋지 않은 패턴 (수정이 어렵다)
  - 공통 스타일 -> tag 선택자
  - 세부 스타일 -> class 선택자
    - id 선택자나 style 속성은 거의 사용X
- 범위가 넓을수록 (해당 요소가 많을수록) 우선순위 낮다
- 범위가 좁을수록 (해당 요소가 적을수록) 우선순위 높다
- 동일 선택자에 대한 스타일 지정은 덮어쓰기 가능하다 (나중에 재정의할 경우 최신버전으로)

#### (참고만) 스타일 우선순위 강제 적용
- 속성 값 다음에 `!important` 붙이기
- 어지간하면 사용하지 않는 것을 권장


#### 복합 관계 선택자
- 하위 선택자 (조상-자손) 
  - `ol li span {background: orange;}`
- 자식 선택자 (바로 위/아래 요소)
  - `li div > span`
- 일반 형제선택자 (형~동생)
  - `.menu5 ~ li {background: gray;}`
    - `class=menu5` 아래에 정의된 형제들을 모두 선택한다.
- 인접 형제 선택자 (형+동생)(기준 -> 기준 바로 아래 요소 1개)
  - `.menu5 + li { background: red; }`





## 메모해 둘 것

### W3Schools 및 공식 레퍼런스 활용

프론트엔드 개발자가 모든 HTML 태그와 속성을 외워서 코딩하는 것은 불가능에 가깝습니다. 실무에서 막히는 부분이 생겼을 때, 공식 문서와 레퍼런스를 검색하여 필요한 정보를 빠르게 찾아내는 능력이 훨씬 더 중요합니다.

**W3Schools HTML 레퍼런스 활용법**

**이용 주소**: [W3Schools HTML Tag Reference](https://www.w3schools.com/tags/)

***핵심 기능 [Try It Yourself]***
각 태그 설명 페이지에 있는 초록색 버튼을 클릭해 보세요. 별도의 설치 없이 브라우저 상에서 왼쪽 코드 창을 수정하면 오른쪽 결과 창에 실시간으로 반영되어 즉각적인 실험이 가능합니다.

**MDN Web Docs 활용법**

**이용 주소**: [MDN Web Docs - HTML](https://developer.mozilla.org/ko/docs/Web/HTML)

***특징***
모질라(Mozilla) 재단에서 관리하는 가장 공신력 있고 전문적인 개발자용 기술 문서입니다. 한국어 번역이 매우 잘 되어 있어, 특정 태그가 표준 웹 접근성에 맞게 설계되었는지 깊이 있게 공부할 때 강력 추천합니다.


### 기타 다양한 입력 양식을 만드는 `<input>` 타입들

| **<input type="...">** | **관련 속성 예시** | **동작 특징 및 특징** | **실제 활용 사례** |
| --- | --- | --- | --- |
| **type="number"** | min, max, step | 오직 숫자만 입력할 수 있는 란을 만들며, 우측에 증감 단추가 생깁니다. | 상품 수량, 나이, 금액 입력 |
| **type="range"** | min, max, step | 슬라이더 바를 조작하여 마우스 드래그로 손쉽게 범위 내 수치를 조절합니다. | 서비스 만족도 점수, 볼륨 조절 |
| **type="date"** | min, max | 달력 모양의 그래픽 위젯을 팝업하여 연/월/일을 편하게 선택합니다. | 예약 날짜, 생년월일, 만료일 |
| **type="time"** | 없음 | 시간과 분(오전/오후)을 설정할 수 있는 전용 위젯을 띄웁니다. | 약속 시간, 수강 교시 지정 |
| **type="datetime-local"** | 없음 | 사용자가 거주하는 국가의 현지 표준 시간대를 기준으로 날짜와 시간을 동시에 선택합니다. | 열차 승차 일시, 마감 기한 설정 |
| **type="color"** | 없음 | 시스템 내장 색상표(Color Picker)를 호출하여 시각적으로 색상을 선택합니다. | 테마 커스텀, 개인 프로필 컬러 지정 |
| **type="email"** | 없음 | 모바일 자판이 @ 기호 포함 자판으로 변하며, 전송 시 이메일 유효성 검사를 수행합니다. | 이메일 수신 주소 입력 |
| **type="file"** | accept (예: image/*) | 내 컴퓨터 내에 보관 중인 이미지, 문서 파일 등을 서버로 업로드하도록 돕습니다. | 이력서 업로드, 프로필 사진 변경 |

### HTML 태그 레퍼런스_목록을 구조화하는 리스트 태그 계열

| **태그명** | **의미** | **설명 및 핵심 역할** | **활용 사례** |
| --- | --- | --- | --- |
| <ul> | Unordered List | 순서가 없는 목록을 생성하며, 항목 앞에 기본 점(•) 기호가 붙습니다. | 웹사이트 상단 메뉴 목록, 카테고리 리스트 |
| <ol> | Ordered List | 순서가 있는 목록을 생성하며, 항목 앞에 1, 2, 3... 숫자가 붙습니다. | 요리 레시피 순서, 인기 검색어 순위 |
| <li> | List Item | <ul>이나 <ol> 내부에서 개별 항목 하나하나를 나타내는 태그입니다. | 리스트에 들어갈 실제 데이터 내용물 |
| <dl> | Description List | 용어와 그에 대한 정의/설명을 세트로 묶어주는 설명형 목록의 시작입니다. | 단어 사전, 상품 사양 고시 |
| <dt> | Description Term | 설명형 목록(<dl>) 내에서 정의할 용어(제목)를 지정합니다. | 상품명, 질문 항목(Q) |
| <dd> | Description Details | 설명형 목록(<dl>) 내에서 용어에 대한 상세 설명(본문)을 적습니다. | 상품 상세 정보, 답변 내용(A) |


## Daily Quest 퀴즈 오답
Q. 블로그 포스팅이나 뉴스 기사처럼, 내용 자체만 떼어내어 다른 곳에 붙여도 독립적으로 완전히 이해가 되는 콘텐츠를 감쌀 때 사용하는 HTML5 시멘틱 태그는 무엇인가요?
- 정답: `<article>`
- 해설: `<article>`은 독립적으로 배포 가능한 단독 콘텐츠(신문 기사, 블로그 포스트 등)를 감싸는 대표적인 시멘틱 태그입니다. `<section>`은 주제별로 연관된 내용을 묶는 용도로 `<article>`과 차이가 있습니다.
- 틀린 이유: `<article>` 태그가 기억나지 않음
  

## 진단 퀴즈 오답
- 없음
