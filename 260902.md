# 26.09.02
KANT 22일차, ML


## 오늘 느낀 점
- 머신러닝 기초를 쭉 배워오면서, 현재 상용 서비스되는 모델에 근접한 모델들을 사용해 보니 감개가 무량하다.
- 모델 자체의 성능을 완전하게 끌어올리기 위해서 고안된 텍스트 전처리에 대해 배웠는데, 한국어는 영어에 비하여 토크나이징이 까다로운 편이기에 성능에도 영향을 일부 미치지 않을까 싶다. 영어권 유저들은 더 좋은 성능의 챗지피티를 쓰고 있는 걸까?
- 데이터베이스의 꽃인 SQL에 대해 간략하게 알아보았고, Docker를 설치해서 Postgres DB 서버를 올려보는 실습을 진행했다. Docker는 AX 실무에서 매우 유용할 것 같아서 꼭 써보고 싶었는데, 이후 튜터님이 주실 예정인 교안 or 특강이 기대된다. AWS 서버에 도커 이미지를 올려서 쓰는 방식도 해보면 재미있을 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
- 허깅페이스(Hugging Face) 라이브러리를 활용해 토크나이저와 AI 모델을 로드하고 구동하는 전체 프로세스를 이해하고 실행할 수 있다.
- 텍스트 전처리의 핵심 기법인 BPE, WordPiece, SentencePiece의 개념과 동작 차이점을 명확히 설명할 수 있다.
- 트랜스포머의 3가지 아키텍처 유형을 코드로 구현하고 구조적 차이점을 비교·분석할 수 있다.

- DS 실무에서 SQL이 필요한 이유를 말할 수 있다
- 데이터 사이언티스트의 역할과 핵심 역량을 파악한다
- 데이터베이스의 정의와 DBMS의 역할을 설명할 수 있다
- 관계형 DB의 핵심 구조(테이블·행·열·셀)를 설명할 수 있다
```

## 목차

- [26.09.02](#260902)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [트랜스포머](#트랜스포머)
    - [데이터베이스](#데이터베이스)
      - [DBMS (DataBase Management System)](#dbms-database-management-system)
      - [SQL (Structured Query Language)](#sql-structured-query-language)
      - [데이터 무결성 제약조건](#데이터-무결성-제약조건)
      - [자료형 (Data Type)](#자료형-data-type)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 트랜스포머
- 인코더 Bert
- 인코더-디코더 BART (요약)
- 디코더
  - GPT (Generative Pre-trained Transformer), Gemini, 상용 모델들
  - Google Gemma

> 토크나이저 -> 토큰을 나누는 기능 (토크나이저의 기준 적용) // BPE, 센텐스리스


### 데이터베이스
- 데이터: "처리되지 않은 값(원시값)"
  - 이름, 나이, 구매금액
- 정보: "데이터에 의미를 부여"

#### DBMS (DataBase Management System)
데이터를 관리하는 소프트웨어
- RDBMS : 테이블 간의 관계를 명시함으로써 연결된 데이터를 설계하고 조회한다.
  - MySQL(오픈소스) / Oracle(유료) / MS SQL(유료) / PostgreSQL(오픈소스) / MariaDB (오픈소스)

#### SQL (Structured Query Language)
- 쿼리: 데이터베이스에 보내는 질문, 질의
- 작성 규칙: SQL
- 테이블(Table) : 데이터를 저장하는 단위 공간
  - 행(row, record)
  - 열(column, field, 속성)
  - 셀(각각의 데이터-주문번호,주문상품)
  - 스키마(scheme) -> 테이블 구조와 제약사항을 정의한 명세서/설계도

#### 데이터 무결성 제약조건
1. 도메인 무결성 제약조건 -> 동일 컬럼, 동일 자료형
2. 개체 무결성 제약조건
   1. 기본키(Primary Key) 제약조건: 중복값X, NULL값 X // 하나의 레코드를 식별할 수 있는 대표컬럼 또는 컬럼의 조합
3. 참조 무결성 제약조건
   1. FK가 참조하는 값이 반드시 참조 대상 테이블의 PK에 존재해야 한다


#### 자료형 (Data Type)
1. 숫자형
   1. 정수형
      1. SMALLINT (2byte)
      2. **INT (4byte)**
      3. **BIGINT (8byte)**
   2. 실수형
      1. REAL (4byte)
      2. DOUBLE PRECISION (8byte)
      3. DECIMAL(p,s) : p 전체 자리수, s 소수점 자리수
2. 문자형
   1. CHAR(자리수) : 고정 사이즈 문자열 (예)남자, 여자
   2. VARCHAR(최대자리수) : 최대 자리수 만큼의 문자열 (*최대를 넘지 않으면 문자크기 만큼만 공간 할당)
   3. TEXT : 여러 줄로 구성된 텍스트 (길이 제한이 없는 가변문자열)
3. 논리형
   1. BOOLEAN : TRUE, FALSE, NULL (1byte)
4. 날짜와 시간
   1. DATE : 날짜
   2. TIME : 시간
   3. TIMESTAMP : 날짜+시간 
   4. **TIMESTAMPZ** : 날짜+시간+시간대 (디폴트 UTC+0 영국시간)
5. JSON
   1. **JSONB** -> 바이너리 형태로 저장
   2. JSON
6. 기타
   1. ENUM : 열거형
      1. ex. 주문상태 - READY / ORDER / IN_CASH / DELIVERY
      2. ENUM("REDAY", "ORDER", "IN_CASH", "DELIVERY")
   2. VECTOR(차원수)


## 메모할 만한 것들
- [ERD클라우드](https://www.erdcloud.com/) - 시스템 시각화 툴
- [DBeaver](https://dbeaver.io/)

Docker Init 커맨드
```
docker --version
docker compose up -d # 도커 이미지 생성
docker ps -a # 도커 구성 정보 확인
docker logs -f postgres # postgres 서버 구동 가능여부 확인(테스트) / 포트 = 데이터 통로 넘버
```


## Daily Quest 퀴즈 오답
Q. 다음 중 SentencePiece 토크나이저의 특징으로 옳은 것은?
- [내가 선택한 답] C. 통합했을 때 코퍼스의 우도(Likelihood)를 통계적으로 계산하여 분할 여부를 결정한다
- [해설] SentencePiece는 공백 자체를 특수 기호(예: _)로 처리하는 독립형 전처리 라이브러리로, 띄어쓰기 규칙이 불규칙한 한국어나 공백이 없는 중국어·일본어에 효과적입니다. 첫 번째 선지는 BPE, 세 번째 선지는 WordPiece의 설명이며, 네 번째는 토크나이저의 역할과 무관한 내용입니다.
- [틀린 이유]
  - 기억나지 않았음


Q. 트랜스포머 디코더에서 학습 시 미래의 정답 단어를 미리 참조하지 못하도록 차단하는 기법의 이름은?
- [내가 선택한 답] A. 패딩 마스킹(Padding Masking)
- [정답] 인과적 마스킹(Causal Masking)
- [해설]
  - 인과적 마스킹은 하삼각행렬 형태의 마스크를 적용해 미래 시점의 단어 정보를 -무한대로 만들어 소프트맥스 확률을 0으로 만들어 컨닝을 방지합니다. 패딩 마스킹은 의미 없는 패딩 토큰을 무시하는 용도이고, 위치 인코딩과 층 정규화는 각각 순서 정보 부여와 값 안정화를 위한 별개의 기법입니다.
- [틀린 이유]
  - 용어를 헷갈림

## 진단 퀴즈 오답
- 없음


