# 26.08.18
KANT 11일차, python


## 오늘 느낀 점
- 객체지향이라는 개념이 프로그래밍 언어로 어떻게 현실 세계를 코드로 바꿀 것인가에 대한 하나의 관점이라고 이해했다. 조금 과장을 보태자면 '모든 것은 객체로 표현되고 만들 수 있다'라는 것이 객체지향이 아닐까?
- 클래스 상속은 포켓몬 교배 시스템이 떠올라서 재미있었다. 파이썬은 특히 다중상속이 가능하다는 점에서 흥미로웠는데, 자유도를 준 만큼 리스크도 있을 것 같다(휴먼에러 등)
- 클래스 속성의 접근 제어의 경우, 나 혼자만 개발하는 코드가 아니라 다수가 협업하고 사용할 경우 유용할 것 같다.

## 오늘 배운 것

```
### [ 학습목표 ]
- 객체지향 프로그래밍(OOP)의 개념을 이해하고 클래스와 객체의 관계를 설명할 수 있다.
- 인스턴스 생성자(__init**__**)를 정의하고 객체의 고유 속성과 메서드를 구현할 수 있다.
- 클래스 상속의 이점을 설명하고 부모 클래스의 기능을 이어받아 메서드를 재정의할 수 있다.
- 접근 제어 명명 규칙을 적용하여 객체의 민감한 데이터를 외부에 드러나지 않게 은닉할 수 있다.
```

## 목차

- [26.08.18](#260818)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [객체(Object)](#객체object)
      - [객체 지향 프로그래밍](#객체-지향-프로그래밍)
    - [클래스 = 붕어빵 틀](#클래스--붕어빵-틀)
      - [인스턴스 = 틀로 만든 붕어빵](#인스턴스--틀로-만든-붕어빵)
      - [클래스와 인스턴스 @메모리영역](#클래스와-인스턴스-메모리영역)
      - [초기화 함수 `__init__` (생성자)](#초기화-함수-__init__-생성자)
      - [self 키워드](#self-키워드)
      - [속성(Attribute)와 인스턴스 메서드(Method)](#속성attribute와-인스턴스-메서드method)
      - [`__str__`과 `__repr__`](#__str__과-__repr__)
    - [상속과 캡슐화](#상속과-캡슐화)
      - [상속(Inheritance)](#상속inheritance)
      - [메서드 오버라이딩(Overriding)](#메서드-오버라이딩overriding)
      - [`isinstance(참조변수, 자료형)`](#isinstance참조변수-자료형)
      - [다중상속 - 파이썬은 여러 부모 클래스를 상속 관계로 정의 가능](#다중상속---파이썬은-여러-부모-클래스를-상속-관계로-정의-가능)
    - [정보 은닉과 캡슐화 FOR "통제"](#정보-은닉과-캡슐화-for-통제)
  - [메모할 만한 것들](#메모할-만한-것들)
    - [\<객체지향, 클래스\> 개념 요약 키워드](#객체지향-클래스-개념-요약-키워드)
    - [객체지향/클래스 관련 용어 사전](#객체지향클래스-관련-용어-사전)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 객체(Object)
코드로 표현된 의미/기능 집합체의 단위
- 정의
  - `속성` (변수)
  - 행위 (함수) = `메서드`

#### 객체 지향 프로그래밍
- "객체 간의 상호작용"이라는 개념/관점으로 프로그래밍 하는 방식

### 클래스 = 붕어빵 틀
![맛있는 붕어빵](https://img.danuri.io/catalog-image/266/754/005/5d89e3adf20c47dba5422473c6f62e96.jpg)
- 인스턴스 객체를 만들기 위한 설계도 객체
- "이 객체는 ~ 속성을 가지고 있고, ~기능을 가지고 있어야 해"

```python
class Bus:
    # 속성 정의
    def __init__(self): # 초기화 함수 (변수 최초 생성 시 최초 값을 대입하는 것)
        self.count = 0 # 승객 수 초기화 
        self.passengers = []
    
    # 행위 정의
    def ride(self, passenger):
        self.count += 1 # 승객수 증가
        self.passengers.append(passenger)

        print("탑승")
    
    def off(self, passenger):
        if self.count > 1:
            self.count -= 1 # 승객수 차감
            self.passengers.remove(passenger)
        
        print("하차")
    
    def show_count(self):
        print("승객 수:", self.count)

    def print_self(self):
        print(id(self))

class Passenger:
    pass

bus = Bus()
p1 = Passenger()
p2 = Passenger()

bus.ride(p1)
bus.ride(p2)

bus.show_count()
```


#### 인스턴스 = 틀로 만든 붕어빵
![맛있는 붕어빵](https://asset.m-gs.kr/prod/1002492371/1/550)

- 실제로 존재한다 = 실체 = instance
- 붕어빵 인스턴스
  - 속성을 갖는다 (가격, 가치, 무게)
  - 메서드를 갖는다 (먹는다, 판매한다)

```python
bus1 = Bus() # 설계도로 만든 버스 객체(인스턴스)

# 마침표(.) 연산자를 통해서 객체의 (인스턴스)변수, 메서드에 접근 가능

print("탑승객 수:", bus.count)
bus1.ride()

bus2 = Bus() # 두번째 인스턴스
print(bus1 is bus2) # False
```

#### 클래스와 인스턴스 @메모리영역
- 클래스 (설계도)
  - 힙 영역 - PyTypeObject - PyCodeOjbect 영역 내부에 할당
- 클래스의 호출 (`bus1 = Bus()`)
  - 콜스택 영역에서 할당
  - 인스턴스 만들고 해제
- 클래스 인스턴스(bus1)
  - 클래스 호출로부터 생성
  - 콜스택 PyFrameObject --참조--> 힙영역 Bus Object


#### 초기화 함수 `__init__` (생성자)
- 객체가 생성이 되면 "즉시 자동으로" 가장 먼저 호출되는 메서드
- 객체가 생성된 직후에 해야 할 처음 작업들 (초기화 작업)
- 주로 하는 작업은 인스턴스 변수에 최초로 값을 할당(초기화)


#### self 키워드
- 클래스 내부에서 메서드의 첫번째 매개변수로 정의
- 객체가 생성이 되면 객체의 자원을 접근하기 위한 참조 변수
- 클래스 내부에서만 사용 가능


#### 속성(Attribute)와 인스턴스 메서드(Method)
- 속성 : 객체마다 정의된 (인스턴스)변수
- 메서드 : 객체가 가지고 있는 함수

#### `__str__`과 `__repr__`
- 클래스 인스턴스를 바로 프린트 했을 때, 주요 정보를 표시하려면 어떻게 해야할까?
- `__str__` : print로 객체의 참조변수를 출력하면 자동 호출되는 메서드
  - 일반적으로 객체 내의 인스턴스 변수의 내용을 출력한다
- `__repr__` : print를 하면 자동 호출되지만, `__str__`보다 우선순위가 낮다 - 로깅목적(디버깅)
  - 일반적으로 클래스명(매개변수=값, ...) -> eval() 문자열을 실행했을 때 객체가 만들어지는 형태로 보통 정의
  - repr(객체 참조변수)
- `eval(문자열)` : 문자열을 파이썬 코드로 해석해서 실행
  - **보안상 위험할 수 있으므로 사용에 유의**

```python
bus = Bus()
print(bus) # <__main__.Bus object at 0x00000...>

class Bus:
    # 속성 정의
    def __init__(self): # 초기화 함수 (변수 최초 생성 시 최초 값을 대입하는 것)
        self.count = 0 # 승객 수 초기화 
        self.passengers = []
    
    ...

    def __str__(self):
        return f"승객 수: {self.count}, 승객: {self.passengers}"
    
    def __repr__(self):
        return "REPR"

bus = Bus()
print(bus) # 승객 수: 0, 승객: []

class Passenger:
    # self.name, self.age -> 객체가 생성되면 만들어지는 인스턴스 변수(클래스 속성)
    # (..., name, age) -> 객체를 생성하기 위해서 매개변수로 정의된 지역변수
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def __str__(self):
        return f"{self.name}({self.age}세)"

    # 관례적으로 이렇게 정의해서 씀
    def __repr__(self):
        return f"Passenger(name='{self.name}', age={self.age})"

bus = Bus()
p1 = Passenger("고길동", 56)
p2 = Passenger("고말숙", 19)

bus.ride(p1)
bus.ride(p2)

print(bus) 
# 승객 수: 2, 승객: [<__main__.Passenger object at 0x00000...>, <__main__.Passenger object at 0x00000...>]
# 단일 출력 호출이 아니면 __str__ 적용되지 않음.
print(p1) # 고길동(56세)
print(p2) # 고말숙(19세)

p1 = Passenger("김철수", 20)
print(p1) # __str__ 호출
print(repr(p1)) # __repr__ 호출

pp = eval(repr(p1)) # Passenger 인스턴스 반환
print(p1) # 김철수(20세)
```


### 상속과 캡슐화

![상속](https://dimg.donga.com/wps/NEWS/IMAGE/2020/02/03/99513346.1.jpg)

#### 상속(Inheritance)
- 클래스 기반의 상속 (상속에 대한 정의 -> 객체 생성(상속 관계 성립))
- `super` : 부모 클래스를 가리키는 참조 변수
  - 자식 클래스가 상속받으려면 우선 `super()`로 부모 인스턴스를 생성하고 `super().__init__(...)`으로 부모 클래스 속성값으로 초기화한다.
  - `self` : 현재 (자식 클래스) 객체의 참조 변수

[컨벤션] 
- 부모클래스: 일반적인 정의, 상위 개념 (공통 속성, 공통 메서드)
- 자식클래스: 구체적인 정의, 하위 개념 (특수 속성, 특수 메서드)

```python
class Animal:
    def __init__(self, kind):
        self.kind = kind # 동물 종류

    def move(self):
        print(f"{self.kind}이(가) 움직인다")

class Dog(Animal):
    
    def __init__(self):
        # 상속(부모클래스)과 상관없이 Dog라는 클래스 속성을 선언하는 것.
        # self.kind = "강아지"

        # 부모쪽 인스턴스 변수인 kind에 초기화
        super().__init__("강아지")
        
class Cat(Animal):
    def __init__(self):
        super().__init__("고양이")

animal = Animal()
dog = Dog()

animal.move()
dog.move()
```

#### 메서드 오버라이딩(Overriding)

![덮어씌우기](https://thx2.sfo2.cdn.digitaloceanspaces.com/detail_meaning/%EB%8D%AE%EC%96%B4%EC%94%8C%EC%9A%B0%EA%B8%B0_1.webp)

- 메서드 재정의
- 부모쪽 클래스와 동일한 이름으로 정의하여 기능을 수정, 재정의
- 자식쪽 클래스에 정의된 동일한 이름의 함수가 호출 우선순위를 가진다.
- 자식쪽에서 부모쪽 동일한 이름의 메서드를 호출해야하는 경우:
  - `self`로 접근하면 안 됨 -> `super().부모메서드(...)`

```python
class Dog(Animal):
    def __init__(self):
        super().__init__("강아지")

    def move(self):
        super().move() # 부모쪽 객체의 move()
        print("왈왈 컹컹 으르르르")

class Cat(Animal):
    def __init__(self):
        super().__init__("고양이")

    def move(self):
        print("하ㅡ악")

dog = Dog()
cat = Cat()

dog.move()
cat.move()
```

```python
class Human:
    def move(self):
        print("두발로 직립보행한다.")

human = Human()
human.move()
```

#### `isinstance(참조변수, 자료형)`
- 객체의 출처(클래스 자료형) 확인하는 함수

```python
human = Human()
print("human은 Animal 클래스로부터 생성되었나?", isinstance(human, Animal)) # False

print("human은 Human 클래스로부터 생성되었나?", isinstance(human, Human)) # True

dog = Dog()
print("dog는 Animal 클래스로부터 생성되었나?", isinstance(dog, Animal)) # True

print("dog는 Dog 클래스로부터 생성되었나?", isinstance(dog, Dog)) # True


##############################

s = "ABC"
# 문자열인가?
print(isinstance(s, str)) # True
```

#### 다중상속 - 파이썬은 여러 부모 클래스를 상속 관계로 정의 가능

- Method Resolution Order'
  - 우선순위가 같다면 먼저 호출된 메서드를 채택한다.
  - (우선순위) 자식 클래스 메서드 -> 상속 관계에서 먼저 호출된 메서드 -> 나중에 호출된 메서드 -> object

```python
class ParentA:
    def a(self):
        print("ParentA - a()")
    def b(self):
        print("ParentA - b()")

class ParentB:
    def a(self):
        print("ParentB - a()")
    def c(self):
        print("ParentB - c()")

class Child(ParentA, ParentB):
    pass

c = Child()
c.b()
c.c()

### 중복된 부모 메서드 a()를 실행하면?
### Method Resolution Order
c.a() # "ParentA - a()"

class Child(ParentB, ParentA):
    pass

c = Child()
c.a() # "ParentB - a()"
```

Method Resolution Order 확인하기
- `__mro__`
```python
# (__main__.Child, __main__.ParentB, __main__.ParentA, object)
Child.__mro__ 
```

### 정보 은닉과 캡슐화 FOR "통제"
GET-SET 메서드의 장점
- 인스턴스 변수에 대한 직접 접근 및 수정을 방지한다.
- GET-SET 메서드를 통해 검증 로직을 추가할 수 있다.

```python
class Schedule:
    def __init__(self, year, month, day):
        self.__year = year
        self.__month = month
        self.__day = day
    
    def __str__(self):
        return f"year={self.__year}, month={self.__month}, day={self.__day}"

    def get_year(self):
        return self.__year

    def set_year(self, year):
        self.__year = year

    def get_month(self):
        return self.__month

    def set_month(self, month):
        self.__month = month

    def get_day(self):
        return self.__day

    def set_day(self, day):
        if self.__month == 2 and day > 28:
            day = 28
        self.__day = day

s1 = Schedule(2026, 8, 18)
print(s1)

# 수정불가
s1.year = 2026
s1.month = 2
s1.day = 28
print(s1)

s1.set_day(31)
print(s1)
```




## 메모할 만한 것들
- `https://pypi.org/`
  - 파이썬 패키지 위키? 

### <객체지향, 클래스> 개념 요약 키워드
- **객체지향 설계 관점:** 프로그램을 순차적 명령어의 흐름으로만 보지 않고, 고유한 상태를 지닌 정형 데이터(속성)와 이를 다루는 행동(메서드)을 하나의 단위로 묶어 객체의 역할과 책임을 중심으로 구성하는 소프트웨어 설계 방식입니다.
- **클래스와 인스턴스:** 객체가 가져야 할 속성과 기능을 정의한 추상적인 틀(클래스)과, 이를 기반으로 실제 메모리 공간을 할당받아 생성된 개별 실체(인스턴스)의 관계입니다.
- **__init__ 초기화 함수:** 인스턴스가 생성 완료되는 시점에 자동 실행되어 객체에 필요한 속성 데이터들의 초깃값을 바인딩해 주는 기본 초기화 함수입니다.
- **self 키워드:** *여러 개의 다중 객체 인스턴스가 실행될 때*, 지금 연산을 수행 중인 대상이 정확히 어떤 객체인지를 파이썬 인터프리터에 명시하여 주소를 추적 연결하는 자기 참조형 매개변수입니다.
- **속성과 인스턴스 메서드:** 객체가 가지는 고유의 상태 데이터인 '속성(변수)'과, self를 통해 해당 속성에 접근하여 데이터를 제어하는 '인스턴스 메서드(함수)'가 유기적으로 결합된 구조입니다.

### 객체지향/클래스 관련 용어 사전
| **용어** | **영어 표기** | **설명** |
| --- | --- | --- |
| **객체 지향 프로그래밍** | Object-Oriented Programming (OOP) | 프로그램을 단순히 순서대로 실행되는 명령어들의 나열로 보지 않고, 데이터와 기능을 '객체'라는 독립된 단위로 묶어 이들의 조립과 상호작용으로 구현하는 개발 방식입니다. |
| **클래스** | Class | 객체를 컴퓨터 메모리에 찍어내기 위해 정의한 고유한 '설계도(틀)'입니다. 객체가 지녀야 할 상태(데이터)와 행동 기능(메서드)을 한곳에 선언해 둔 곳입니다. |
| **인스턴스 (객체)** | Instance | 클래스라는 설계도를 기반으로 컴퓨터 메모리 공간에 실제 존재하도록 구현된 데이터 실체입니다. |
| **인스턴스화** | Instantiation | 텍스트 파일 형태의 클래스 설계도를 파이썬 실행 엔진이 읽어 들여, 실제 메모리에 할당해 움직이는 인스턴스 객체로 변환하고 만들어내는 과정 자체를 뜻합니다. |
| **생성자** | Constructor (__init__) | 인스턴스가 처음 생성될 때 파이썬이 자동으로 가장 먼저 실행해 주는 특수 매직 메서드입니다. 주로 객체가 가지게 될 초기 데이터(속성)의 방을 만들고 값을 채워주는 역할을 합니다. |
| **self 키워드** | self Keyword | 함수 내부에서 "지금 현재 메모리에 살아서 움직이고 있는 바로 그 자기 자신 인스턴스"를 가리키는 파이썬만의 고유 이름표입니다. 클래스 내부에 메서드를 만들 때 첫 번째 매개변수로 반드시 적어주어야 합니다. |
| **속성** | Attribute | 객체가 가지고 있는 고유한 '상태나 변수 데이터'입니다. 클래스 내부에서 주로 self.변수명 형태로 선언되어 해당 객체만의 개별적인 데이터를 저장합니다. |
| **메서드** | Method | 클래스 구조 내부에 귀속되어 객체가 수행하는 '행동이나 기능'을 정의한 전용 함수입니다. 클래스에 정의된 함수를 메서드라고 합니다. 인스턴스 메서드는 현재 인스턴스를 전달받는 첫 번째 매개변수로 관례적으로 `self`를 사용합니다. |

## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


