# 26.09.17
KANT 33일차, 프론트엔드


## 오늘 느낀 점
- 이번 주까지 배우는 HTML / CSS 내용이 기초가 되는 스타트 라인인 것 같다. HTML / CSS로 어떤 내용들을 표현할 수 있고, 어떤 방식으로 문제를 해결할 수 있는지에 대해 배웠다.
- 실습과 발표과제를 수행하면서 가능한 만큼 커버하고, 이후 프로젝트를 진행하면서 이해도를 더 올리는 것을 목표로 해야겠다.


## 오늘 배운 것

```
### [ 학습목표 ]
Frontend
- Flexbox 핵심 속성(방향·정렬·비율)을 조절하여 1차원 화면을 설계할 수 있다.
- Grid 핵심 속성(구조·여백·공간)을 사용하여 2차원 바둑판 레이아웃을 구현할 수 있다.
- 화면 크기 변화에 유연하게 반응하여 늘어나고 줄어드는 가변 요소를 작성할 수 있다.
```

## 목차

- [26.09.17](#260917)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [flex (어제 이어서)](#flex-어제-이어서)
      - [`flex_grow`](#flex_grow)
      - [3개 박스 요소를 정확히 3등분 정렬하는 법](#3개-박스-요소를-정확히-3등분-정렬하는-법)
      - [`flex_shrink`](#flex_shrink)
      - [`flex-wrap`](#flex-wrap)
    - [그리드 레이아웃](#그리드-레이아웃)
      - [`grid-template-columns`](#grid-template-columns)
      - [`row-gap` \& `column-gap`](#row-gap--column-gap)
      - [예시: 병합을 포함하는 그리드 생성하기 (2행 3열)](#예시-병합을-포함하는-그리드-생성하기-2행-3열)
      - [`place_items`](#place_items)
    - [CSS 복습 + 보강 내용들](#css-복습--보강-내용들)
      - [`font`](#font)
      - [`@font-face`](#font-face)
      - [`overflow`란?](#overflow란)
      - [border - 경계선](#border---경계선)
      - [border-top, bottom, left, right](#border-top-bottom-left-right)
      - [border-radius : 코너 라운딩 처리](#border-radius--코너-라운딩-처리)
      - [`background` - 배경](#background---배경)
      - [`background-attachment`](#background-attachment)
      - [`background` - 단축 표기법](#background---단축-표기법)
    - [`positioning` - 위치](#positioning---위치)
      - [`position`](#position)
      - [`z-index`](#z-index)
  - [메모해 둘 것](#메모해-둘-것)
    - [XEICON](#xeicon)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


### flex (어제 이어서)

#### `flex_grow`
1. 자식요소 적용
2. 남은 공백을 차지할 비율 설정
   1. `flex-grow: 1;` 
      1. 요소 1개에만 할당 시, 남은 여백을 해당 요소가 단독으로 차지한다.
      2. 요소 2개에 할당 시, 남은 여백을 2개의 요소가 1:1 비율로 차지한다.
   2. `flex-grow: {n}`
      1. 할당된 n의 총합이 전체 여백의 100%를 의미한다.
      2. 각 요소에 할당된 n은 전체 여백 대비 n만큼의 비율의 여백을 차지한다.

#### 3개 박스 요소를 정확히 3등분 정렬하는 법
- 여백을 없앤다: `width: 0;`
- `flex-grow: 1`을 각 박스 요소에 모두 지정한다


#### `flex_shrink`
1. 부모 요소의 공간이 부족할 때, 자식요소의 크기 줄임 비율
2. 크기 줄임 비율은 가중치이다.
3. `flex_shink=0` - 요소를 축소하지 않는다
4. `flex-basis` - 요소의 기본 사이즈(auto - 기본값)
   1. 과도하게 축소되어 형태/비율이 망가지는 일을 방지한다

#### `flex-wrap` 
- (wrap) 부모 요소 너비가 부족하면 다음 칸에 개행한다.
- (nowrap) (기본값) - 개행하지 않는다.


### 그리드 레이아웃
2차원 격자 레이아웃에 담을 요소의 부모 요소 스타일로 선언한다. `grid-template-columns`를 주로 사용한다 (rows도 있긴 있음)


#### `grid-template-columns`
1. `grid-template-columns: 150px 150px 300px;`
2. `fr` : 남은 공간(여백)에 대한 비율
   1. `grid-template-columns: 150px 1fr 2fr` -> 남은 공간을 2번열, 3번열이 1:2 비율로 차지한다.
   2. `width: 500px` 선언 시, 500px에서 1번열을 제외한 잔여 폭을 1:2 비율로 나눈다.
3. `repeat` : 컬럼 갯수, 사이즈
   1. 같은 사이즈의 지정된 컬럼 갯수만큼 설정한다.
   2. `1fr 1fr 1fr` -> `repeat(3, 1fr)`


#### `row-gap` & `column-gap`
- `row-gap` 행별 여백
- `column-gap` 열별 여백
- `gap`
  - `gap: 20px 10px;` -> row-gap, column-gap 순서
  - `gap: 30px;` -> row-gap + column-gap


#### 예시: 병합을 포함하는 그리드 생성하기 (2행 3열)
grid-column
- `grid-column: 1/3;` - 1번열~3번열까지
- `grid-column: span 2;`
  - 컬럼 영역 한칸을 두칸으로 늘린다.
- `grid-row: span 2;` 
  - 행 영역 한칸을 두칸으로 늘린다.
  - 높이 지정이 필요하다.


#### `place_items`
- `stretch` 기본값
- `start` 왼쪽 상단
- `end` 오른쪽 하단
- `center` 정가운데



### CSS 복습 + 보강 내용들

#### `font`
1. `color`
   1. 색상이름, hexcode, rgb, rgba
2. `font_size` - 절대크기, 상대크기(em, rem)
3. `line-height` - 행간
   1. 윗줄과 아랫줄 사이의 여백 공간
   2. 기본값 1.2 **1 = 글자 높이/ 권장사항
      1. 20% 여백 -> 상단, 하단에 각각 10%씩 할당
4. `letter-spacing` : 자간
   1. 음수값도 가능하다 (글자겹침)
5. `word-spacing` : 단어 사이의 간격
6. `word-break`
   1. 글자가 줄 끝에서 잘렸을 때의 처리
   2. `wrap`, `nowrap`, `break -all`
7. `font-weight` : 폰트 두께
   1. normal (기본값)
   2. bold
   3. 100(가늘다) ~ 900(두꺼움)
      1. 폰트별로 지원하는 두께 range는 다르다!
8. `text-align`
   1. 인라인 요소(글)에 대한 정렬
   2. 글 요소가 아니라, 글 요소를 담는 요소 스타일로 정의해야 함
   3. `left` (기본값) : 왼쪽 정렬
   4. `right` : 오른쪽 정렬
   5. `center` : 가운데 정렬
   6. `justify` : 양쪽정렬 (문장 끝 단어잘림으로 들쭉날쭉한 길이를 자간조정으로 모두 통일)
      1. 문장마다 끝에 오는 단어는 변하지 않는다, 현재 상태 그대로 끌어 맞춘다
9. `font-style`
   1.  normal 기본값
   2.  italic 기울임체
   3.  oblique 흘림체
       1.  `oblique 90deg;` 90도 기울인 흘림체?
10. `text-decoration`
    1.  `underline` : 밑줄
    2.  `line-through` : 취소선
    3.  none (기본값)
11. `font-family` 글꼴
    1.  `폰트명, 대체폰트1, 대체폰트2, ...`
    2.  `'Malgun Gothic', 'AppleGothic', sans-serif;`
    3.  폰트명에 공백이 있으면 따옴표로 감싼다 (`'Malgun Gothic`)

#### `@font-face`

```html
<style>
    @font-face {
        font-family: 'NotoFont';
        src : url('./font/NotoSansKR-Medium.ttf') format('truetype');
        font-weight: 400;
        font-style: normal;
        font-display: swap; /* 폰트 로딩 전에 기본 폰트 적용, 로딩 완료시 현재 폰트로 적용*/
    }

    * {
        /* font-family: "Noto Sans KR", sans-serif; */
        font-family: NotoFont sans-serif;
        font-size: 30px;
    }  
</style>
```


#### `overflow`란?
- `visible` -> 넘치는 컨텐츠가 있어도 보이게 하기 (기본값)
- `hidden` -> 넘치는 컨텐츠를 영역 이내까지만 표시하고 나머지는 감추기
- `scroll` -> 스크롤 바
  - 공간이 넘치지 않아도 표시된다
- `auto`
  - 공간이 넘치면 스크롤바, 없으면 X
- 축을 따라서 overflow (?)
  - `x` : x축을 따라서
  - `y` : y축을 따라서


#### border - 경계선
- `border-width` : 두께
- `border-style`
  - none 기본값
  - sold 직선
  - dashed 파선
  - dotted 점선
  - double 이중실선
- `border-color` : 색상

border 단축표현법
- `border : {두께} {스타일} {색상}`
  - ex. `border: 2px solid red;`

#### border-top, bottom, left, right
  - ex. `border-top: 0` 상단 모서리 지우기 (두께 = 0)

#### border-radius : 코너 라운딩 처리
- 모서리 코너 2개 선에 접하는 가상의 원을 그리고, 그 원의 반지름값을 지정하여 코너 라운딩
    - `border-radius` 모든 코너
      - `border-radius: 50%` -> 반지름 = 직사각형의 절반이므로, 직사각형 안에 꽉 차는 원이 그려진다.
    - `border-top-right-radius`
    - `border-top-left-radius`
    - `border-bottom-right-radius`
    - `border-bottom-left-radius`



```html
<style>
    .border {
            /* 공통스타일 */
            width: 250px;
            height: 250px;
            margin: 20px;
            border: 3px solid blue;
        }

        .border1 {
            /* 개별스타일 */
            /* border-width: 3px;
            border-style: solid;
            border-color: blue; */
            border-radius: 20px;
        }
        .border2 {
            border-style: dashed;
            border-top-right-radius: 20px;
        }
        .border3 {
            border-style: dotted;
        }
        .border4 {
            border-style: double;
        }
        .border5 {
            /* 우측,좌측 경계선 지우기 (두께=0) */
            border-left: 0;
            border-right: 0;
        }
        .border6 {
            border-radius: 50%;
        }
</style>
```

#### `background` - 배경
1. `background-color` : 배경색
2. `background-image` : url("이미지경로")
3. `background-repeat`
   1. `repeat` (기본값: x,y축 모두)
   2. `repeat-x` : x축 반복
   3. `repeat-y` : y축 반복
   4. `no-repeat` : 이미지 1개만 출력
      1. 지정 영역 전체를 채우지는 않는다 (이미지 사이즈만큼만, 기본->좌측상단위치)
4. `background-position` : 배경 이미지 위치 설정
   1. `background-position: 100px 50px;`
      1. 위->아래 100px 이동 + 좌->우 50px 이동
   2. `background-position: 50% 50%`
      1. 부모영역 정중앙 (`background-position: center center`와 동일)
      2. `background-position: 50%`만 입력해도 정중앙으로 간다 (생략 자동이동)
      3. `background-position: 100%` -> position-x 100%(우측끝) / position-y center
5. `background-size`
   1. `background-size: 200px 100px;` (가로->세로)
   2. `background-size: 100%` 
   3. `background-size: cover` : 영역을 모두 채운다
      1. 이미지 비율이 배경 요소와 차이가 있다면 일부는 짤린다.
   4. `background-size: contain` : 이미지가 모두 포함되도록 채운다
      1. 영역이 작다면, 이미지도 줄여서 다 넣어준다.
      2. 원본 이미지 축소 과정에서 부모 영역에 여백이 생길 수 있다.


#### `background-attachment`
- `fixed scroll`
  - 스크롤 시, 배경 이미지가 따라온다.
  - 구현하고 싶으면 fixed scroll로 검색해서 공부해볼 것



#### `background` - 단축 표기법

```html
<style>
    .box {
        width: 700px;
        height: 700px;
        border: 1px solid #000;
        
        /* background-image: url('images/main_banner.avif');
        background-repeat: no-repeat;
        background-size: cover;
        background-position: center center;
        background-color: #ccc;  */

        background: #ccc url("images/logo_126.png") no-repeat center center;
    }
</style>
```

### `positioning` - 위치

#### `position`
- 절대위치 (기본값)
  - `position = static`
- 상대위치 (-> 기준)
  - relative : 속성이 정의되어 있는 요소
  - absolute
    - 문서 전체 기준
    - 부모가 상대위치(relative, absolute, fixed) 속성이면 부모가 기준점
  - fixed : viewport(보이는화면) 기준

#### `z-index`
높을수록 우선해서 영역을 보여준다 (겹치면 z-index가 더 높은 요소를 상단으로)

```html
<style>
    .box {
        width: 300px;
        height: 300px;
        position: absolute;
        left: 0;
        top: 0;
    }

    .box1 {
        background: red;
        z-index: 3;
        left: 100px;
        top: 100px;
    }

    .box2 {
        background: green;
        z-index: 2;
        left: 50px;
        top: 50px;
    }

    .box3 {
        background: blue;
        z-index: 1;
    }
</style>
```

## 메모해 둘 것

### XEICON
- icon library
- [xpressengine.github.io/XEIcon/library-2.4.0.html](xpressengine.github.io/XEIcon/library-2.4.0.html)


## Daily Quest 퀴즈 오답
Q. 한 요소에 여러 CSS 규칙이 충돌할 때, 선택자가 더 구체적이고 좁은 범위를 타겟팅할수록 점수가 높게 산정되어 최우선 적용되는 규칙을 무엇이라고 하나요?
- (정답) 명시도(Specificity)
- (해설) 명시도(Specificity)는 인라인 스타일, ID 선택자, 클래스 선택자, 태그 선택자 순으로 가중치 점수가 매겨지며, 점수가 높은 선택자가 우선 적용됩니다. 가중치가 같을 경우에는 캐스케이딩 규칙에 따라 나중에 선언된 스타일이 우선합니다.
- (틀린이유) 기억이 나지 않음

Q. Flexbox에서 자식 요소가 부모 공간의 남는 여백을 나누어 가지도록 확장 비율을 지정할 때 사용하는 속성명은 무엇인가요?
- (정답) flex-grow
- (해설) flex-grow는 부모 상자 안에서 남는 여백을 자식 요소들이 지정된 비율만큼 나누어 가지도록 확장시키는 속성입니다. 기본값은 0으로 늘어나지 않으며, 검색창처럼 남은 공간을 채워야 할 때 1 이상의 값을 지정합니다.
- (틀린이유) 기억이 나지 않음


## 진단 퀴즈 오답
- 없음
