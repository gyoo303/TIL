# 26.08.25
KANT 16일차, AI Literacy + ML


## 오늘 느낀 점
- pandas 라이브러리를 활용하여 타이타닉 데이터셋을 분석해 보았는데, EDA 과정은 어느 AI 관련 분야든 가장 기본이 되는 작업으로, 가능한 익숙해져 두면 좋을 것 같다. 어느 인공지능 모델을 쓰든, 데이터가 많이 오염되었을 때 전처리를 깔끔하게 하는 것은 필수다.
- EDA 과정에서 시각화는 자연스럽게 필요하다. 마찬가지로 hist, scatter 등 유용한 함수들은 기억해 두자.
- 학습목표 '주어진 비즈니스 요구사항을 분석하여 적절한 머신러닝 문제 유형으로 분류할 수 있다' ... 열심히 노력해서 할 수 있는 사람이 되자!

## 오늘 배운 것

```
### [ 학습목표 ]
- Pandas Series와 DataFrame을 생성·조작하고 인덱스를 제어할 수 있다.
- loc, iloc, query() 등을 활용해 원하는 데이터를 정밀하게 추출할 수 있다.
- 데이터 진단과 결측치를 정제하고 NumPy 연계 파이프라인을 구축할 수 있다.
- Matplotlib 객체지향 API로 데이터 분포와 이상치를 시각화할 수 있다.
- 전통적 프로그래밍과 머신러닝의 개념적 차이 및 지도·비지도·강화학습의 메커니즘을 설명할 수 있다.
- 주어진 비즈니스 요구사항을 분석하여 적절한 머신러닝 문제 유형으로 분류할 수 있다.
- 데이터 조작 및 시각화를 위한 핵심 라이브러리의 구조와 역할을 설명할 수 있다.
- Python 환경에서 데이터 분석에 필수적인 라이브러리를 임포트하고 기본 데이터를 가공 및 시각화할 수 있다.
```

## 목차

- [26.08.25](#260825)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [pandas (계속)](#pandas-계속)
      - [loc, iloc](#loc-iloc)
      - [query() 필터링](#query-필터링)
      - [결측치(Missing Data) 7대 처리 기법](#결측치missing-data-7대-처리-기법)
      - [그룹화 집계(groupby) \& `.to_numpy()` 상호 변환](#그룹화-집계groupby--to_numpy-상호-변환)
    - [Matplotlib 기초](#matplotlib-기초)
      - [캔버스(Figure)와 축(Axes) 기본 구조(fig, ax)](#캔버스figure와-축axes-기본-구조fig-ax)
    - [데이터 분석 기초 - 탐색적 데이터 분석(EDA)과 결측치/이상치](#데이터-분석-기초---탐색적-데이터-분석eda과-결측치이상치)
      - [기본개념](#기본개념)
      - [라이브러리 및 데이터 로드](#라이브러리-및-데이터-로드)
      - [데이터 요약 및 통계 확인](#데이터-요약-및-통계-확인)
      - [결측치 파악하기](#결측치-파악하기)
      - [숫자형 데이터(나이): 전체 탑승객의 '평균(mean)'으로 빈칸 채우기](#숫자형-데이터나이-전체-탑승객의-평균mean으로-빈칸-채우기)
      - [문자/범주형 데이터(탑승 항구): 가장 많이 탑승한 곳인 '최빈값(mode)'으로 빈칸 채우기](#문자범주형-데이터탑승-항구-가장-많이-탑승한-곳인-최빈값mode으로-빈칸-채우기)
      - [특정 컬럼(deck) 을 '기준'으로 좌우(열 방향) 데이터를 채우기](#특정-컬럼deck-을-기준으로-좌우열-방향-데이터를-채우기)
      - [너무 비싼 요금(이상치) 걸러내기](#너무-비싼-요금이상치-걸러내기)
      - [결측치/이상치 보완 결과 확인](#결측치이상치-보완-결과-확인)
    - [데이터 분석 기초 - 중복 데이터 처리하기](#데이터-분석-기초---중복-데이터-처리하기)
      - [중복 데이터 제거하기](#중복-데이터-제거하기)
      - [그래프로 시각화하기](#그래프로-시각화하기)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### pandas (계속)

#### loc, iloc
- loc : 행의 이름으로 검색(선택)
  - loc [행 라벨]
  - loc[행 라벨, 컬럼 이름]
  - loc[시작행:종료행, [컬럼1, 컬럼2, ...]]
  - loc[..., 시작컬럼:종료컬럼]
- iloc : 숫자 인덱스로 검색(선택)
  - loc와 사용 방법은 동일하지만 행 라벨이 아니라 인덱스 번호
  - 범위를 설정할 경우 종료 번호는 '미만'
  - iloc[0:2, 0:2] -> 인덱스 0, 1, 컬럼: 0, 1

```python
df = pd.DataFrame(
    {
        "나이":[25,30, 35,40],
        "점수":[80,95,70,85],
        "등급":['B','A','C','B']
    },
    index = ['user1', 'user2', 'user3', 'user4']
)

print(df.loc['user1'], type(df.loc['user1']))

print(df.loc['user1':'user3', ['점수', '등급']])
print(df.loc['user1':'user3', '점수':'등급'])


print((df['나이'] >= 30) & (df['점수'] >= 80))

print(df[(df['나이'] >= 30) & (df['점수'] >= 80)])
```

#### query() 필터링
df.query('나이 >= 30 and 점수 >= 80')


#### 결측치(Missing Data) 7대 처리 기법


1. 결측치 탐지 (detection)
- `info()`
- `isna()`, `isnull()`

```python
import pandas as pd
import numpy as np

raw_df = pd.DataFrame({'A': [1, np.nan, 3], 'B': [pd.NA, 5, 6], 'C': [7, 8, 9]})

print("[결측치 위치 불리언 마스크]:\n", raw_df.isna())
print("[컬럼별 결측치 총 개수]:\n", raw_df.isna().sum())
```


2. 결측체 삭제 (Drop)
- axis - 0: 행 기준 - 특정 행에 결측치 있으면 제거 (기본)
- axis - 1: 열 기준 - 특정 열에 결측치가 있으면 제거

```python
# 행 기준에서 결측치 삭제
raw_df.dropna()
# 열 기준에서 결측치 삭제
raw_df.dropna(axis=1) # 컬럼 C만 남는다 - 결측치가 하나 이상 있는 컬럼을 날린다.

print(raw_df.isna())
print(raw_df.isna().sum()) # 컬럼별 결측치의 갯수가 나온다.
```

3. 통계적 대푯값 대치 (imputation)
- 평균(mean())
- 중앙값(median()) : 극단치(이상치) 영향을 덜 받는다

```python
df_age = pd.DataFrame({'나이': [20, 25, None, 30, 95]})

median_val = df_age['나이'].median()
print("중앙값:", median_val) # 25와 30이 중앙에 위치한 값이며, 두 값의 평균 = 중앙값 -> 양극단 이상치의 영향을 적게 받는다.

df_age['나이'] = df_age['나이'].fillna(median_val)

print(df_age)
```

4. 시계열 직전/직후 값 대치 (Forward / Backward Fill)
시간 순서나 연속성이 중요한 

- ffill() : 앞쪽에 있는 값으로 대체
- bfill() : 뒤쪽에 있는 값으로 대체

```python
ts_df = pd.DataFrame({'온도': [18.2, None, None, 21.0]})

print("[직전 유효값 대치(ffill)]:\n", ts_df.ffill())
print("[직후 유효값 대치(bfill)]:\n", ts_df.bfill())
```

5. 선형 보간법 (Linear Interpolation)

- 기본적으로 데이터가 비례관계를 형성하고 있을 때 적용

```python
speed_df = pd.DataFrame({'속도': [10.0, None, None, 40.0]})

speed_df['속도'] = speed_df['속도'].interpolate(method='linear')

print(speed_df)
```

6. 이상 기호 치환 및 수치형 변환 (Replace & Type Casting)
- replace() : 특정 데이터를 찾아서 교체
- pandas.to_numeric(...) : 숫자 자료형으로 변환

```python
survey_df = pd.DataFrame({'점수' : [85, -999, '?', 95]})

survey_df.info()
survey_df.describe() # 수치 통계량 계산이 빠진다 (계산불가능)


survey_df['점수'] = survey_df['점수'].replace([-999, '?'], pd.NA)
# <NA> 대체되었지만, 여전히 통계량 계산은 할 수 없다 (object-string 자료형)


survey_df['점수'] = pd.to_numeric(survey_df['점수'], errors="coerce") # 형변환 강제 적용 : float 타입의 NaN 자료형
print(survey_df.info())
print(survey_df.describe()) # NaN 제외하고 나머지 값만으로 수치통계량 계산된다 - float NaN은 무시한다.
```

*`errors='coerce' 의미?`
```
❓ **errors='coerce' 의미?**

---
- errors='coerce' (강제 변환): 숫자로 바꿀 수 없는 값이 있으면 에러를 내지 않고 NaN(np.nan)으로 대체합니다.
- errors='raise' (기본값): 변환할 수 없는 값이 하나라도 있으면 즉시 ValueError 예외를 발생시키고 작업을 중단합니다.
- errors='ignore': 변환에 실패하면 에러 없이 원래 데이터를 그대로 유지합니다.
```


7. 결측 여부 지표 변수 생성 (Missing Indicator)
- 누락 여부가 중요한 변수로 활용 가능할 때

```python
user_df = pd.DataFrame({'소득': [300, None, 450, None]})
user_df['미응답여부'] = user_df['소득'].isna()

print(user_df)
```

#### 그룹화 집계(groupby) & `.to_numpy()` 상호 변환

```python
sales_df = pd.DataFrame({
    '지점' : ['서울', '서울', '부산', '부산'],
    '매출' : [100, 150, 80, 120]
})

print(sales_df)

grouped_df = sales_df.groupby("지점").mean(numeric_only = True) # 숫자가 아닌 데이터는 무시한다
print(grouped_df)

import numpy as np
sales_arr = sales_df['매출'].to_numpy(dtype=np.float64)
print(sales_arr, type(sales_arr))
```


### Matplotlib 기초

#### 캔버스(Figure)와 축(Axes) 기본 구조(fig, ax)
- 산점도 : 데이터의 분포
- 꺾은선 그래프 : 시계열 데이터의 흐름
- 히스토그램 : 데이터의 빈도 수
- 박스플롯 : 이상치의 분포


```
- 캔버스 생성 
    - plt.figure(figsize=(...)) 1개 그릴때
    - plt.subplot(행, 열, figsize=(...)) 여러개 그릴때
    - figsize : 캔버스의 크기 (가로inch, 세로inch)
```

```python
import matplotlib.pyplot as plt

# Figure와 Axe 캔버스 기본 구조 생성
fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(10,4))
```


### 데이터 분석 기초 - 탐색적 데이터 분석(EDA)과 결측치/이상치

#### 기본개념
- 평균 : 데이터 전체의 합을 데이터 갯수로 나눈 값
- 분산 : 각 데이터와 평균의 차이에 대한 제곱을 모두 더한 뒤 데이터 갯수로 나눈 값
- 표준편차 : 분산의 제곱근


#### 라이브러리 및 데이터 로드
```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

df = sns.load_dataset("titanic")

#df.iloc[:5]
#df[:5]
df.head() # 첫 5행
df.tail() # 끝 5행
```

#### 데이터 요약 및 통계 확인

```python
print(df.columns) # 컬럼 목록 확인
print(df.info()) # 컬럼 기본정보 (이름, Non-Null count, Dtype)
print(df.describe()) # 컬럼별 통계량
```

#### 결측치 파악하기

```python
print(df.isna().sum())
```

#### 숫자형 데이터(나이): 전체 탑승객의 '평균(mean)'으로 빈칸 채우기

```python
mean_age = df['age'].mean()
print("평균 나이:", mean_age)

df['age'] = df['age'].fillna(mean_age)

df.isna().sum()
```


#### 문자/범주형 데이터(탑승 항구): 가장 많이 탑승한 곳인 '최빈값(mode)'으로 빈칸 채우기

```python
mode_embarked = df['embark_town'].mode()

print(mode_embarked) # 시리즈 자료형, 최빈값이 여러개일 수 있다.

df['embark_twon'] = df['embark_twon'].fillna(mode_embarked[0])
# 첫글자 대문자로 
df['embarked'] = df['embarked'].map(lambda x: x[0].upper())

print(df.isna().sum())
```


#### 특정 컬럼(deck) 을 '기준'으로 좌우(열 방향) 데이터를 채우기

```python
df['deck'] = df['deck'].ffill()
df['deck'] = df['deck'].bfill()

print(df.isna().sum())
```

#### 너무 비싼 요금(이상치) 걸러내기


```python
print("요금 평균:", df['fare'].mean())
print("중앙값:", df['fare'].median())
```

```python
print(df['fare'].describe())

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(10,4))

# 좌측: 히스토그램
ax1.hist(df['fare'], bins=50, color='skyblue', edgecolor='black')
ax1.set_title('Fare (Histogram)')
ax1.set_xlabel('Amount');
ax1.set_ylabel('Count')

# 우측: 박스플롯 
ax2.boxplot(df['fare'], tick_labels=['Fare'])
ax2.set_title('Outlier Check (Boxplot)')
ax2.set_ylabel('Amount')

plt.tight_layout()
plt.show()
```

```python
print(df[(df['fare']>100)]['class'].unique())

print(len(df[(df['fare']>100) & (df['class'] != 'First')]))

df = df[df['fare'] <= 500]
```

#### 결측치/이상치 보완 결과 확인

```python
print(df.isna().sum())
```

### 데이터 분석 기초 - 중복 데이터 처리하기

```python
df.duplicated().sum()
```


#### 중복 데이터 제거하기

```python
# drop_duplicates()
# 중복 제거하고 인덱스 초기화 (0부터 순차적으로) 
# -> drop=True까지 설정해야 기존 인덱스 컬럼 삭제됨 (설정 안하면 이전 인덱스가 남아있음)
df = df.drop_duplicates().reset_index()
```

중복 데이터 확인하기

```python
print('중복 데이터 수:', df.duplicated().sum())
```

#### 그래프로 시각화하기

내 컴퓨터에 맞는 한글 폰트 설정하기
```python
import platform
from matplotlib import rc

os_name = platform.system()
if os_name == "Windows":
    rc("font", family="Malgun Gothic")
else:
    rc("font", family="AppleGothic")

```


도화지(Figure)와 3개의 액자(Axes) 준비

```python
fig, axes = plt.subplots(1, 3, figsize=(18, 5)) # 폭 18인치, 1행 3열 배치
fig.suptitle("타이타닉 탑승객 데이터 분석", fontsize=16)

# [히스토그램] 탑승객 나이 분포
axes[0].hist(df['age'], bins = 20, color='skyblue', edgecolor='black') # 20개의 등구간을 갖는 히스토그램
axes[0].set_title("탑승객 나이 분포")
axes[0].set_xlabel('나이')
axes[0].set_ylabel('승객수')

# [박스 플롯] 티켓 등급에 따른 요금 이상치 확인
sns.boxplot(x='class', y='fare', data=df, ax=axes[1], palette='Set2')
axes[1].set_title('티켓 등급별 요금 분포 (박스 플롯)')
axes[1].set_xlabel('티켓 등급')
axes[1].set_ylabel('티켓 요금 (달러)')

# [산점도] 나이와 클리닝된 요금의 관계
```python
# 내 컴퓨터에 맞는 한글 폰트 설정하기
os_name = platform.system()
if os_name == "Windows":
    plt.rcParams['font.family'] = 'Malgun Gothic' # 윈도우
elif os_name == "Darwin":
    plt.rcParams['font.family'] = 'AppleGothic'   # 맥북
else:
    plt.rcParams['font.family'] = 'NanumGothic'   # 기타(코랩 등)

plt.rcParams['axes.unicode_minus'] = False # 마이너스 기호 깨짐 방지

# 도화지(Figure)와 3개의 액자(Axes) 준비
fig, axes = plt.subplots(1, 3, figsize=(18, 5))
fig.suptitle('타이타닉 탑승객 데이터 분석 (데이터 정제 및 시각화)', fontsize=16)

# [히스토그램] 탑승객 나이 분포
axes[0].hist(df['age'], bins=20, color='skyblue', edgecolor='black')
axes[0].set_title('탑승객 나이 분포 (결측치 평균 대체 후)')
axes[0].set_xlabel('나이 (세)')
axes[0].set_ylabel('승객 수 (명)')

# [박스 플롯] 티켓 등급에 따른 요금 이상치 확인
# 박스(상자) 바깥쪽에 외롭게 찍혀 있는 점들이 바로 일반적인 범위를 벗어난 '이상치'들입니다.
sns.boxplot(x='class', y='fare', data=df, ax=axes[1], palette='Set2')
axes[1].set_title('티켓 등급별 요금 분포 (박스 플롯)')
axes[1].set_xlabel('티켓 등급')
axes[1].set_ylabel('티켓 요금 (달러)')

# [산점도] 나이와 클리닝된 요금의 관계
scatter = axes[2].scatter(
    df['age'], 
    df['fare'], 
    c=df['survived'], # 생존 여부(1: 생존, 0: 사망)에 따라 색상이 다름
    cmap='coolwarm',
    s=df['fare'] / 2, # 요금이 비쌀수록 원이 조금 더 큼
    alpha=0.7
)
axes[2].set_title('나이 vs 티켓 요금 (빨간색: 생존, 파란색: 사망)')
axes[2].set_xlabel('나이 (세)')
axes[2].set_ylabel('티켓 요금 (달러)')

fig.colorbar(scatter, ax=axes[2], label='생존 여부')

plt.tight_layout()
plt.show()
```



## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
Q. 타이타닉 데이터에서 '나이(age)'처럼 숫자형 데이터의 결측치를 채울 때 일반적으로 사용하는 대표값은 무엇인가요?
- 정답: 평균(mean)
- 내가 쓴 답: 중앙값
- 해설
  - 숫자형 데이터는 산술 계산이 가능하므로 전체 데이터의 평균(mean)으로 결측치를 자연스럽게 채울 수 있습니다. 반면 항구 이름 같은 문자/범주형 데이터는 평균을 낼 수 없어 최빈값(mode)을 사용합니다.
- 틀린 이유
  - 평균은 이상치에 취약하므로 중앙값이라고 생각했는데, 컬럼 전체의 평균값을 변경시키지 않는다는 점에서 평균값이 더 일반적인 접근인 것 같다.

## 진단 퀴즈 오답
- 없음


