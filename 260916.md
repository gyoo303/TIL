# 26.09.16
KANT 32일차, 프론트엔드


## 오늘 느낀 점
- 한꺼번에 다 외우기에는 시간이 걸릴 것 같다. 실습하면서 천천히 하나씩 익혀보자.


## 오늘 배운 것

```
### [ 학습목표 ]
Frontend
- CSS 기본 구문(선택자·속성·값)의 개념과 역할을 정의할 수 있다.
- 다양한 선택자(기본·복합·가상 클래스·가상 요소)의 구조와 차이점을 설명할 수 있다
- 고급 선택자를 활용하여 HTML 구조 변경 없이 디자인을 제어할 수 있다.
- CSS 결합 3가지 방식(인라인·내부·외부)의 특징과 차이점을 설명할 수 있다.
- 블록과 인라인 요소의 특징을 이해하고 명확히 구분할 수 있다.
- box-sizing 속성(content-box·border-box)의 차이를 정의하고 레이아웃에 적용할 수 있다.
- display: none과 visibility: hidden의 공간 차지 차이를 비교하여 설명할 수 있다.
- 박스 속성과 position을 활용하여 요소를 원하는 위치에 정밀하게 배치할 수 있다.

```

## 목차

- [26.09.16](#260916)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [CSS / Style](#css--style)
      - [그룹 선택자 (`,`)](#그룹-선택자-)
      - [속성 선택자](#속성-선택자)
      - [가상클래스 선택자](#가상클래스-선택자)
      - [CSS 속성](#css-속성)
      - [padding, margin, border](#padding-margin-border)
      - [box-sizing : 너비와 높이의 기준](#box-sizing--너비와-높이의-기준)
      - [외부 여백 (margin)](#외부-여백-margin)
      - [display](#display)
    - [플렉스 박스와 그리드 레이아웃](#플렉스-박스와-그리드-레이아웃)
      - [플렉스 박스 (1차원 정렬)](#플렉스-박스-1차원-정렬)
      - [`justify_content` : 정렬 속성 제어](#justify_content--정렬-속성-제어)
      - [`align-items` : 자식요소들의 위치 배치](#align-items--자식요소들의-위치-배치)
  - [메모해 둘 것](#메모해-둘-것)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### CSS / Style

#### 그룹 선택자 (`,`)
- 여러 개의 선택자를 동시 설정 가능
- 선택자 범위를 좁히는 것도 가능

```html
<style>
    a, p, .greeting {
        color: green;
    }

    span.greeting {
        color: orange;
    }
</style>
```

#### 속성 선택자
[속성명] : 지정된 속성이 있으면 선택
- 다른 선택자와 조합을 통한 선택 주로 사용

```html
<style>
    input[type] {
        border: 3px solid red;
    }
</style>
```

[속성명 = "값"] : 속성의 값까지 일치하는 요소 선택
```html
<style>
    input[type="number"] {
        border-color: blue;
    }
</style>

- [속성명^ = "값"] : 속성 값으로 시작하는 요소
- [속성명$ = "값"] : 속성 값으로 끝나는 요소
- [속성명* = "값"] : 속성 값이 포함된 요소
```

#### 가상클래스 선택자
가상 선택자 - 상태, 조건, 위치
- 가상 선택자 : 위치
  - `:first-child` 첫번째 자식 요소
  - `:last-child` 마지막 자식 요소
  - `:nth-child(숫자)`
    -  숫자는 1부터 시작한다
    -  숫자 위치에 있는 요소를 선택한다
  -  `:nth-child(수식)` 
    -  수식에 해당하는 요소를 선택한다
    -  ex. `:nth-child(2n+1)` -> 홀수번째 요소
  - `:first-of-type`
  - `:last-of-type`
  - `:nth-of-type`
  - `:only-of-type` : 자식 요소가 1개만 있는 경우
- 조건 선택자
  - `not(선택자)` : 지정된 선택자가 아닌 요소 선택
  - `is(선택자1, 선택자2,...)` (자식 선택자)
    - 상위요소 그룹으로부터 하위요소를 선택하기

```html
<style>
    li:not(.menu5) {
        background: orange;
    }
</style>
```

- 상태 선택자
  - `:hover` : 마우스가 요소에 올라간 상태
  - `:focus` : 특정요소 선택, 커서가 있는 상태
  - `:blur` : 커서가 나간 상태
  - `:checked` : 체크된 상태
  - `:read-only` : 읽기 전용 상태
  - `:disabled` : 비활성 상태 <-> `:enabled`

- 요소 추가 선택자
  - `::before` : 자식의 첫번째 요소로 추가
  - `::after` : 자식의 마지막 요소로 추가
    - 반드시 추가할 내용이 있어야 노출 -> "content 필수"
    - 일부러 `content: '';`으로 선언하여 빈 공간으로 스타일링하는 경우도 많다
    - (주의) 기본적으로 inline 속성 -- 너비 속성 등을 정의할 수 없다 (영역X)
  - `::placeholder` : 가이드라인 텍스트
  - `::selection` : "선택된 영역"


#### CSS 속성
1. 텍스트 및 폰트 속성
   1. color : 폰트 색상
      1. 색상명 red, blue, ...
      2. rgb(___, ___, ___) // 0~255 사이의 값
      3. 16진법 표기 (HEX CODE) : 자릿수 줄이려고 쓴다 (앞뒤 자리가 동일하면 생략 가능 -> #FFF = #FFFFFF)
      4. rgba(~255, ~255, ~255, 0.0~1.0) // 마지막은 투명도를 표시하는 alpha 채널
2. font-size : 글자 크기
   1. 절대 크기
      1. px(픽셀) : 모니터 화소 기준
      2. pt : 인쇄 기준
   2. 상대 크기
      1. em : 상대 상위 태그 그룹의 글자 사이즈가 기준이 된다
      2. rem (r for root) : 최상위 태그의 글자 사이즈가 기준이 된다 -> html 태그에 설정된 px = 1 rem
      3. 일반적으로 rem을 많이 사용한다

```html
<style>
    html {
        font-size: 13px; /* 1rem -> 13px */
    }

    .text1 {
        font-size: 1rem; /* 별도 지정하지 않아도 13px - cascading */
    }

    .text2 {
        font-size: 2rem;
    }

    .text3 {
        font-size: 3rem;
    }

    .text-group {
        font-size: 20px; /* 20px -> 1em */
    }

    .text4 {
        font-size: 1em; /* 20px */
    }

    .text5 {
        font-size: 2em; /* 40px */
    }

</style>
```

#### padding, margin, border
- width / height -> 컨텐츠 영역
- padding -> border 기준으로 내부 여백 / border 안쪽에 생성된다
- margin -> border 기준으로 외부 여백 / border 바깥쪽에 생성된다
- 컨텐츠 ~ 경계선 내부까지의 사이즈 = box size
  - 150 + 30 + 30 + 5 + 5 = 220
  - 컨텐츠, 패딩, 외곽선 비율을 유지한 채로 box size를 조정하는 방법 -> box-sizing (다음 항목에서 설명)

```html
<style>
    .box {
        width: 150px; 
        height: 150px; /* 콘텐츠영역 - 150x150px */
        padding: 30px; /* 내부 여백 - border 기준 */
        margin: 50px;  /* 외부 여백 - border 기준 */
        border: 5px solid blue;
    }
</style>
```


#### box-sizing : 너비와 높이의 기준
1. content-box (기본값) : 내용영역(content) 기준.
2. border-box : 경계선(border) 기준
   1. 내부여백(padding) 및 경계선 두께 변경 -> 내용 영역의 크기가 변경
   2. width-height 사이즈 = 경계선까지의 영역 사이즈로 맞춘다 + 비율을 유지한다

```html
<style>
    .box {
        width: 150px; 
        height: 150px; /* 콘텐츠영역 - 150x150px */
        padding: 30px; /* 내부 여백 - border 기준 */
        border: 5px solid blue;
        margin: 50px;  /* 외부 여백 - border 기준 */
    }

    .box2 {
        box-sizing: border-box;
        height: 150px; 
        padding: 30px; 
        border: 5px solid red; /* 여기까지가 150 * 150px 사이즈로 맞춰진다 */
        margin: 50px; /* 축소된 비율만큼 작아지지 않고 그대로 50px 마진 적용 */ 
    }
</style>
```

#### 외부 여백 (margin)
- block / inline 요소별 차이
  - block 속성 태그 : 제한없음
  - inline 속성 태그 : 위,아래 여백X / 좌,우 여백O
    - inline은 보통 text-align을 사용한다
- margin 수치 입력방법
  - 4개입력 : 상 우 하 좌 (12시부터 시계방향)
  - 3개입력 : 상 (좌우) 하
  - 2개입력 : (상하) (좌우)
    - `margin: 100px auto;` 상하 100px, 좌우 가운데정렬
    - **상하 margin은 auto가 안먹는다.**
  - 1개입력 : (상하좌우)
- margin 영역이 겹칠 경우
  - 겹치면 더 값이 큰 여백 하나만 적용한다
  - 위 margin 100px, 아래 margin 50px인 div 요소 2개가 위아래 붙어있는 경우:
    - 아래에 위치한 div의 위 margin 100px이 적용된다
- 특정 방향 명시 -> 한 방향만 margin 주기
  - margin-top: 50px;
  - margin-bottom: 50px;
  - margin-right: 50px;
  - margin-left: 50px;
  - **특정방향 명시 방법은 padding도 동일**


#### display
1. block -> block이 아닌 요소를 block으로 변경
2. inline -> inline이 아닌 요소를 inline으로 변경
3. inline-block
   1. 줄바꿈은 일어나지 않지만
   2. 공간을 가지며 상하좌우 외부여백(margin)을 설정할 수 있다
   3. **외부여백이 겹칠 때 각각 적용된다** (block의 경우 외부여백 겹치면 더 큰 margin 적용)
4. none -> 요소 숨기기 + 영역까지 날린다
   1. (비교) visibility 속성 -> 영역은 안날린다
      1. `visibility: visible;` (기본값)
      2. `visibility: hidden;`



### 플렉스 박스와 그리드 레이아웃


#### 플렉스 박스 (1차원 정렬)
- 설정 -> 부모요소: `display: flex`
- 적용 -> 자식요소
- 플렉스 적용 시 block / inline 의미 없어진다
  - 플렉스 박스는 기본적으로 영역을 갖는다
- 정렬 방향: `flex-direction`
  - [예시 상황] A, B, C 요소를 갖는 unordered list + `list-style: none;`
  - `row` (기본값) 좌측정렬+현재순서, ABC
  - `row-reverse` 우측정렬+반대순서, CBA
  - `column` 세로정렬+현재순서, ABC
  - `column-reverse` 세로정렬+반대순서, CBA


#### `justify_content` : 정렬 속성 제어
- `flex_start` 왼쪽정렬
- `flex_end` 오른쪽정렬
- `center` 가운데정렬
- `space-between` 첫 요소와 마지막 요소를 화면 양끝 배치 + 나머지 요소들 균등 배치
- `space-around` 양끝 배치 + 나머지 균등배치는 동일 / 각 요소가 각각의 좌우 여백을 갖는다 (여백이 각 요소를 감싼다)
- `space-evenly` space between과 유사하지만 여백으로 시작하고 끝나며, 모든 여백의 간격이 균등하다


#### `align-items` : 자식요소들의 위치 배치
- `stretch`(기본값) : 부모 요소의 크기만큼 자식 요소 크기를 늘린다
- `flex-start` : 위쪽 배치
- `flex-end` : 아래쪽 배치
- `center` : 가운데 배치



## 메모해 둘 것



## Daily Quest 퀴즈 오답
Q. 부모 요소에 적용된 스타일 속성이 별도의 선언 없이도 자식 요소에게 자연스럽게 물려 내려오는 CSS의 현상을 무엇이라고 하나요?
- 정답: 상속
- body에 글꼴을 지정하면 하위 p, span 태그에 따로 선언하지 않아도 동일한 글꼴이 적용되는 현상이 상속입니다. 이는 캐스케이딩과 함께 CSS의 두 가지 핵심 규칙 중 하나입니다.
- 틀린 이유: 상속이 아니라 별도의 용어가 있는 줄 알았음


Q. 다음 CSS 코드가 적용될 때, HTML 구조가 아래와 같다고 가정하면 빨간색으로 스타일이 적용되는 요소는?

```html
CSS: .parent > p { color: red; }

HTML:
<div class="parent">
  <p>A문단</p>
  <div><p>B문단</p></div>
</div>
```

- 정답: A문단만 적용
- 해설: 자식 선택자(>)는 부모 바로 한 단계 아래에 있는 요소만 선택하므로 parent의 직계 자식인 A문단만 선택됩니다. B문단은 parent의 손자이므로 선택되지 않습니다. 만약 하위(자손) 선택자였다면 B문단도 선택되었을 것입니다.
- 틀린 이유: `<p>` 태그가 B문단에만 있는 것으로 잘못 봄

Q. 콜론 두 개(::)를 사용하며, HTML 문서에 실제로 존재하지 않는 가상의 요소를 CSS를 통해 동적으로 삽입할 때 사용하는 문법을 무엇이라고 하나요?
- 정답: 가상 요소
- 해설: 가상 요소(Pseudo-element)는 ::before, ::after처럼 콜론 2개를 사용하며 content 속성과 함께 텍스트나 디자인 공간을 동적으로 추가합니다. 반면 가상 클래스는 콜론 1개를 사용하며 요소의 상태나 조건에 스타일을 부여하는 점에서 차이가 있습니다.
- 틀린 이유: 문제에 답이 있어서 다른 표현으로 작성함



## 진단 퀴즈 오답
- 없음
