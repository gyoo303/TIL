# 26.09.11
KANT 29일차, Database & AI


## 오늘 느낀 점
- 오늘은 PostgreSQL 특강으로, 함수와 프로시저를 활용한 트랜잭션 제어를 배웠다. 트랜잭션을 안전하게 수행하기 위한 두 개의 방법론으로, 데이터베이스의 원자성을 보장하면서 복잡한 쿼리를 수행할 수 있는 수단이 된다.
- 쿼리 작성에만 활용하는 줄 알았으나 함수, 프로시저를 작성해 보니 SQL도 프로그래밍 언어라는 체감이 들었다.
- 라이브 서비스에서 대용량 데이터를 처리하는 작업은 테이블 잠금 현상으로 인해 사용자의 서비스 이용이 불가능해질 수 있으며, 이러한 문제를 방지하기 위하여 짧은 간격으로 데이터를 처리하는 배치 프로시저는 매우 유용할 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
- PostgreSQL의 사용자 정의 함수(Function)와 프로시저(Procedure)의 정의 방법과 트랜잭션 제어 권한의 차이를 명확히 비교할 수 있다.
- 1:N 관계(주문-주문상품)의 데이터를 Supabase RPC 함수로 묶어 완벽한 원자성(Atomicity)을 보장하는 주문 트랜잭션을 구현할 수 있다.
- 대용량 데이터를 처리할 때 테이블 잠금(Lock)을 방지하기 위해 프로시저의 제어문(LOOP)과 자율 커밋(COMMIT)을 활용한 배치 작업을 작성할 수 있다.
```

## 목차

- [26.09.11](#260911)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - ["REST" "API" 이해하기](#rest-api-이해하기)
      - [REST API](#rest-api)
      - [SDK](#sdk)
    - [PostgreSQL 함수(function)와 프로시져를 활용한 트랜잭션](#postgresql-함수function와-프로시져를-활용한-트랜잭션)
      - [저장 프로그램 (stored program)](#저장-프로그램-stored-program)
      - [함수 vs 프로시저](#함수-vs-프로시저)
    - [\[실습1\] 프론트엔드를 위한 원자적 주문 함수 (Supabase RPC)](#실습1-프론트엔드를-위한-원자적-주문-함수-supabase-rpc)
      - [데이터 스냅샷 전략](#데이터-스냅샷-전략)
      - [JSONB 배열 처리를 포함한 주문 트랜잭션 함수 작성](#jsonb-배열-처리를-포함한-주문-트랜잭션-함수-작성)
      - [\[실습 2\] 백엔드를 위한 대용량 분할 커밋 프로시저 (Batch)](#실습-2-백엔드를-위한-대용량-분할-커밋-프로시저-batch)
  - [메모할 만한 것들](#메모할-만한-것들)
    - [개념 요약 키워드](#개념-요약-키워드)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### "REST" "API" 이해하기

#### REST API
- 표현적 상태 전이
- [행위] HTTP 메서드 - GET POST PUT PATCH DELETE
- [자원] URI (Uniform Resources Identifier) 자원 식별자
  - 하위개념 URL (Locator) - 컴퓨터 네트워크상의 자원 전체에 대한 표기법
  - 하위개념 URN (Name) - urn:scheme를 사용하는 URI를 위한 영속적 네임, 보통 URL만 쓴다 (리소스가 네임에 매핑되어야 하기에 찾기 힘듬)

#### SDK
- API 명세를 구현해 놓는 라이브러리


### PostgreSQL 함수(function)와 프로시져를 활용한 트랜잭션

PostgreSQL 내장 언어인 plpgsql을 사용하면 조건문, 반복문, 변수가 포함된 복잡한 로직을 DB 서버에 직접 구축할 수 있습니다.

#### 저장 프로그램 (stored program)

함수(function) => 매개변수의 반환값
- SELECT 구문 내에서 사용하는 AVG(), SUM()
- 함수는 계산을 수행하고 그 결과를 호출자에게 반환(Return)하는 목적으로 사용됩니다.
- Supabase에서는 이 함수들을 RPC(Remote Procedure Call) 방식으로 클라이언트에서 직접 호출합니다.

```sql
-- 기본 함수 정의 문법
CREATE OR REPLACE FUNCTION get_discounted_price(
    p_original_price INT, 
    p_discount_rate DECIMAL
) 
RETURNS INT           -- 반드시 반환할 데이터의 타입(RETURNS)을 명시해야 합니다.
LANGUAGE plpgsql
AS $$
DECLARE
    v_final_price INT; -- 내부 변수 선언
BEGIN
    v_final_price := p_original_price - (p_original_price * p_discount_rate);
    RETURN v_final_price; -- 최종 계산 값 반환
END;
$$;

-- 실행: SELECT get_discounted_price(10000, 0.1);
```

프로시져(procedure) => 독립 프로그램
- CALL 프로시저명(...)
- ostgreSQL 11버전부터 도입된 프로시저는 함수와 달리 내부에서 직접 COMMIT이나 ROLLBACK을 실행하여 트랜잭션을 능동적으로 제어할 수 있습니다.
- 단, 단일 값을 RETURN 문으로 반환할 수 없습니다.

```sql
-- 기본 프로시저 정의 문법
CREATE OR REPLACE PROCEDURE reset_daily_stats()
LANGUAGE plpgsql
AS $$
BEGIN
    UPDATE user_stats SET daily_visits = 0;
    
    COMMIT; -- 일반 함수에서는 불가능한 중간 커밋 확정 기능
END;
$$;

-- 실행: CALL reset_daily_stats();
```


#### 함수 vs 프로시저

| **구분** | **함수 (Function / Supabase RPC)** | **프로시저 (Procedure)** |
| --- | --- | --- |
| **호출 명령어** | SELECT 함수명(); | CALL 프로시저명(); |
| **반환값** | 반드시 존재 (RETURNS JSONB 등) | 없음 (INOUT 파라미터로 대체) |
| **자율 트랜잭션** | **불가능** (호출 시 하나의 통짜 트랜잭션으로 자동 묶임) | **가능** (COMMIT으로 중간 락 해제 가능) |
| **실무 활용처** | 프론트엔드와 직접 통신하는 주문/결제 로직 (성공 결과 반환 필요) | 수백만 건 데이터를 백엔드에서 쪼개서 정리하는 심야 배치(Batch) 작업 |



### [실습1] 프론트엔드를 위한 원자적 주문 함수 (Supabase RPC)

Supabase JS SDK를 사용할 때, 클라이언트에서 orders 테이블과 order_items 테이블에 각각 INSERT 요청을 보내면 네트워크 단절 시 심각한 데이터 불일치가 발생합니다. 이를 함수로 감싸면 전체 로직이 단일 트랜잭션으로 보호되어 에러 시 자동 롤백됩니다.


#### 데이터 스냅샷 전략
**가격 데이터를 중복해서 저장하는 이유 (스냅샷 패턴)**

- 원본 상품의 단가는 차후 인상되거나 할인될 수 있습니다. 
- 만약 order_items가 원본 상품의 price를 외래키로만 참조한다면, 상품 가격이 바뀔 때 과거 고객의 영수증 금액까지 소급되어 변경되는 문제가 생깁니다. 
- 따라서 **주문 결제 시점의 판매 단가와 상품명을 그대로 복사하여 order_items에 영구 보존(스냅샷)**해야 합니다. 
  - **또한, 클라이언트가 넘겨준 가격은 위변조될 위험이 있으므로 반드시 데이터베이스 내부에서 products 테이블을 조회하여 직접 계산합니다.**


#### JSONB 배열 처리를 포함한 주문 트랜잭션 함수 작성

클라이언트에서 동적인 장바구니 배열 데이터를 받아 마스터 주문서(orders)를 생성하고, 품목(order_items)을 검증·등록한 뒤 최종 계산 금액을 반환하는 함수를 작성합니다.

```sql
CREATE OR REPLACE FUNCTION create_order_transaction(
    p_order_name TEXT,
    p_order_email TEXT,
    p_order_phone TEXT,
    p_receiver_name TEXT,
    p_receiver_phone TEXT,
    p_zipcode TEXT,
    p_address TEXT,
    p_address_sub TEXT,
    p_delivery_memo TEXT,
    p_user_id UUID,
    p_items JSONB -- [{"product_id": "uuid", "quantity": 1}, ...]
)
RETURNS JSONB
LANGUAGE plpgsql
SECURITY DEFINER -- orders 및 order_items 테이블의 직접 INSERT 권한을 완전히 박탈(REVOKE)합니다.
									-- 유저는 테이블에 직접 데이터를 넣을 수 없으며, 오직 DB 관리자 권한으로 실행되는 create_order_transaction 함수를 통해서만 주문을 생성할 수 있게 강제
AS $$
DECLARE
    v_order_id UUID;
    v_order_no TEXT;
    v_total_price INT := 0;
    v_item RECORD;
    v_product RECORD;
BEGIN
    -- 장바구니 검증
    IF p_items IS NULL OR jsonb_array_length(p_items) = 0 THEN
        RAISE EXCEPTION '주문할 상품 목록이 비어 있습니다.';
    END IF;

    -- 고유 주문번호(order_no) 생성 (YYYYMMDDHH24MISS)
    v_order_no := TO_CHAR(CLOCK_TIMESTAMP(), 'YYYYMMDDHH24MISS');

    -- orders 테이블에 기본 주문서 삽입 (금액은 0으로 임시 저장)
    INSERT INTO orders (
        order_no, order_name, order_email, order_phone,
        receiver_name, receiver_phone, zipcode, address,
        address_sub, delivery_memo, total_price, order_status, user_id
    ) VALUES (
        v_order_no, p_order_name, p_order_email, p_order_phone,
        p_receiver_name, p_receiver_phone, p_zipcode, p_address,
        p_address_sub, p_delivery_memo, 0, 'READY', p_user_id
    )
    RETURNING id INTO v_order_id;

    -- JSONB 배열을 순회하며 order_items 삽입 및 총액 누적
    FOR v_item IN 
        SELECT 
            (elem->>'product_id')::UUID AS product_id,
            (elem->>'quantity')::INT AS quantity
        FROM jsonb_array_elements(p_items) AS elem
    LOOP -- FOR 루프 동작코드
        -- 수량 유효성 검사
        IF v_item.quantity <= 0 THEN
            RAISE EXCEPTION '주문 수량은 1개 이상이어야 합니다.';
        END IF;

        -- 원본 상품 정보 조회
        SELECT id, name, price 
        INTO v_product
        FROM products
        WHERE id = v_item.product_id AND deleted_at IS NULL;

        -- 상품 조회 결과가 없는 경우
        IF NOT FOUND THEN
            RAISE EXCEPTION '유효하지 않거나 삭제된 상품이 포함되어 있습니다. (ID: %)', v_item.product_id;
        END IF;

        -- 주문 상품 상세 등록 (단가 스냅샷 복사)
        INSERT INTO order_items (
            order_id, product_id, item_name, price, quantity
        ) VALUES (
            v_order_id, v_product.id, v_product.name, v_product.price, v_item.quantity
        );

        -- 총액 누적
        v_total_price := v_total_price + (v_product.price * v_item.quantity);
    END LOOP;

    -- 최종 계산된 총 결제금액으로 orders 행 갱신
    UPDATE orders 
    SET total_price = v_total_price 
    WHERE id = v_order_id;

    -- 결과 반환 (클라이언트에 응답)
    RETURN jsonb_build_object(
        'success', TRUE,
        'order_id', v_order_id,
        'order_no', v_order_no,
        'total_price', v_total_price
    );

EXCEPTION
    -- 도중 에러 발생 시 단일 트랜잭션 규칙에 따라 앞선 orders 삽입까지 모두 롤백
    WHEN OTHERS THEN
        RAISE EXCEPTION '주문 처리 실패 (트랜잭션 롤백): %', SQLERRM;
END;
$$;
```

Python Supabase SDK 연동 + 자동 롤백 검증
- Python 환경에서 supabase-py를 사용하여 방금 생성한 함수를 원격 호출합니다. 파이썬 클라이언트에서는 .rpc() 뒤에 .execute()를 호출해야 실제로 쿼리가 전송됩니다.

```python
import os
from supabase import create_client

# Supabase 클라이언트 초기화
url = os.getenv("SUPABASE_URL")
key = os.getenv("SUPABASE_KEY")
supabase = create_client(url, key)

def test_order_transaction():
    # 테스트 페이로드 구성 (의도적으로 두 번째 상품 ID를 비정상 UUID로 입력)
    payload = {
        "p_order_name": "홍길동",
        "p_order_email": "hong@example.com",
        "p_order_phone": "010-1234-5678",
        "p_receiver_name": "이순신",
        "p_receiver_phone": "010-9876-5432",
        "p_zipcode": "06234",
        "p_address": "서울시 강남구 테헤란로 123",
        "p_address_sub": "7층",
        "p_delivery_memo": "문 앞에 놓아주세요.",
        "p_user_id": "aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa",
        "p_items": [
            { "product_id": "11111111-1111-1111-1111-111111111111", "quantity": 1 }, # 정상 상품 (키보드)
            { "product_id": "99999999-9999-9999-9999-999999999999", "quantity": 2 }  # 존재하지 않는 가짜 상품
        ]
    }

    try:
        # Supabase RPC 원격 함수 실행
        response = supabase.rpc("create_order_transaction", payload).execute()
        print("주문 성공 완료! 응답 데이터:", response.data)

    except Exception as error:
        # DB에서 RAISE EXCEPTION이 발생하면 파이썬 Exception으로 포착됩니다.
        print("주문 처리 실패 (트랜잭션 롤백 완료):", error)

if __name__ == "__main__":
    test_order_transaction()
```


#### [실습 2] 백엔드를 위한 대용량 분할 커밋 프로시저 (Batch)
대용량 처리 시 락(Lock) 문제와 분할 커밋의 필요성
- 함수는 단일 트랜잭션의 원자성을 보장하지만, **작업이 끝날 때까지 락(Lock)을 쥐고 놓아주지 않는다**는 치명적인 특성이 있습니다.
  - 5만 건 이상의 서비스 **로그 데이터를 일괄 업데이트**할 경우, **테이블 전체에 잠금이 걸려 다른 사용자의 웹 서비스 요청이 전부 대기(Hang) 상태에 빠질 수 있습니다**.
- 이때는 프로시저(Procedure)를 생성하여 데이터를 5,000건씩 쪼개어 변경한 뒤, 반복문 안에서 수시로 ***COMMIT***을 호출하여 락을 즉각 해제해 주어야 합니다.


반복문(루프) 기반의 배치 프로시저 작성

```sql
CREATE OR REPLACE PROCEDURE batch_archive_logs(p_batch_size INT)
LANGUAGE plpgsql
AS $$
DECLARE
    v_rows_affected INT := 0;
    v_total_processed INT := 0;
    v_iteration INT := 0;
BEGIN
    LOOP
        v_iteration := v_iteration + 1;

        -- 서브쿼리를 통해 지정한 배치 크기만큼만 잠금(FOR UPDATE) 및 업데이트
        WITH target_batch AS (
            SELECT id FROM service_logs 
            WHERE is_archived = FALSE 
            ORDER BY id LIMIT p_batch_size
            FOR UPDATE
        )
        UPDATE service_logs 
        SET is_archived = TRUE 
        WHERE id IN (SELECT id FROM target_batch);

        -- 직전 UPDATE로 영향을 받은 행 개수 추출
        GET DIAGNOSTICS v_rows_affected = ROW_COUNT;
        
        -- 더 이상 처리할 데이터가 없으면 루프 즉시 탈출
        EXIT WHEN v_rows_affected = 0;

        v_total_processed := v_total_processed + v_rows_affected;
        RAISE NOTICE '[배치 %회차] %건 처리 완료 (누적: %건)', 
            v_iteration, v_rows_affected, v_total_processed;

        --  매 배치 회차마다 즉시 확정하여 락을 해제함
        COMMIT;
    END LOOP;

    RAISE NOTICE '[최종 완료] 총 %건의 로그 보관 처리가 완료되었습니다.', v_total_processed;
END;
$$;
```


```sql
-- 백엔드 스케줄러(Cron) 등에서 프로시저 호출 (5만 건을 5,000건 단위로 10번에 나누어 커밋)
CALL batch_archive_logs(5000);
```


## 메모할 만한 것들

### 개념 요약 키워드
- **사용자 정의 함수 (Function):** 결과를 반환(RETURNS)하며 단일 트랜잭션 블록으로 자동 보호되어 Supabase RPC와 결합 시 자동 롤백을 제공하는 객체입니다.
- **저장 프로시저 (Procedure):** 반환값은 없지만, 내부에서 COMMIT/ROLLBACK을 직접 실행하여 락을 분산할 수 있는 객체입니다.
- **스냅샷 패턴 (Snapshot):** 결제 시점의 단가처럼 시간에 따라 변하는 데이터를 복사하여 영구 보존하는 이커머스 핵심 설계 기법입니다.
- **jsonb_array_elements:** 프론트엔드에서 전달된 JSON 배열 파라미터를 관계형 행(Row) 형태로 언패킹하는 PostgreSQL 내장 함수입니다.
- **분할 커밋 (Batch Commit):** 프로시저 내에서 대용량 데이터를 특정 단위로 나누어 반복 확정함으로써 시스템 잠금(Lock)을 해제하는 기법입니다.


| **용어** | **영어 표현** | **설명** |
| --- | --- | --- |
| **원격 프로시저 호출** | RPC (Remote Procedure Call) | 클라이언트 환경에서 원격 데이터베이스에 정의된 함수를 API 형태로 직접 실행하는 인터페이스입니다. |
| **원자성** | Atomicity | 트랜잭션 내의 모든 작업이 100% 성공하거나, 하나라도 실패하면 모두 취소(Rollback)되어야 하는 특성입니다. |
| **스냅샷** | Snapshot | 기준 데이터가 변경되더라도 영향을 받지 않도록 거래 시점의 상태 값을 그대로 복사해 두는 저장 방식입니다. |
| **JSONB** | Binary JSON | 파싱된 바이너리 형태로 JSON 데이터를 저장하여 인덱싱과 탐색 속도를 극대화한 PostgreSQL 전용 타입입니다. |
| **행 잠금** | Row-level Lock | 데이터 갱신 중 동시성 충돌을 방지하기 위해 특정 행에 대해 타 트랜잭션의 쓰기 작업을 일시 차단하는 장치입니다. |
| **진단 정보 획득** | GET DIAGNOSTICS | 직전에 실행된 SQL 쿼리로 인해 실제로 영향을 받은 행(Row)의 수를 변수로 추출하는 PL/pgSQL 문법입니다. |


## Daily Quest 퀴즈 오답
Q. 주문 시점의 상품명과 단가를 order_items 테이블에 별도로 복사하여 영구 보존함으로써, 이후 원본 상품 가격이 변경되어도 과거 영수증 금액이 왜곡되지 않도록 하는 설계 기법의 이름은 무엇입니까?
- (정답) 스냅샷 패턴
- (틀린 이유) 기억 이슈

Q. 교안의 용어 사전과 3.2절 코드에서 GET DIAGNOSTICS v_rows_affected = ROW_COUNT; 구문을 통해 직전 UPDATE로 영향을 받은 행의 개수를 변수로 추출하는 것을 확인할 수 있습니다.
- (정답) GET DIAGNOSTICS
- (틀린 이유) DIAGNOSTICS만 써도 되는 줄 알았음


## 진단 퀴즈 오답
- 없음
