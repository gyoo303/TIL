# 26.08.11
KANT 7일차, python


## 오늘 느낀 점
- `[*range(1,101)]` 과 같이 간단하게 더미 데이터를 만드는 법을 기억해두면 편할 것 같다.
- 리스트 컴프리헨션을 사용하면 코드 가독성도 올라가고 뭔가 뿌듯한 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
- 조건문이 프로그램의 순차적인 실행 흐름을 제어하는 원리를 설명할 수 있다.
- if, elif, else를 활용하여 다중 분기 및 중첩 조건문을 올바른 들여쓰기로 작성할 수 있다.
- for문과 while 문의 구동 원리를 비교하고 목적에 맞게 선택할 수 있다.
- 루프 제어 명령어(break, continue)를 활용하여 반복의 진행 흐름을 변경할 수 있다.
```


### 오늘 수행한 실습과제

```python
if __name__ == "__main__":
    for i in range(1, 5):
        print(f"{i}번째 고객님입니다.")
```
```python

if __name__ == "__main__":
    current_temp = 25

    if current_temp >= 30:
        print("폭염 주의보가 발령되었습니다.")
    else:
        print("쾌적한 날씨입니다.")
```
```python
if __name__ == "__main__":

    age = 9
    height = 180

    if age >= 12 and height >= 130:
        print("탑승 가능합니다. 관리자 확인을 진행하십시오.")
    else:
        print("탑승 요건을 만족하지 못햇습니다.")
```

```python
if __name__ == "__main__":

    for i in range(1, 3):
        for j in range(1, 4):
            print(f"{i}층 {j}호")
```
```python
if __name__ == "__main__":
    battery_level = 80
    while battery_level <= 100:
        print(f"현재 배터리: {battery_level}")
        battery_level += 5
```
```python

if __name__ == "__main__":

    weight = 10

    if weight > 23:
        print("대형 수하물 대상입니다. 추가 요금이 부과됩니다.")
    elif weight > 15:
        print("일반 위탁 수하물 대상입니다.")
    else:
        print("무료 수하물 대상입니다.")
```
```python
if __name__ == "__main__":

    fruit_sweetness = [12.5, 14.2, 11.8, 10.5, 15.0, 13.6, 11.2, 14.8, 12.0, 13.1]

    max_sweetness = fruit_sweetness[0]
    min_sweetness = fruit_sweetness[0]
    total_sweetness = 0

    for sweet in fruit_sweetness:
        if sweet > max_sweetness:
            max_sweetness = sweet
        if sweet < min_sweetness:
            min_sweetness = sweet
        total_sweetness += sweet

    avg_sweetness = total_sweetness / len(fruit_sweetness)

    print(f"최고 과일 당도: {max_sweetness}")
    print(f"최저 과일 당도: {min_sweetness}")
    print(f"평균 과일 당도: {avg_sweetness:.2f}")
```

```python
if __name__ == "__main__":

    order_database = [
    {"name": "김철수", "completed": True, "price": 60000},
    {"name": "이영희", "completed": False, "price": 35000},
    {"name": "박민수", "completed": True, "price": 25000},
    {"name": "최지영", "completed": True, "price": 85000},
    {"name": "정성민", "completed": False, "price": 90000}
    ]

    total = 0
    order = 0

    for info in order_database:
        if info["completed"]:
            if info["price"] >= 50000:
                total += info["price"]
                order += 1

    print(f"우수 고객 주문 건수: {order}건\n우수 고객 총 결제 금액: {total}원")    
    
```

```python
import random

if __name__ == "__main__":
    random_number = random.randint(1, 50)
    total_try = 0

    while True:
        user_input = int(input("1~50 사이의 숫자를 입력하세요: "))
        total_try += 1

        if user_input < random_number:
            print("보물은 더 큰 숫자의 방에 있습니다.")
        elif user_input > random_number:
            print("보물은 더 작은 숫자의 방에 있습니다.")
        else:
            print(f"축하합니다! {total_try}회 만에 보물을 찾았습니다.")
            break
```


## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음



----------------
