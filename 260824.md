# 26.08.24
KANT 2일차, Git

## Git 변경사항 되돌리기

`git reset`
- 커밋ID or HEAD~(숫자)
- `--soft` 
  - commit만 취소/ 변경이력만 삭제/ 로컬소스 변경X
- `--mixed` (디폴트)
  - add, commit 둘다 취소/ log 변경이력만 삭제/ 로컬소스 변경x
- `--hard`
  - add, commit 둘다 취소/ 로컬소스(원본파일)까지 삭제
  - `git reflog` 통해 커밋ID 찾아서 reset으로 복구 가능
  - hard reset 아후 git add, git commit 실행하여 반영

`git reflog`
- commit, checkout, reset 히스토리 + 커밋ID 출력
- reset으로 삭제한 커밋ID로 다시 돌아갈 수 있다 (git reset)

`git revert`
- HEAD~
  - HEAD -> 현재 포인터 위치
  - HEAD~ -> 직전 단계 
  - HEAD~숫자 -> 숫자만큼 전 단계
- 커밋 기록 유지 
  - reset과 달리 변경이력을 삭제하지 않고 모두 기록
  - 신규 커밋의 형태로 수행한다

# Branch
- git branch
  - 현재 존재하는 branch 확인
- git branch 브랜치명
  - 브랜치는 일반적으로 기능 단위로 분리
- 브랜치는 파일이 독립적으로 관리된다
  - A브랜치에서 작업한 로컬 파일은 B브랜치에서 보이지 않는다

## Branch 이동
- git checkout -b 브랜치명
- git switch 브랜치명
  - git switch -c 브랜치명 : 새로 만들고 이동
  - git switch - : 직전브랜치 이동


## Branch 네이밍 컨벤션
- feature/____  → ex. feature/order-form
- fix/____
- bugfix/____
- hotfix/____
- release/____
- test/____
- docs/____
- dev/____
- ...

## Branch 병합
- `git merge 브랜치명`
  - 현재 브랜치를 타깃 브랜치와 병합한다
  - 병합 결과물 = 현재 브랜치
- 병합 방식(결과)는 아래 3가지
  - Merge 
    - 변경사항 중복X / 양측에 변경사항O
    - 신규 커밋을 추가하여 브랜치 병합 수행 (사용자 커밋 진행)
  - Fast-forward
    - 변경사항 중복X / 한쪽에만 변경사항O
    - 신규 커밋 X -> 커서만 옮기는 개념(각각 변경-commit 필요 없다)
  - Conflict
    - 변경사항

## Branch 삭제
- `git branch -d 브랜치명`
- `git branch -D 브랜치명` -> 강제삭제

## Branch 이름 변경
- `git branch -m 브랜치명`
- `git branch -M 브랜치명` -> 강제변경

## 로컬 변경 후 add 없이 브랜치 변경 시도 시
*Q. 브랜치가 공유하는 파일일 경우 자동 staging? - 어떤 때는 브랜치 변경이 불가하고, 어떤 때는 브랜치가 변경되는데 구체적인 상황이 궁금함.*
- 오류 - 브랜치 변경 불가능
  - 조금 있다가 변경사항을 반영하고 싶으면?
- 변경사항을 임시보관함에 넣으면 브랜치 변경 가능
  - `git stash` - 로컬 변경 취소 후 임시보관함 저장
  - `git stash list` - 임시보관함 확인
  - `git stash pop`
    - 임시 저장된 내용이 원복되고
    - 임시 저장된 내용을 보관함에서 지운다
    - 임시 저장 내용이 원복되면 add-commit 해서 처리 반영
    - pop -> (Last In, First Out)

## git rebase
- 커밋 순서 바꿔치기, B 브랜치의 작업 내용을 A 브랜치 다음에 수행된 선형 형태로 변경한다.
- Rebase 황금률 : '공유된 커밋은 rebase 하지 않는다' = 미공유 개인 로컬 브랜치에서만 사용한다.
-  git rebase -> 충돌 -> 로컬 파일 조정 후
  - git add ... -> git rebase --continue
  - 신규 커밋으로 인정
- 충돌 후 스킵 : git rebase --skip
- 충돌 후 중단(취소) : git rebase --abort
- Rebase 성공 후 merge
  - 깔끔하게 선형으로 정리된 상황: fast-forward 방식으로 merge 된다.



-----


## daily quest 오답
### Q. git diff --staged 명령어가 비교하는 대상으로 옳은 것은?

### 내가 선택한 답 : 워킹 디렉터리 vs 스테이징 영역

### 왜 틀렸나?
수업 실습 과정에서 add로 어떤 것들이 commit되지 않고 스테이징 상태에 남아 있는지 단순히 체크하는 명령어라고 이해했는데, 실제로 무엇이 반영될 지 최종 점검할 때 쓴다는 맥락은 잘 몰랐다. 어떤 목적으로 만들어진 명령어인가를 잘 인지하고 기억해야겠다.

### 모범 답안
git diff --staged는 git add로 스테이징된 내용과 마지막 커밋(HEAD) 사이의 차이를 보여줍니다. 커밋 전에 실제로 무엇이 반영될지 최종 점검할 때 사용합니다. 반면 git diff는 워킹 디렉터리와 스테이징 영역을, git diff HEAD는 워킹 디렉터리와 마지막 커밋을 비교합니다.