# 26.08.24
KANT 15일차, AI Literacy


## 오늘 느낀 점
- 적절한 프롬프트 내용을 통해 확률적으로 텍스트를 생성하는 모델의 벡터 스페이스 범위를 한정짓고, 사용자가 기대하는 답변을 생성할 확률을 올린다는 접근이 재미있었다.
- Numpy, Pandas를 이용해 데이터가 어떤 식으로 로드되고 연산되는지 대략적으로 알 수 있었다. Pandas가 조금 더 인간 친화적인 방향성을 가지고 있으며, Numpy는 C 기반으로 더 높은 효율을 기대할 수 있을 것 같다.

## 오늘 배운 것

```
### [ 학습목표 ]
- 프롬프트의 품질을 결정하는 5대 핵심 구성 요소(역할, 목적, 맥락, 조건, 형식)의 메커니즘을 이해할 수 있다.
- 업무 및 학습 목적에 맞게 5대 요소로 구조화된 프롬프트의 구성 원리를 설명하고, 좋은 예시와 나쁜 예시를 구버할 수 있다.
- NumPy 배열의 타입 지정, 차원 제어 및 선형대수 연산을 수행할 수 있다.
- Pandas Series와 DataFrame을 생성·조작하고 인덱스를 제어할 수 있다.
```

## 목차

- [26.08.24](#260824)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [프롬프트 기법](#프롬프트-기법)
      - [Zero-shot / Few-shot / Role-prompting](#zero-shot--few-shot--role-prompting)
      - [Zero-shot CoT (Zero-shot Chain-of-Thought)](#zero-shot-cot-zero-shot-chain-of-thought)
      - [Few-shot CoT (Chain of Thought)](#few-shot-cot-chain-of-thought)
      - [Step-Back Prompting](#step-back-prompting)
    - [레고처럼 조립하는 프롬프트 5대 핵심 요소 (R-T-C-C-F)](#레고처럼-조립하는-프롬프트-5대-핵심-요소-r-t-c-c-f)
      - [**TIPS**](#tips)
      - [프롬프트 작성 실습](#프롬프트-작성-실습)
      - [꼬리 질문을 통한 점진적 품질 고도화](#꼬리-질문을-통한-점진적-품질-고도화)
    - [Lovable 실습](#lovable-실습)
    - [Numpy, Pandas, Matplotlib](#numpy-pandas-matplotlib)
      - [Numpy : 고속 연산, 슬라이싱 및 차원 제어](#numpy--고속-연산-슬라이싱-및-차원-제어)
      - [Pandas](#pandas)
      - [데이터 탐색 속성(shape, dtypes)과 진단(info, describe)](#데이터-탐색-속성shape-dtypes과-진단info-describe)
      - [데이터 변경 및 인덱스 제어(set\_index, reset\_index)](#데이터-변경-및-인덱스-제어set_index-reset_index)
      - [Pandas Series \& DataFrame 브로드캐스팅 연산](#pandas-series--dataframe-브로드캐스팅-연산)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 프롬프트 기법

#### Zero-shot / Few-shot / Role-prompting
* Zero-shot : 별도의 입출력 예시(Example) 없이 지시문(Instruction)과 입력 데이터
* Few-shot : 원하는 출력 형태, 논리 구조, 분류 기준을 모델에게 명확히 전달하기 위해 몇 가지 예시
(Sample)를 제시한 후 질문
* Role-prompting : AI에게 특정 직업, 페르소나, 전문가의 정체성을 지정하여 답변의 전문성, 관점, 어조를 고정
하는 기법

#### Zero-shot CoT (Zero-shot Chain-of-Thought)
예시 없이 단순 지시문 끝에 "단계별로 생각해보자(Let's think step by step)"와 같은 추론 유도 문구를 추가하여 복잡한 계산이나 논리적 문제 해결 능력을 끌어올리는 기법

#### Few-shot CoT (Chain of Thought)
예시를 제공할 때 정답만 보여주는 것이 아니라 정답
에 이르는 중간 추론 단계(Thought Process)를 함께 명시하는 방식

#### Step-Back Prompting
1. 사용자 질의 작성
2. 질의에 필요한 배경지식 먼저 질문
3. 배경지식 도출
4. 도출된 배경지식을 기반으로 사용자 질의에 답하라고 요청 
-> 환각 현상을 최소화하는 효과



### 레고처럼 조립하는 프롬프트 5대 핵심 요소 (R-T-C-C-F)


**R-T-C-C-F 프레임워크**
1. 역할 (Role)
   1. 전문가의 페르소나와 출력 확률 분포의 재구성
   2. 당신은 B2B SaaS 시장에 대한 깊은 이해도를 가진 15년 차 B2B 콘텐츠 마케팅 전문가입니다.
2. 목적 (Task)
   1. 핵심 임무 하달과 어텐션 메커니즘의 집중
   2. 제공된 주간 회의록 텍스트를 바탕으로, 각 부서별 'Action Item(실행 과제)'을 추출하여 1차 보고서 초안을 작성해 주세요.
3. 맥락 (Context)
   1. 배경지식 제공을 통한 컨텍스트 엔지니어링
   2. 맥락 기반 결과물 조정(In-context steering) 효과
4. 제약조건 (Constraint)
   1. 탈선 방지 목적
   2. 물리적/논리적 규칙 설정
   3. 네거티브 프롬프팅(Negative Prompting)
5. 형식 (Format)
   1. 데이터의 구조화 및 출력 그릇의 설계
   2. 후가공(Post-processing) 시간 단축 ->
출력 형식을 사전에 명확히 지정하면 생성된 결과물을 외부 업무용 문서(엑셀, 워드, 노션 등)에 바로 복사하여 붙여넣을 수 있어 업무 효율성을 획기적으로 향상시킵니다. 

| **요청하는 형식 종류** | **프롬프트 적용 예시** | **실무 활용 포인트** |
| --- | --- | --- |
| **마크다운 표 (Table)** | "결과물은 [장점 | 단점 | 실무 적용 방안 | 기대 효과]를 헤더로 하는 표 형식으로 정리해 줘." | 복잡한 대안들을 한눈에 비교 분석하거나 보고서에 즉시 삽입할 때 유용합니다. |
| **코드 스니펫 (Code)** | "위의 고객 분류 데이터를 파이썬(Python)의 딕셔너리(Dictionary) 형태로 출력해 줘." | 개발 업무 보조 및 데이터 파이프라인 연계 시 텍스트 파싱 오류를 줄여줍니다. |
| **이메일 초안 형태** | "제목: [명확한 용건 요약], 본문: [인사말-상황 설명-요청 사항-맺음말] 구조로 작성해 줘." | 외부 파트너사나 고객에게 발송할 커뮤니케이션 문서 초안 작성 시 시간을 절약합니다. |



#### **TIPS**
**행동 동사(Action Verb)의 사용** -
목적은 인공지능이 궁극적으로 수행해야 할 단 하나의 핵심 행동을 지칭하며, 프롬프트 내에서 명확한 동사의 형태로 가장 두드러지게 표현되어야 합니다.

**체인 프롬프팅(Chain of Prompting)** -
하나의 프롬프트에 너무 많은 목적을 한꺼번에 담으면 인지적 주의력이 분산되어 완수 확률이 낮아집니다. 복잡한 작업은 단계를 잘게 나누어 순차적으로 지시하는 것이 바람직합니다.

**맥락 기반 결과물 조정(In-context steering) 효과** - 구체적인 배경을 풍부하게 제공하면 AI는 어조를 진중하게 바꾸거나 데이터 중심의 논리를 전개하는 등 결과물의 결을 사용자의 상황에 완벽히 맞추어 출력하며, 환각 현상 또한 극적으로 줄어듭니다. 

**네거티브 프롬프팅(Negative Prompting)** -
'하지 말아야 할 것'을 명확히 지정하는 기법입니다. "어려운 한자어나 전문 용어를 남발하지 마라", "뻔한 이야기는 철저히 제외하라"와 같은 조건을 걸어두면 불필요한 사족을 방지하고 실용적인 결과물을 얻을 수 있습니다. 


#### 프롬프트 작성 실습
```
[Role]
당신은 20년차 동남아 여행 전문가입니다.

[Task]
여름에 가족들과 함께 조용한 휴식을 취할 수 있는 3박 4일 필리핀 여행 일정을 작성해 주세요.

[Context]
- 여행 인원: 4인 가족(초등학생 1, 유아1)
- 이동 수단: 렌터카 이용
- 호텔: 최소 4성급 이상
- 참고 사항: 초등학생ㅇ과 유아가 할만한 액티비티 참여

[Constraints]
- 가족 여행이므로 가족이 함께 참여할 수 있는 장소로 선택할 것
- 지역은 필리핀 세부로 한정
- 초등학생ㅇ과 유아가 포함되어 있으므로 이동시간이 길지 않아야 함

[Format]
- 1~4일차 별로 [오전/점심/오후/저녁]으로 타임라인을 Markdown 형식의 표로 정리할 것
- 각 항목들에는 참고할 여행팁을 2줄 이내로 포함 시켜 줄 것
```



#### 꼬리 질문을 통한 점진적 품질 고도화


**실무 고도화를 위한 꼬리 질문의 3가지 핵심 유형**

---

**톤앤매너 및 문체 수정**
* 이 표의 '활동 상세 내용' 부분의 말투를 '~해요', '~해볼까요?' 같은 부드럽고 친절한 구어체 톤으로 전체적으로 다시 수정해 주세요.

**특정 요소 딥다이브(Deep-dive)**
* 2일 차에 제안해 준 '사내 방탈출 게임 미션' 아이디어가 아주 신선하고 마음에 듭니다. 이 방탈출 게임 아이템만 따로 떼어서, 1시간 동안 진행할 수 있는 구체적인 시나리오 단계와 진행자가 사전에 필요한 준비물 리스트만 상세하게 다시 기획해 줄 수 있나요?


**비판적 시각(Critical Thinking) 및 레드팀 요구**
* 당신이 제안한 이 3일 일정에서, 만약 매우 내향적이고 낯을 가리는(I 성향) 신입사원이 참가한다면 가장 부담스럽고 피로감을 느낄 만한 프로그램은 무엇일까요? 그 프로그램을 찾아서 논리적인 이유를 설명하고, 내향적인 사람도 편안하게 소속감을 느낄 수 있는 대안 프로그램으로 교체하여 표를 다시 작성해 주세요.


### Lovable 실습



```
"너는 Lovable.dev, v0, Cursor 등 AI 코딩 빌더에 최적화된 Web Application PRD(제품 요구사항 문서) 작성 전문가야.
내가 전달하는 [앱 아이디어]를 바탕으로, Lovable이 한 번에 정확한 코드를 생성할 수 있도록 완벽하게 구조화된 ""Lovable 전용 웹 앱 기획서""를 작성해 줘.

### [기본 기술 스택 전제]
- Frontend: React (Vite), Tailwind CSS, shadcn/ui, Lucide Icons
- Backend/Database: Supabase (Auth, Postgres DB, Storage)
ㄴ
---

### [출력 형식 및 작성 가이드]

1. **앱 개요 (Overview)**
   - 앱 이름, 목적, 주요 타겟 유저, 핵심 가치 제안

2. **페이지 구조 및 라우팅 (Page Architecture)**
   - 경로별 페이지 구성 (예: `/dashboard`, `/settings`)
   - 각 페이지에 배치될 주요 UI 컴포넌트 목록 (shadcn/ui 기반)

3. **데이터베이스 스키마 설계 (Supabase DB Schema)**
   - 필요한 테이블 명, 컬럼 명, 데이터 타입, 제약 조건 (PK, FK, Unique)
   - RLS(Row Level Security) 정책 요약

4. **페이지별 UI/UX 및 기능 상세**
   - 각 화면의 레이아웃 및 사용자 흐름 (User Flow)
   - 각 화면별 4가지 상태 정의 (Initial/Empty, Loading, Success, Error)

5. **Lovable 단계별 입력 프롬프트 (Step-by-Step Prompts)**
   *Lovable에 순차적으로 입력할 수 있는 실전 프롬프트를 작성해 줘.*
   - **Phase 1 (MVP/기초 레이아웃 & Supabase 연동):** 전체 레이아웃, 더미 데이터 기반 UI, DB 스키마 생성용 프롬프트
   - **Phase 2 (핵심 CRUD 및 데이터 로직):** 실제 데이터 C.R.U.D 처리 및 상태 관리 구현 프롬프트
   - **Phase 3 (디테일 및 예외 처리):** 예외 처리, 로딩 스피너, 토스트 알림, 반응형 모바일 최적화 프롬프트

---

[앱 아이디어]:
(여기에 만들고자 하는 앱 아이디어를 자유롭게 적으세요. 예: 개인 사용자를 위한 구독 서비스 관리 및 지출 분석 대시보드)"
"""=== TaskFlow (칸반 보드) Lovable 단계별 프롬프트 모음 ===

---
[Phase 1: 전체 UI 레이아웃 및 더미 데이터 기반 칸반 보드 구축]
---
React, Tailwind CSS, Lucide Icons, shadcn/ui 컴포넌트를 사용하여 개인 및 팀을 위한 모던 칸반 보드 웹 애플리케이션 ""TaskFlow(태스크플로우)""를 만들어 주세요. 모든 UI 텍스트와 레이블은 한국어로 작성해 주세요.

[레이아웃 구조]
- 상단 네비게이션 바: 앱 로고(""TaskFlow""), 보드 이동 드롭다운, 프로필 아바타, ""+ 새 보드"" 버튼 포함.
- 좌측 사이드바: 내 보드 목록 바로가기, 설정 메뉴 포함.

[페이지 구현 (더미 데이터 우선 사용)]
1. `/boards (보드 목록)`:
   - 보드 카드가 그리드 형태로 나열됨. 각 카드는 보드 제목, 설명, 색상 태그, 생성일 포함.
   - ""+ 새 보드 생성"" 다이얼로그 모달 포함 (제목, 설명, 대표 색상 선택).
2. `/board/:boardId (칸반 보드 상세)`:
   - 상단 필터바: 검색창, 우선순위 필터(높음/보통/낮음), ""+ 컬럼 추가"" 버튼.
   - 가로 스크롤 가능한 칸반 영역: 기본 컬럼 3개 (""할 일"", ""진행 중"", ""완료"").
   - 컬럼 카드 구성: 카드 제목, 우선순위 배지(높음: 빨강, 보통: 노랑, 낮음: 파랑), 마감일 배지, 체크리스트 현황(예: 1/3).
   - 태스크 상세 모달 (`Dialog`): 태스크 제목, 설명, 우선순위 변경, 마감일 달력 선택(`Popover` + `Calendar`), 체크리스트(Subtasks) 추가 및 체크박스 토글 기능.
3. 드래그 앤 드롭 구현 준비: `@hello-pangea/dnd` 또는 `@dnd-kit`을 사용하여 컬럼 간 태스크 카드 이동 시각 효과 적용.



---
[Phase 2: Supabase 연동, 인증(Auth) 및 데이터 CRUD 로직 구현]
---
TaskFlow 애플리케이션에 Supabase를 연동하여 회원가입/로그인 및 데이터 영구 저장을 구현해 주세요. 모든 UI는 한국어로 유지해 주세요.

[작업 내용]
1. Supabase 인증 활성화: `/login` 페이지에서 이메일/비밀번호 로그인 및 회원가입 구현.
2. Supabase 데이터베이스 스키마 구성:
   - `profiles` (id, email, display_name, avatar_url)
   - `boards` (id, user_id, title, description, color, created_at, updated_at)
   - `columns` (id, board_id, title, position, created_at)
   - `tasks` (id, column_id, board_id, title, description, priority, due_date, position, created_at, updated_at)
   - `subtasks` (id, task_id, title, is_completed, position)
3. RLS(Row Level Security) 정책 적용: 유저가 자신의 보드 및 속한 컬럼/태스크만 C.R.U.D 처리 가능하도록 구현 (`auth.uid() = user_id`).
4. 실제 데이터 C.R.U.D 연동:
   - 보드 생성, 컬럼 추가/수정/삭제, 카드 추가/수정/삭제.
   - 태스크 카드를 다른 컬럼으로 드래그 앤 드롭 시 Supabase `tasks` 테이블의 `column_id` 및 `position` 컬럼이 즉시 업데이트되도록 구현.
   - 태스크 상세 모달 내 체크리스트 항목 추가, 삭제, 완료 토글 처리.



---
[Phase 3: 예외 처리, 디테일 Polish, 토스트 알림 및 반응형 최적화]
---
TaskFlow 애플리케이션의 사용자 경험(UX)을 완성하고 예외 처리 및 반응형 스타일을 적용해 주세요.

[요구사항]
1. UX 및 로딩 상태:
   - 보드 및 칸반 카드 로딩 시 shadcn `Skeleton` 로더 표시.
   - 컬럼이나 카드가 없는 경우 ""카드를 추가해 보세요"" 안내 문구와 깔끔한 Empty State 표시.
2. 토스트 알림 연동:
   - shadcn `Toast` / `Sonner` 연동 (예: ""새 카드가 생성되었습니다"", ""카드가 이동되었습니다"", ""삭제 완료"").
3. 반응형 최적화:
   - 모바일 기기(768px 미만)에서는 칸반 컬럼이 세로 탭 스크롤 또는 스와이프 형태 카드 뷰로 전환.
   - 모바일 햄버거 메뉴를 통한 보드 목록 사이드바 전환.
4. 애니메이션 및 터치 드래그 지원:
   - 태스크 드래그 시 부드러운 호버 및 그림자 효과 적용. 모바일 터치 기반 드래그 앤 드롭 지원."
```


### Numpy, Pandas, Matplotlib

#### Numpy : 고속 연산, 슬라이싱 및 차원 제어

배열
* 메모리상 연속된 구조, 인덱스 연산자([]) 접근 가능
* 동일한 자료형으로 데이터가 구성
* C언어로 개발 (직접 메모리 접근)

```python
import numpy as np

nums = np.array([10,20,30])
print(nums)
print(nums[1])

matrix = np.array([
    [10,20,30],
    [40,50,60],
    [70,80,90]
]) # 2차원 배열

# 3차원 이상의 배열 -> tensor

print(matrix)
```

배열 선택, 슬라이싱
```python
print(matrix[:, 0])
print(matrix[1:3, :2])
```

차원 변경(reshape, flatten)과 축(Axis)
- reshape(행, 열) / flatten()
  - `-1` : 자동 결정
- arange(시작, 종료, 증감단위) -> 시작 이상 종료 미만으로 숫자 생성

```python
arr = np.arange(1, 13) # 1 ~ 12

mat_3x4 = arr.reshape(3, 4)
print(mat_3x4)

mat_3x4 = arr.reshape(3, -1)
print(mat_3x4)

mat_3x4 = arr.reshape(-1, 4)
print(mat_3x4)

# 기본값: .flatten(order='C') -> 행 우선
# .flatten(order='F') -> 열 우선
# .flatten(order='A') -> 메모리 배치 형태에 따라 결정
# .flatten(order='K') -> 메모리에 저장된 순서대로 펼침
mat_flatten = mat_3x4.flatten()
print(mat_flatten)
```

축(axis) : 2차원 배열에서 연산 방향을 설정
- axis 0 -> 행 기준에서 연산
- axis 1 -> 열 기준에서 연산

```python
mat = np.arange(1,7).reshape(2, -1)
print(mat)

sum_axis0 = mat.sum(axis=0)
sum_axis1 = mat.sum(axis=1)

print("행 기준 합계:", sum_axis0)
print("열 기준 합계:", sum_axis1)
```

Numpy 브로드캐스팅(Broadcasting) 연산
```python
mat = np.array([
    [10,20,30],
    [40,50,60]
])

print(mat * 5) # 모든 요소 각각 *5
print(mat + 5)

vec = np.array([1, 2, 3])
print(mat + vec) # 각 차원에 대하여 + // 단, 첫번째 차원의 크기는 동일해야 한다.
```

행렬 전치(Transpose)

* `객체.transpose()` 또는 `객체.T` -> (행, 열) -> (열, 행) / 전치

```python
# 2x3
mat = np.array([
    [1,2,3],
    [4,5,6]
])

print(mat)
print("전치 행렬:\n", mat.transpose()) # 2x3 -> 3x2
print("전치 행렬:\n", mat.T) # 2x3 -> 3x2
```

행렬곱 연산(@)
- 행렬곱을 하기 위해서는 차원 수가 대칭이어야 함
- 행렬과 자신의 전치행렬은 항상 행렬곱이 가능하다.
```python
print(mat@mat.T) # 2x3 @ 3x2 -> 2x2
# [[14 32]
#  [32 77]]
```

#### Pandas
Pandas 기본 구조 : Series와 DataFrame
* Series : 1차원 구조체

```python
import pandas as pd

scores = pd.Series([90, 85, 88])
print(scores) # 인덱스와 데이터로 구성
print(type(scores)) # pandas.Series
```

* DataFrame : 2차원(행, 열) 구조체

```python
data = {
    "이름": ['김철수', '이영희', '박민수'],
    "부서": ["개발", "기획", "개발"],
    "급여": [4500, 5200, 4800]
}

df = pd.DataFrame(data)
print(df)
```

#### 데이터 탐색 속성(shape, dtypes)과 진단(info, describe)

- 탐색적 데이터 분석(EDA)
- `df.info()`
  - 누락치 확인(Non-Null Count)
  - dtype: 통계 처리가 가능한 자료형인지 체크 -> (잘못된 데이터 자료형 변환)
- `df.describe()`
  - 평균, 중앙값, 최대, 최소값을 통해서 이상치(극단치) 확인

```python
print("df.info()")
print(df.info())
print(df.describe()) # 수치 데이터에 대해 갯수, 평균, 표준편차, 최대최소치 등 기본 통계량을 보여준다.
```

#### 데이터 변경 및 인덱스 제어(set_index, reset_index)
```python
# df['급여'], type(df['급여'])
df['보너스'] = df['급여'] * 0.1
print(df)

# 인덱스를 변경
# '이름' 컬럼을 인덱스로 설정한다.
df_indexed = df.set_index('이름')

# 인덱스 초기화
df_reset = df_indexed.reset_index() # 이름 컬럼이 다시 복구되고 인덱스는 넘버 인덱스로 초기화
```


#### Pandas Series & DataFrame 브로드캐스팅 연산
- `axis = 0` -> 행의 인덱스 기준에서 연산한다, 행 축을 쭉 타고 내려오면서 연산한다.
- `axis = 1` -> 열 기준에서 연산한다, 열 축을 쭉 이동해 나가면서 연산한다.

```python
df = pd.DataFrame({
    '중간고사' : [80, 95, 70],
    '기말고사' : [85, 90, 75],   
}, index = ["김철수", "이영희", "박민수"])

score_means = df.mean(axis = 0) # 행 기준 (인덱스 기준)
print(score_means)

score_gap = df.sub(score_means, axis=1)
print(score_gap)
```

## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
Q. 여러 값을 하나의 변수에 튜플 형태로 묶어 담는 것을 '패킹'이라고 합니다. 반대로 하나의 자료구조에 담긴 값들을 여러 개의 변수에 순서대로 나누어 담는 동작을 무엇이라고 하나요?

- 해설
  - 시퀀스 자료구조의 요소들을 각각 독립된 변수에 순서대로 대입하여 분해하는 기술을 언패킹이라고 하며, 이때 자료구조의 요소 개수와 변수의 개수가 일치해야 정상적으로 분해됩니다.
- 틀린 이유
  - 반대 개념을 부르는 명칭이 있었는지 기억이 나지 않았음

Q. 다음 중 프로그램 실행 도중에 발생하는 '런타임 예외(Exception)'에 해당하는 것은?

- 해설
  - 콜론 누락, 들여쓰기 불일치, 괄호 미매칭은 모두 실행 전 단계에서 인터프리터가 걸러내는 구문 오류(Syntax Error)입니다. 반면 0으로 나누는 연산은 문법적으로는 문제가 없으나 실행 중에 ZeroDivisionError라는 런타임 예외를 발생시킵니다.
- 틀린 이유
  - "신택스 에러"와 "런타임 에러"의 정확한 정의/개념이 헷갈렸음
  - 인터프리터 단계에서 코드의 '문법'이 잘못됨 -> 신택스 에러
  - 코드 '문법'은 이상이 없어서 코드가 실행되었으나 실행 중 문제 발생 -> 런타임 에러


## 진단 퀴즈 오답
- 없음


