# 26.08.19
KANT 12일차, python


## 오늘 느낀 점
- 사용자의 예상치 못한 입력 등에 대응할 때 예외 처리는 필수 같다.
- 예외 상황에 적합한 Exception을 직접 자식 클래스로 정의하면, 사용자에게 적절한 안내를 제공할 수 있다.
- VS Code 중단점은 복잡한 함수 등을 구현할 때, 중간중간 확인해 나가면서 작업할 때 유용해 보인다. 또한, 에러가 발생했는데 원인을 한 번에 알기 어려울 때 디버그 모드로 로컬/글로벌 변수를 트래킹하는 것도 좋은 해결책이 될 것 같다.

## 오늘 배운 것

```
### [ 학습목표 ]
- 예외 처리로 숫자가 아닌 값 입력 시 프로그램의 비정상 종료를 방지할 수 있다.
- 리스트 범위 예외를 처리하고 성공·실패와 관계없이 마무리 코드를 실행할 수 있다.
- 의도적 예외(raise)로 기획 의도에 맞지 않는 데이터 입력 시 흐름을 제어할 수 있다.
- 트레이스백 메시지를 읽고 에러 발생 위치와 최종 원인을 찾아낼 수 있다.
- VS Code 중단점을 활용하여 코드 실행을 일시 정지시킬 수 있다.
- Step Over 명령으로 흐름을 단계별로 제어하며 변수 상태를 확인할 수 있다.
- 내장 함수를 활용하여 데이터의 자료형과 조건을 검증할 수 있다.
- 수치 데이터의 총합, 개수, 반올림을 계산하여 통계 지표를 구할 수 있다.
```

## 목차

- [26.08.19](#260819)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [클래스 변수(정적 변수), 인스턴스 변수](#클래스-변수정적-변수-인스턴스-변수)
    - [@classmethod, @staticmethod](#classmethod-staticmethod)
    - [예외 처리](#예외-처리)
      - [구문 오류와 런타임 예외](#구문-오류와-런타임-예외)
      - [파이썬 대표 예외](#파이썬-대표-예외)
      - [try - except - else - finally](#try---except---else---finally)
      - [의도적 예외 발생을 위한 raise 키워드](#의도적-예외-발생을-위한-raise-키워드)
      - [raise를 활용한 데이터 유효성 검증](#raise를-활용한-데이터-유효성-검증)
      - [사용자 정의 예외](#사용자-정의-예외)
      - [traceback](#traceback)
    - [파이썬 디버깅 기초](#파이썬-디버깅-기초)
      - [Python debugger (VS Code Extension)](#python-debugger-vs-code-extension)
    - [내장 함수](#내장-함수)
      - [`isinstance()`](#isinstance)
      - [`all()` \& `any()`](#all--any)
      - [`len()`, `sum()`, `round()`](#len-sum-round)
      - [`mean()`, `median()`](#mean-median)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
    - [오답 해설](#오답-해설)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


### 클래스 변수(정적 변수), 인스턴스 변수

- 정적 변수 호출은 인스턴스 변수와 헷갈릴 수 있다
  - 따라서, 정적 변수의 경우 `클래스명.변수명` 형태로 호출한다 -> '클래스 변수'라고 부른다

```python
class User:
    pass

user = User()
user.name = "김철수"
user.age = 20

print(user.name, user.age)
```

```python
class User:
    count = 0

user1 = User()
user2 = User()

print(user1.count, user2.count)

user1.count = 10
print(user1.count, user2.count)

### 인스턴스 변수랑 헷갈린다!
### 정적변수의 경우 클래스명으로 접근한다.
User.count = 100
print(User.count)
```

### @classmethod, @staticmethod
- 객체를 생성하지 않아도 접근할 수 있는 메서드를 정의할 때

```python
class Calculator:

    # clz -> Calculator 클래스 객체 참조 변수
    @classmethod
    def add(clz, num1, num2):
        clz.count += 1
        print("호출 횟수:", clz.count)
        return num1 + num2
    
    @staticmethod
    def minus(num1, num2):
        return num1 - num2

Calcualtor.add(10, 20)
```


### 예외 처리

#### 구문 오류와 런타임 예외
구문 오류(Syntax Error)
- 파이썬 문법 사용상의 문제
- 인터프리터가 해석 -> 실행(런타임) X

런타임 예외(Exception)
- 문법상에는 문제 X
- 논리적 오류
- 실행 과정 중 발생하는 문제

#### 파이썬 대표 예외
- IndexError
  - 리스트, 튜플 등 인덱스로 접근하는 객체에서 존재하지 않거나 잘못된 인덱스로 접근
- ValueError 
  - 특정 자료형으로 변환이 불가하거나 처리가 어려울 때 발생 (int(..), float(..))
- TypeError
  - (예) 다른 타입끼리 연산을 하는 경우
- ZeroDivisionError
  - 0으로 나누기를 시도할 때


#### try - except - else - finally

```python
try:
    # 예외가 발생할 가능성이 있는 코드

except:
    # 예외가 발생하면 유입되는 구간, 예외에 대한 대안 처리
    # 대안 로직, 로깅 처리
    # 여러번 사용하여 에러 종류별로 다르게 처리 가능 (입력 순으로 호출됨)
    # 에러 종류를 tuple로 묶어서 에러 그룹별 처리 가능

else:
    # 예외가 발생하지 않으면 실행되는 구간

finally:
    # 예외발생 여부와 상관없이 항상 실행하는 필수 공통 처리
    # 자원 정리(메모리 해제 등)
```

```python
try:
    num = int(input())
    result = 10 / num

    print(result)

except:
    print("0이 아닌 정수를 입력하세요.")

else:
    print("정상 처리")

finally:
    print("로깅처리...")
```


#### 의도적 예외 발생을 위한 raise 키워드

- 파이썬 인터프리터 입장에서는 문법적 오류가 아니더라도, 프로그램 기획 의도나 비즈니스 규칙에 맞지 않는 데이터가 들어왔을 때 개발자가 직접 에러를 발생시켜 흐름을 제어할 수 있습니다.

```python
try:
    num = int(input())
    result = 10 / num

    print(result)

except ZeroDivisionError:
    print("0이 아닌 정수를 입력하세요.")

except ValueError:
    print("정수만 입력하세요")

except (ZeroDivisionError, ValueError):
    print("그룹 지어서 처리하는 것도 가능합니다")

except ZeroDivisionError as e:
    # 에러 클래스를 직접 정의할 경우 커스텀 에러 메시지 지정 가능 (__str__)
    print(e) # divided by zero
```

- 모든 예외 클래스는 Exception의 자식 클래스
- 예외 종류를 특정하지 못하는 경우 Exception으로 처리 가능


```python
try:
    num = int(input())
    result = 10 / num

    print(result)

except ZeroDivisionError:
    print("0이 아닌 정수를 입력하세요.")

except Exception as e: # Exception은 가장 하단 except로 정의한다
    print(e)

```

#### raise를 활용한 데이터 유효성 검증
- 예외를 발생 시키기

```
raise 예외클래스("메시지...")
```

```python
def login(user_id, user_pass):
    if not (user_id == "user01"):
        print("아이디가 일치하지 않습니다.")
        return
    
    if not (user_pass == "1234"):
        print("비밀번호가 일치하지 않습니다.")
        return

    # 다 통과 -> 로그인 처리
    print("로그인 처리 로직...")


def login2(user_id, user_pass):
    if user_id != "user01":
        raise ValueError("아이디가 일치하지 않습니다.") # 예외발생 -> 코드실행 중단
    if user_pass != "1234":
        raise ValueError("비밀번호가 일치하지 않습니다.") # 예외발생 -> 코드실행 중단

    print("로그인 처리 로직...")

```

```python
# 회원페이지
try:
    login2("user02", "1234")
except ValueError as e:
    print(e)
    print("관리자 페이지에서 추가적으로 할 로직...")
```


#### 사용자 정의 예외
- Exception은 모든 예외의 부모클래스
- Exception을 상속 받으면 또는 Exception의 자식 클래스를 상속 받으면 -> 그 클래스도 예외 클래스
- `__str__` 메소드를 통해 커스텀 에러 메시지 정의 가능

```python
class UserIdMismatchError(Exception):
    def __str__(self):
        return "유저 ID가 일치하지 않습니다. 유감입니다."

class userPassMismatchError(Exception):
    def __str__(self):
        return "유저 비밀번호가가 일치하지 않습니다. 안타깝네요."

def login3(user_id, user_pass):
    if user_id != "user01":
        raise UserIdMismatchError() # 예외발생 -> 코드실행 중단
    if user_pass != "1234":
        raise userPassMismatchError() # 예외발생 -> 코드실행 중단

    print("로그인 처리 로직...")

try:
    login3("user02", 1235)
except (UserIdMismatchError, userPassMismatchError) as e:
    print(e)
```

#### traceback
`import traceback`
- `traceback.print_exc()` 에러 스택 콘솔 출력
- `traceback.format_exc()` 에러 스택을 문자열로 반환
- `traceback.print_stack()` 에러 없이 현재 호출 흐름 확인

```python
import traceback

try:
    result = 10 / 0
    print(result)
except:
    print("에러 발생")
    
    traceback.print_exc()
    print(traceback.format_exc())
```


### 파이썬 디버깅 기초

#### Python debugger (VS Code Extension)
- 파이썬 파일(노트북 포함)의 코드 실행 중단점 등을 설정 가능
  - 중단점 걸고 `Debug Cell` 실행 시, 디버그 모드 진입
  - 코드 라인 단위로 로컬/글로벌 변수의 상태를 확인 가능
  - 중단점에 위치한 코드는 실행되지 않는다 -> 중단점 바로 위 코드까지 실행
- VS Code 상단 디버거 인터페이스 (중단점 설정 후 디버그 셀 실행 시 팝업)
  - Continue
  - Step Over : 현재 중단점이 걸려 있는 줄의 코드를 한 줄 실행하고 바로 다음 줄로 이동합니다.
  - Step Into : 현재 줄에 호출되는 함수가 있을 경우, 해당 함수의 첫 번째 줄로 진입하여 멈춥니다.
  - Step Out : 현재 실행 중인 함수의 나머지 코드를 끝까지 실행하고, 해당 함수를 호출했던 상위 위치로 즉시 복귀합니다.
  - Restart
  - Disconnect : 디버그 모드 해


| 기능 | 단축키 | 핵심 동작 | 함수 호출 라인을 만났을 때의 세부 동작 | 주요 사용 시나리오 |
| :--- | :--- | :--- | :--- | :--- |
| **Step Over** | `F10` | 현재 라인을 한 줄 실행하고 다음 라인으로 이동 | 함수 내부로 들어가지 않고 **함수 전체를 한 번에 실행**한 뒤, 현재 파일의 다음 줄에 멈춤 | • 이미 검증된 내장 함수/유틸리티 함수를 건너뛸 때<br>• 현재 코드 블록의 흐름만 빠르게 추적하고 싶을 때 |
| **Step Into** | `F11` | 현재 라인을 실행하되 호출 함수가 있으면 **내부로 진입** | 호출되는 함수의 **첫 번째 줄로 이동**하여 정지 (함수가 없으면 `Step Over`처럼 다음 줄로 이동) | • 사용자 정의 함수의 매개변수 전달 및 세부 로직을 확인하고 싶을 때<br>• 특정 함수 내부에서 발생하는 버그를 추적할 때 |
| **Step Out** | `Shift + F11` | 현재 함수의 나머지 코드를 모두 실행하고 **상위 호출자로 탈출** | 현재 함수의 남은 구문을 끝까지 실행한 후, **해당 함수를 호출했던 상위 코드 라인으로 즉시 복귀** | • 실수로 `Step Into`로 들어간 긴 함수를 빠르게 빠져나올 때<br>• 함수 반환값 전달 시점의 상태로 즉시 돌아가고 싶을 때 |


### 내장 함수

#### `isinstance()`
- 변수 객체가 특정 자료형(클래스)에 속하는지 검사

#### `all()` & `any()`
- all(iterable) : 모든 요소가 True 인식 -> True 반환
  - 예외: empty list -> True
- any(iterable) : 요소 중 하나라도 True 인식 -> True 반환

```python
# 공백도 문자이므로
all([True, 1, " "]) # True
all([True, 1, ""]) # False
all([False, False]) # False
all([]) # 예외: empty list -> True

any([False, 1, 0]) # True
any([]) # False
```

#### `len()`, `sum()`, `round()`
- `len()` : 문자열 -> 문자열의 길이, 리스트, 튜플에 담긴 요소 갯수
- `sum(iterable)` : 합계(요소는 숫자로만 구성)
- `round()` : 반올림
  - `math.ceil(..)` : 올림
  - `math.floor(..)` : 버림

```python
num = 10.56

print(round(num)) # 정수 반올림
print(round(num, 1)) # 소숫점 1자리까지 반올림

import math
print(math.ceil(num)) # 11
print(math.floor(num)) # 10
```


#### `mean()`, `median()`
- statistics 모듈에 포함된 표준 라이브러리
- mean(iterable) : 평균
- median(iterable) : 중앙값


```python
import statistics

nums = [10, 15, 20, 8, 7, 100000]
print(f"평균: {statistics.mean(nums)}, 중앙값: {statistics.median(nums)}")
```

## 메모할 만한 것들


## Daily Quest 퀴즈 오답
Q. 파이썬에서 변수 이름 앞에 언더바 두 개(__)를 붙였을 때, 외부에서 원래 이름으로 직접 접근하지 못하도록 이름이 자동으로 바뀌는 규칙을 무엇이라고 하나요?

- 내가 쓴 답: 모르겠습니다

### 오답 해설
언더바 두 개(__)를 붙이면 내부적으로 '_클래스명__변수명' 형태로 이름이 변형되는 이름 변형 규칙(Name Mangling)이 적용되어, 원래 이름으로는 외부에서 직접 접근할 수 없게 됩니다.

- 틀린 이유: 기억이 나지 않음


## 진단 퀴즈 오답
- 없음


