# 26.09.01
KANT 21일차, ML


## 오늘 느낀 점
- 현대 LLM의 뼈대인 트랜스포머의 핵심 구조를 알 수 있어서 좋았고, 이전까지의 언어 모델들의 한계를 어떤 방식으로 극복했는지의 흐름이 정리되는 느낌이었다.
- 수업을 듣고 한 번에 명확하게 이해하기는 어려웠지만, 튜터님께 질문하고 조원과 함께 스터디를 진행하면서 애매한 부분들을 확인하고 구체화 할 수 있었다.
- Q. 역전파 시, 멀티헤드 어텐션 웨이트도 임베딩에 영향을 미치는지? 트랜스포머 모델의 경우 임베딩은 학습하지 않고 사전학습된 임베딩을 사용만 하는지?
  - (튜터님) 당연히 역전파 과정에서 멀티헤드 어텐션 레이어에서의 결과가 임베딩 레이어로 반영된다. 
- Q. 트랜스포머의 멀티헤드 어텐션 레이어 다음에 소프트맥스 함수를 적용하는 이유에 대해서
  - (튜터님) 근본적으로는 '멀티헤드 어텐션' 레이어의 목적이 토큰(단어)의 상대적인 중요도를 표현하는 것이며, 소프트맥스를 사용하여 일관된 기준인 확률로 만들어주는 것. 일관되지 않은(소프트맥스 처리되지 않은) 기준값을 그대로 적용할 경우, 중요하지 않은 값이 너무 커지는 등 문제가 발생할 수 있음. 라이브러리에 따라 트랜스포머 구조에서 소프트맥스를 빼고 그냥 내보내는 경우도 있기는 함




## 오늘 배운 것

```
### [ 학습목표 ]
- 인코더와 디코더의 역할 분담 및 고정 크기 컨텍스트 벡터의 한계를 설명할 수 있다.
- PyTorch를 사용하여 기본적인 인코더-디코더 구조의 시퀀스 변환 모델을 구축할 수 있다.
- Query-Key-Value 관계 및 어텐션 가중치를 통한 동적 컨텍스트 벡터 생성 원리를 설명할 수 있다.
- 기존 Seq2Seq 모델에 어텐션 레이어를 추가하여 긴 시퀀스의 정보 손실 문제를 해결할 수 있다.
- 순환 신경망 기반의 Seq2Seq 모델이 가진 구조적 한계와 이를 보완하기 위해 등장한 어텐션 메커니즘의 장단점을 설명할 수 있다.
- 위치 인코딩(Positional Encoding)의 수학적 원리를 이해하고, 단계별 계산 과정을 명확히 설명할 수 있다.
- 셀프 어텐션의 3요소인 $Q, K, V$를 활용해 어텐션 점수 행렬을 구하는 단계를 정량적으로 추적할 수 있다.
- 멀티헤드 어텐션(Multi-Head Attention)의 세부 연산 흐름을 파악하고 이를 논리적으로 구성할 수 있다.
- 인코더·디코더 결합 및 단독 변형 패러다임(BERT, GPT 등)의 실전 쓰임새를 구분하여, 각 구조별 전체 파라미터 크기를 계산할 수 있다.
```

## 목차

- [26.09.01](#260901)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [시퀀스 투 시퀀스 (Sequence 2 Sequence)](#시퀀스-투-시퀀스-sequence-2-sequence)
      - [인코더](#인코더)
      - [디코더](#디코더)
      - [구조적 한계: 고정 크기 컨텍스트](#구조적-한계-고정-크기-컨텍스트)
    - [Attention](#attention)
    - [TRANSFORMER](#transformer)
      - [임베딩과 위치 인코딩](#임베딩과-위치-인코딩)
      - [위치 인코딩에 사인(Sine)/코사인(Cosine) 함수를 사용하는 이유](#위치-인코딩에-사인sine코사인cosine-함수를-사용하는-이유)
      - [잔차연결 후 정규화](#잔차연결-후-정규화)
      - [크로스 어텐션](#크로스-어텐션)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


### 시퀀스 투 시퀀스 (Sequence 2 Sequence)
- 구글이 번역하려고 만들었던 모델
- 임의의 길이를 가진 입력 시퀀스를 다른 길이를 가진 출력 시퀀스로 변환하는 대표적인 딥러닝 아키텍쳐


#### 인코더
- 인코더: 입력 문장을 읽고 맥락 요약
- 정보 유실(Long-Term Dependency 등)을 방지하기 위해 LSTM 레이어를 활용
- 단기 기억을 담당하는 은닉 상태($h_t$)와 장기 기억을 담당하는 셀 상태($c_t$)를 함께 업데이트


```math
(h_t, c_t) = \text{LSTM}(x_t, (h_{t-1}, c_{t-1}))
```
- $h_t$**(은닉 상태)**: 현재 시점($t$)의 단어를 읽었을 때 활성화된 단기 정보입니다.
- $c_t$ **(셀 상태):** 처음부터 현재 시점까지 읽은 내용 중 장기적으로 유지해야 할 정보입니다.
- $x_t$**:** 현재 시점에 입력된 단어 벡터입니다.
- **$(h_{t-1}, c_{t-1})$**: 직전 시점까지 누적된 인코더의 기억 상태입니다.


```math
\text{컨텍스트 벡터(Context Vector): } C = (h_T, c_T)
```
- $C$ (컨텍스트 벡터): 입력 문장의 마지막 단어 시점($T$)에서 도출된 최종 은닉 상태와 셀 상태의 세트입니다. 입력 문장의 전체 맥락이 이 벡터에 압축됩니다.


#### 디코더
- 디코더: 요약된 정보를 바탕으로 타깃 문장 생성
- 인코더가 전달한 최종 컨텍스트 벡터 $C$를 초기 은닉 상태와 셀 상태로 설정한 후, 번역할 언어의 단어를 한 단계씩 예측하여 생성

```math
(s_t, d_t) = \text{LSTM}(y_{t-1}, (s_{t-1}, d_{t-1}))
```
- $s_t, d_t$: 디코더의 현재 시점 은닉 상태와 셀 상태입니다. (초기 상태 $(s_0, d_0) = C$)
- $y_{t-1}$: 디코더가 직전 시점에 입력받은 단어입니다. 최초 기동 시에는 문장의 시작을 뜻하는 특수 토큰인 `<sos>`(Start of Sequence)를 입력으로 주입합니다.

[**교사 강요(Teacher Forcing)**]
- 학습 초기 디코더가 잘못된 단어를 예측하더라도, 다음 시점의 입력으로 디코더의 예측값 대신 실제 정답(Ground Truth) 단어를 강제로 주입하는 기법
- 이 방식을 통해 모델이 잘못된 예측 경로로 빠지는 것을 방지하고 학습 속도를 안정적으로 끌어올릴 수 있습니다

#### 구조적 한계: 고정 크기 컨텍스트
- 병목 현상
  - 입력 문장이 아무리 길어도 컨텍스트 벡터의 크기는 항상 고정
  - 문장이 길어질수록 많은 정보를 담아야 하므로 앞부분에 입력된 단어 정보가 점차 유실되는 한계
- 순차적 처리 (Sequential Processing)
  - 구조상 앞 단어 처리가 끝나야 다음 단어 처리 가능
  - GPU 병렬 연산을 사용할 수 없어 대용량 학습에 치명적인 속도 저하


### Attention
* 모든 은닉 정보 보관, 디코더가 언제든지 참고가능 (hidden state)
* 중요한 정보를 탐색해서 추가적인 Attention weight
  * 디코더가 단어를 생성할 때마다 인코더의 단어들을 다시 확인하며, 현재 예측하려는 단어와 관련성이 높은 성분에 더 높은 가중치 부여
* Q, K, V 벡터
  * Query : 무엇을 찾을 것인가?
    * (현재 시점에서 정보를 찾고자 하는 주체 (디코더의 현재 상태 벡터))
  * Key : 어떻게 찾을 것인가? (이름표)
    * 비교 대상이 되는 정보의 이름표 (인코더의 모든 단어 상태 벡터)
  * Value : Key로 찾은 의미 정보
    * 이름표와 연결된 실제 의미 정보 (인코더의 모든 단어 맥락 벡터)
* Q, K, V 벡터는 학습을 통해 업데이트되는 "가중치 행렬"
  * nn.Linear(~)


| **연산 단계** | **수학적 공식** | **의미 및 해석** |
| --- | --- | --- |
| **1단계: 유사도 측정** | $\text{Score}(Q, K) = Q \cdot K^T$ | 디코더의 현재 상태($Q$)와 인코더의 각 단어 이름표($K$)를 내적하여 단어 간의 유사도 점수를 계산합니다. |
| **2단계: 가중치 분배** | $\text{Attention Weights} = \text{Softmax}(\text{Score})$ | 유사도 점수에 소프트맥스 함수를 적용하여, 총합이 1(100%)이 되는 확률 분포 형태의 가중치로 변환합니다. |
| **3단계: 가중합 계산** | $\text{Context Vector} = \sum (\text{Attention Weights} \times V)$ | 계산된 가중치를 각 Value($V$) 벡터에 곱한 후 모두 더하여, 현재 시점에 필요한 최종 요약 벡터를 생성합니다. |


### TRANSFORMER

#### 임베딩과 위치 인코딩

- 전체 병렬 연산 (한번에)
- 하지만 모든 단어가 동시에 연산 레이어에 쏟아지다 보니, 모델 입장에서는 어떤 단어가 몇 번째 자리에 왔는지에 대한 선후 관계(순서 감각)를 전혀 분간하지 못합니다.
- 위치 인코딩이 없다면 "I love you"와 "You love me"라는 완전히 다른 두 문장의 어텐션 스코어 판이 토큰 위치에 상관없이 동일하게 계산되는 치명적인 부작용

![](https://teamsparta.notion.site/image/attachment%3A5726888d-f71a-4bc6-b750-d365339526d5%3Aimage.png?table=block&id=3ae2dc3e-f514-814e-ad5f-d5a83cdeb89d&spaceId=83c75a39-3aba-4ba4-a792-7aefe4b07895&width=740&userId=&cache=v2&imgBuildSrc=requestProxiedImageUrl)


단어의 문장 내 절대 위치에 따라 변하는 벡터를 생성하여 기존 임베딩에 더하는 과정 -> 필수적

```math
PE_{(pos, 2i)} = \sin\left(\frac{pos}{10000^{2i/d_{\text{model}}}}\right)
```

```math
PE_{(pos, 2i+1)} = \cos\left(\frac{pos}{10000^{2i/d_{\text{model}}}}\right)
```

![](https://teamsparta.notion.site/image/attachment%3A5e94dbdd-a007-443d-b489-fcb9cd4e66f3%3A%E1%84%89%E1%85%B3%E1%84%8F%E1%85%B3%E1%84%85%E1%85%B5%E1%86%AB%E1%84%89%E1%85%A3%E1%86%BA_2026-06-29_%E1%84%8B%E1%85%A9%E1%84%92%E1%85%AE_12.30.51.png?table=block&id=3ae2dc3e-f514-81f5-8f85-fb48015406fd&spaceId=83c75a39-3aba-4ba4-a792-7aefe4b07895&width=1330&userId=&cache=v2&imgBuildSrc=requestProxiedImageUrl)

포지셔널 정보가 sin/cos로 추가되긴 하지만, 임베딩 값과 섞이는 형태.
- 어쨌든 위치가 다르면 동일 토큰도 다른 임베딩 값을 갖는다.
- 수많은 데이터를 학습하는 과정에서, 인공지능이 sin/cos로 발생하는 포지셔널 값의 패턴을 이해한다.(다음 항목에서 자세히)

#### 위치 인코딩에 사인(Sine)/코사인(Cosine) 함수를 사용하는 이유
- **상대적 위치(Relative Position) 학습의 용이성**
  - 삼각함수의 덧셈 정리에 의해, 기준 위치에서 특정 거리($k$)만큼 떨어진 단어의 위치 벡터는 현재 위치 벡터의 선형 변환(회전 행렬)으로 깔끔하게 표현됩니다. 덕분에 모델이 'A 단어와 B 단어가 몇 칸 떨어져 있는지'와 같은 **단어 간의 상대적 거리 패턴을 쉽게 학습**할 수 있습니다.
- **시퀀스 길이의 확장성 (Generalization)**
  - 단어의 위치를 1, 2, 3 같은 정수로 부여하면 문장이 길어질수록 값이 지나치게 커져 학습이 불안정해집니다. 반대로 문장 길이에 맞춰 0~1 사이로 정규화하면, 문장 길이에 따라 단어 간 위치 값의 간격이 매번 달라지는 문제가 생깁니다. 삼각함수는 주기를 가지며 항상 -1과 1 사이의 값을 반환하므로, **학습 데이터에서 보지 못했던 훨씬 더 긴 문장이 입력되어도 안정적으로 대응**할 수 있습니다.
- **모든 위치에 대한 고유한 벡터 생성**
  - 다양한 파장(Frequency)을 가진 사인과 코사인 함수를 교차로 사용하면, 마치 시계의 초침, 분침, 시침이 조합되어 고유한 시간을 나타내듯 각 위치마다 **절대 겹치지 않는 고유(Unique)한 위치 벡터**를 만들어낼 수 있습니다.


#### 잔차연결 후 정규화
- 잔차연결
  - 이전 레이어에서 출력한 값을 가져와서 현재 출력값에 더해준다
  - 멀티헤드 어텐션 레이어를 거친 뒤 값이 매우 작아지는 문제 -> 잔차를 더해줌으로써 값을 키운다
- 정규화: 잔차연결로 인해 발생하는 불안정성을 해소하기 위해 정규화를 진행한다


#### 크로스 어텐션
- 이 블록의 $Q$(파란색 박스)는 아래층 마스크드 어텐션 출력값을 받습니다. 
- 반면 $K$(빨간색 박스), $V$(노란색 박스)는 상단 라인 "인코더의 출력"에서 다이렉트로 끌어옵니다.
- 즉, 번역할 다음 단어를 고민할 때($Q$), 원문 전체 맥락을 요약한 이정표($K, V$)를 참고하여 답을 찾는 과정입니다.

```python
class TransformerDecoderLayer(nn.Module):
    def __init__(self, d_model, num_heads, d_ff, dropout=0.1):
        super().__init__()
        self.masked_self_attn = MultiHeadAttention(d_model, num_heads, dropout)
        self.cross_attn = MultiHeadAttention(d_model, num_heads, dropout)
        self.feed_forward = PositionwiseFeedForward(d_model, d_ff, dropout)
        
        self.norm1 = nn.LayerNorm(d_model)
        self.norm2 = nn.LayerNorm(d_model)
        self.norm3 = nn.LayerNorm(d_model)
        self.dropout = nn.Dropout(dropout)

    def forward(self, x, enc_output, src_mask=None, tgt_mask=None):
        # 마스크드 셀프 어텐션 (미래 단어 컨닝 방지용 tgt_mask 적용)
        residual = x
        self_attn_out = self.masked_self_attn(query=x, key=x, value=x, mask=tgt_mask)
        x = self.norm1(residual + self.dropout(self_attn_out))
        
        # 크로스 어텐션 (Query: 디코더 변환값 x / Key, Value: 인코더 최상단 기둥의 enc_output)
        residual = x
        cross_attn_out = self.cross_attn(query=x, key=enc_output, value=enc_output, mask=src_mask)
        x = self.norm2(residual + self.dropout(cross_attn_out))
        
        # 피드 포워드 네트워크
        residual = x
        ff_out = self.feed_forward(x)
        x = self.norm3(residual + self.dropout(ff_out))
        return x
```


## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
Q. 트랜스포머 디코더에서 학습 시 미래의 정답 단어를 미리 참조하지 못하도록 차단하는 기법의 이름은?
- [내가 선택한 답] A. 패딩 마스킹(Padding Masking)
- [정답] 인과적 마스킹(Causal Masking)
- [해설]
  - 인과적 마스킹은 하삼각행렬 형태의 마스크를 적용해 미래 시점의 단어 정보를 -무한대로 만들어 소프트맥스 확률을 0으로 만들어 컨닝을 방지합니다. 패딩 마스킹은 의미 없는 패딩 토큰을 무시하는 용도이고, 위치 인코딩과 층 정규화는 각각 순서 정보 부여와 값 안정화를 위한 별개의 기법입니다.
- [틀린 이유]
  - 용어를 헷갈림

## 진단 퀴즈 오답
- 없음


