# 26.08.06
KANT 4일차, python

## 오늘 배운 것

### 파이썬 가상환경

가상환경의 필요성
- dependency hell
- 전역에 단일 환경으로 개발할 경우 환경 충돌 발생


기존 파이썬 가상환경 -> 라이브러리 간 의존성 문제
- `python -m venv 환경이름` 
- anaconda

의존성을 자동 구축해주는 파이썬 가상환경
- 모던 툴체인 **uv**
    - rust 기반으로 빠르다
    - 프로젝트 단위 관리
    - 의존성 문제 해결
    - requirements.txt 필요 없음

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

$env:Path = "C:\Users\dance\.local\bin;$env:Path"
```

버전 표기 방식 -> ex. 0.12.2
- 0 = major version
    - 하위 버전과 호환 X
- 12 = minor version
    - 하위 버전과 호환 O
    - 기능 추가, 작은 변경
- 2 = patch version
    - 오류 수정, 코드 개선


### UV 구축
1. `uv init --python 3.13`
   - 현재 경로(폴더)에 프로젝트 생성 + 가상환경 (프로젝트/가상환경 명칭 = 현재 폴더명)
   - `uv init ex_venv` -> 프로젝트 이름 명시할 경우, 현재 경로에 해당 네임의 폴더 생성
   - python 버전 명시 -> 파이썬 3.13버전 이상을 요구하는 프로젝트
2. `uv add pandas`
   - 버전 미지정 시, 현재 프로젝트 환경과 호환되는 가장 최신 버전을 자동 탐색하여 설치한다.
3. `uv sync`
   - 가상환경 생성 및 버전 부여
4. `./venv/Scripts/activate`
   - 현재 프로젝트의 가상환경 활성화
   - A가상환경 활성화 상태에서 바로 B가상환경 activate 하면 변경 (굳이 비활성화 하지 않아도 ok)
5. `./venv/Scripts/deactivate.bat`
   - 현재 프로젝트의 가상환경 비활성화
6. 가상환경 삭제 & 재설치
   - `.venv` 폴더 삭제
   - `pyproject.toml` 정보가 있다면, `pyproject.toml`이 있는 경로에서 `uv sync` 명령어로 재설치 가능
7. 가상환경 의존성 변경
   - `uv python pin 3.12`(현재 프로젝트가 사용할 타깃 파이썬 버전 고정) 이후 `uv sync` -> `.python-version` 문서 업데이트 + 파이썬 명시 버전으로 재설치
   - 단, `pyproject.toml`에 명시된 파이썬 의존성 조건에 부합하여야 하며, 상충될 경우 `pyproject.toml` 내용을 수정해야 한다.
   - 다른 라이브러리와 의존성 충돌이 발생할 경우, 조건에 만족하도록 새롭게 가상환경을 재생성한다.
8. `uv remove pandas` -> 라이브러리 삭제
9. `uv run main.py` -> uv 명령어로 파이썬 파일 실행
10. `uv tree` -> 현재 프로젝트의 라이브러리 의존성 계층 구조 시각화

### 주피터 노트북
- ctrl + enter -> 셀 실행
- shift+enter 현재 셀 실행하고 셀 추가 생성
- 셀 바깥 영역을 선택한 상태에서:
  - a -> 위에 셀 생성
  - b -> 아래 셀 생성
  - x -> 현재 셀 삭제
  - m -> 마크다운 셀로 변경
  - y -> 코드 셀로 변경


### Python 변수 네이밍
(다른 언어도 대체로 비슷하다!)

변수 네이밍 제한사항
- 사용가능 : 영문(대소문자), 숫자, $, _
- 숫자로 시작 X (ex. 1st)
- 공백 포함 X
- 파이썬 예약어 X (ex. if, for, True, ...)

변수 네이밍 권장사항
- 의미있는 단어
- 여러 단어로 구성할 경우 `_`(언더바)로 구분

### Python 자료형
자료 = 연산에 사용되는 값

자료형 종류
- 숫자형 (정수형 int / 실수형 float)
- 문자형 ('' or "")
- 논리형 (True or False)


## 메모할 만한 것들
- 컴파일 언어 vs 인터프리터 언어
  - 컴파일 언어 -> 실행 전 컴파일 필요, 실행속도 빠르다
  - 인터프리터 언어 -> 컴파일 필요X(즉시실행), 실행속도 느리다
- vscode에서 python jupyter notebook 사용하기
  - extention - python, jupyter 맨위 익스텐션 설치
  - uv 환경에 ipykernel 설치 (uv add ipykernel)
  - vscode kernel -> uv 환경 선택 및 활성화

## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
git rebase를 수행하는 도중에 충돌이 발생했습니다. 파일을 에디터로 열어 충돌 마커를 제거하고 원하는 내용으로 수정한 후 git add <충돌_파일>까지 완료했습니다. 이어서 rebase 과정을 계속 진행하기 위해 실행해야 할 명령어는 무엇입니까?

- A. git commit -m "resolve conflict"
- B. git rebase --skip
- C. git rebase --abort
- D. git rebase --continue

정답: D
* rebase 진행 중 충돌이 발생해 이를 수동으로 해결하고 git add를 마쳤다면, 다음 커밋 재적용을 이어서 진행하기 위해 git rebase --continue를 입력해야 합니다.
* --skip은 해당 커밋을 완전히 건너뛸 때 사용하며, --abort는 rebase 작업 전체를 취소하고 이전 상태로 되돌릴 때 사용합니다.


## 복습 노트 
git rebase를 수행하는 도중에 충돌이 발생했습니다. 파일을 에디터로 열어 충돌 마커를 제거하고 원하는 내용으로 수정한 후 git add <충돌_파일>까지 완료했습니다. 이어서 rebase 과정을 계속 진행하기 위해 실행해야 할 명령어는 무엇입니까?

1. git commit -m "resolve conflict"
2. git rebase --skip
3. git rebase --abort
4. git rebase --continue

내가 고른 답: 1
정답: 4

* rebase 진행 중 충돌이 발생해 이를 수동으로 해결하고 git add를 마쳤다면, 다음 커밋 재적용을 이어서 진행하기 위해 git rebase --continue를 입력해야 합니다. --skip은 해당 커밋을 완전히 건너뛸 때 사용하며, --abort는 rebase 작업 전체를 취소하고 이전 상태로 되돌릴 때 사용합니다.

----------------
