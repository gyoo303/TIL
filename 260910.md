# 26.09.10
KANT 28일차, Database & AI


## 오늘 느낀 점
- 데이터베이스의 동시성 문제를 해결하기 위한 비관적 락과 낙관적 락을 설명해 주셔서 재미있었다.
- Supabase의 메서드 체이닝의 실제 구현 방법(return self)이 재미있었다.
- Supabase 실습이 힘들지만 재미있었다.


## 오늘 배운 것

```
### [ 학습목표 ]

```

## 목차

- [26.09.10](#260910)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [supabase](#supabase)
      - [메서드 체이닝](#메서드-체이닝)
      - [supabase RLS (Row Level Security)](#supabase-rls-row-level-security)
      - [회원(`auth.users`) - 회원상세 테이블 \[1:1 연결\]](#회원authusers---회원상세-테이블-11-연결)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

1. 비관적 락 (pessimistic lock)
   1. `SELECT ... FOR update`
   2. 수정이 완료되면 잠금 해제
   3. tradeoff
      1. 성능 저하 가능성 O / 강력한 잠금방식
2. 낙관적 락 (optimistic lock) - table versioning
   1. [user1] 조회->version1
   2. [user1] 수정->version2
   3. [user2] 조회->version1
   4. [user2] 수정->version1 != version2
      1. 예외발생 (프로그램 단의 처리)
   5. tradeoff
      1. DB 성능저하 적다
      2. 부하가 몰리면 처리가 느려질 수는 있다
3. 원자적 조건부 갱신
   1. ex. 위키 페이지 작성 - 상태관리 "작성중..."(못쓴다) "작성 가능"(쓸수있다)
4. 외부 분산락 (redis / redisson?)
   1. 콘서트티켓 예매 선점여부 확인 -> 외부서버가 DB에 접근하기 전에 분산락 서버에 대해 "가능?" 물어보고 접근한다


### supabase

#### 메서드 체이닝
-선언적 방식

```python
class SupabaseEx:
    def __init__(self):
        self.settings = []

    def table(self, table):
        self.settings.append(table)
        return self

    def select(self, fields):
        self.settings.append(fields)

        return self # 생성된 동일 객체 

    def eq(self, name, value):
        self.settings.append(f"{name}={value}")
        return self

    def execute(self):
        print(self.settings)
        print("실행...")
        return "처리된 결과..."

sbe = SupabaseEx()

response = ( 
    sbe.table(...)
    .select(...)
    .eq(...)
    .execute()
)
```

#### supabase RLS (Row Level Security)

- CRUD
- 회원의 권한을 체크 (DB)
  - SELECT, INSERT, UPDATE, DELETE

```sql
-- documents 테이블에 RLS 활성화
alter table documents enable row level security;

-- 본인 행만 SELECT 허용하는 정책
create policy "본인 문서만 조회"
on documents
for select
using (auth.uid() = user_id);

-- 본인 데이터에 대해 모든 작업(SELECT, INSERT, UPDATE, DELETE) 허용
create policy "본인 문서 전체 관리"
on documents
for all
to authenticated
using (auth.uid() = user_id) -- 있는 데이터에 대한 통제(조회,수정,삭제)
with check (auth.uid() = user_id); -- 추가, 수정, 삭제 시 권한 체크
```


작업별로 개별 정책 분리하기
```sql
-- 생성 (INSERT): 저장할 데이터의 user_id가 본인인지 확인
create policy "본인 문서 생성"
on documents for insert to authenticated
with check (auth.uid() = user_id);

-- 조회 (SELECT): 기존 데이터의 user_id가 본인인지 확인
create policy "본인 문서 조회"
on documents for select to authenticated
using (auth.uid() = user_id);

-- 수정 (UPDATE): 수정할 대상과 수정 후 데이터 모두 본인인지 확인
create policy "본인 문서 수정"
on documents for update to authenticated
using (auth.uid() = user_id)
with check (auth.uid() = user_id);

-- 삭제 (DELETE): 삭제할 대상이 본인 데이터인지 확인
create policy "본인 문서 삭제"
on documents for delete to authenticated
using (auth.uid() = user_id);
```


#### 회원(`auth.users`) - 회원상세 테이블 [1:1 연결]
회원 테이블 auth.users
- id uuid, email, password
  
회원상세 테이블 user_details
- id (PK,FK)
- type SELLER,BUYER DEFAULT
- cellphone
- zipcode
- address
- address_sub
- created_at
- modified_at
- deleted_at

상품 테이블 products
- id uuid PK
- name text
- description text
- price int
- seller_id uuid FK
- created_at
- modified_at
- deleted_at

주문 orders
- id uuid pk
- order_no text unq
  - "2026091011322536"
  - 연-월-일-시-분-초-milliseconds
- order_name text 주문자명
- order_email text
- order_phone text
- receiver_name text
- receiver_phone text
- zipcode text
- address text
- address_sub text
- delivery_memo text
- total_price int
- order_status text
  - READY 주문접수전 (default)
  - ORDER 주문접수 완료
  - IN_CASH 입금확인
  - PREPARE 상품준비중
  - ON_DELIVERY 배송중
  - DONE_DELIVERY 배송완료


주문상품 order_items
- id uuid pk
- order_no text fk
- product_id uuid fk
- item_name text
- item_price int
- quantity int
- created_at
- modified_at
- deleted_at
- user_id fk

![erd](./src/img/erd.jpg)


## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
Q. Supabase Client에서 select 요청이 성공했을 때, 실제 조회된 데이터가 담기는 응답 객체의 속성 이름은 무엇인가요?
- `response.data`
- Python SDK의 select 요청 결과는 응답 객체로 반환되며, 실제 데이터는 response.data에 리스트 형태로 담깁니다. 데이터가 없을 경우 response.data는 빈 리스트가 되며 이는 오류가 아니라 정상 응답입니다.

## 진단 퀴즈 오답
- 없음
