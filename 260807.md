# 26.08.07
KANT 5일차, python


## 오늘 느낀 점
- 연산자 우선순위에서 비교연산자(`>,<,=,!=` 등)와 논리연산자(`and, or` 등)가 헷갈리기 쉬울 것 같다. 기호부터 처리한다고 외워보자.
- 문자열 형식화에서 f-string이 가장 직관적이고 쓰기 편해 보이지만, 레거시 코드에서는 1, 2세대도 많이 사용되었을 테니 가볍게 외워 두자.


## 오늘 배운 것

```
### [ 학습목표 ]
- 정수, 실수, 문자열, 불리언 자료형을 분류하고 식별할 수 있다.
- 명시적 형변환을 적용하여 데이터 타입을 자유롭게 변환할 수 있다.
- 산술, 비교, 논리 연산자의 종류를 분류하고 연산 우선순위를 나열할 수 있다.
- 단락 평가의 특성을 이해하고 복합 조건식을 구성할 수 있다.
- % 연산자 및 str.format() 방식의 문법적 한계와 비효율성을 인지할 수 있다.
- 현대 파이썬의 표준인 f-string의 도입 배경과 장점을 설명할 수 있다.
- f-string의 정렬 패딩 소수점 제어 및 디버깅 전용 조절자를 활용할 수 있다.
```


### Python 자료형 (Primitive Data Types)
- 자료: 연산에 필요한 데이터
- 자료형: 값의 유형(데이터의 종류)
  - 숫자형: 정수형(int, 10) / 실수형(float, 1.5)
  - 논리형: 참(True), 거짓(False)
  - 문자형: 문자, 문장, '', " ", 감싸서 선언(한줄) / """ ... """ (여러줄)
  - 값 없음: None

### 주석(comment)
- 코드 상에 설명을 추가할 때 사용, 인터프리터가 해석하지 않는다.
- `#` 한줄 주석
- `""" ... """` 여러줄 주석

```python
# 한줄주석
"""
여러
줄 
주석
"""
```

### 명시적 형변환(Explicit Type Conversion)
* 연산은 같은 자료형끼리 가능하다.
```python
num = "100"
print(type(num)) # string

# 형변환 함수
# int() 정수변환
# float() 실수변환
# bool() True/False 변환
# str() 문자형변환

print(int(num) + 200) # 300
print("현재 시간은 " + str(10) + "시") # 현재 시간은 10시
```

### 묵시적 형변환(Implicit Type Conversion)

```python
num1 = 100 # int
num2 = 3.123 # float

# float 타입과의 연산을 위해 num1 -> float으로 자동 형변환 후 연산된다.
# 단, num1의 원본이 float 타입(100.0)으로 바뀌는 것이 아니다.
num3 = num1 + num2 # float
```

### 형변환 시 주의해야 할 예외와 에러(ValueError)
```python
time = "10시"
if time.isdigit():
    print(int(time) + 1)
```

### 거짓으로 인식하는 값
0, "", [], None
```python
message = "1"
if message:
    print("있음")
```


### 항과 연산자
- 항: 연산에 사용되는 값, 1개 -> 단항, 2개 이상 -> 다항
- 연산자: 연산에 사용되는 기호(+, =, ...)


### 대입 연산자, 산술 연산자
```python
# 대입 연산자(=)
# 연산 우선순위가 가장 낮다. 가장 마지막에 연산된다.
x = 10
x = 10 + 20 # 1. 더하기연산 -> 2. 대입연산

# 산술 연산자(+, -, *, /, //나눈몫, %나머지, **제곱)
# 연산 우선순위: (**, *, /, //, %) > +, -, ...
# 연산 우선순위 강제적용 : (..) 소괄호 씌우기
```

```python
num1 = 10
num2 = 3
print(num1//num2, num1 % num2) # 3, 1
```

```python
num1 = 3
print(num1**3) # 27
```

### 비교연산자
- `>, <, >=, <=` 
- `==` : 같은지 비교한다
- `!=` : 다른지 비교한다

연산의 결과 -> 무조건 True or False
- 판별이 필요한 구문에서 사용(조건문, 반복문)

```python
print(10 > 5)
```

동등성 vs. 동일성
- 동등성: 동일한 가치를 비교
  - (`==`)
- 동일성: 동일한 대상인지 비교
  - (`is` - 주소값`id` 비교)

```python
num1 = 1000
num2 = 1000

# 동등성 비교
num1 == num2 # True, 동등하다.

# 동일성 비교
id(num1) == id(num2) # False, 동일하지 않다.
num1 is num2 # False, 동일하지 않다.
```


### 논리 연산자 (and, or, not)
`and`
- 각 항이 모두 True -> True
- 하나라도 False -> False

`or`
- 각 항의 하나라도 True -> True
- 모두 False일 때만 -> False

`not`
- True 조건 -> False 조건으로 반전
- False 조건 -> True 조건으로 반전


```python
# and
num1 = 20
result1 = num1 > 10
result2 = num1 < 100

print(result1 and result2) # True

print(not(result1 and result2)) # False

fruit1 = "사과"
fruit2 = "배"

result1 = fruit1 == "사과"
result2 = fruit2 == "오렌지"

print(result1, result2) # (True, False)
print(result1 or result2) # True
```

### 복합 대입 연산자
대입 연산자(=) 및 다른 연산자(주로 산술 연산자 + - * / // % **) 결합
```python
num = 1
num = num + 1
num += 1 # 단항연산, num = num + 1
print(num) # 3

num *= 2 # 6
num **= 2 # 36
```

### 부호 반전 연산자(-)
* 음수 -> 양수
* 양수 -> 음수

```python
num = 100
num2 = -num # -100
num3 = -num2 # 100
```

### 단락 회로 평가 (Short-Circuit Evaluation)
```python
result = True or 10**2 # True or True
# 첫 항의 결과가 이미 True -> 결과 확정 -> 다음 연산 진행X

result = False or 10**2 # 첫 항의 결과가 False -> 결과 미확정 -> 다음 연산 진행

result = False and 10**2
# 첫 항의 결과가 False -> 결과확정 -> 다음 연산 진행X

result = False or 10**2
# 첫 항의 결과가 False -> 결과 미확정 -> 다음 연산 진행
```

### 연산자 우선순위
1. `(...)` 소괄호
2. `*,**,/,//,%` 산술 연산자
3. `+,-` 산술 연산자
4. 비교 연산자
5. 논리 연산자
6. `=` 대입 연산자


### 문자열 형식화
#### 1세대: `%` 연산자 방식

- `%s` 문자열
  - `%{숫자}s` -> 숫자 자리수만큼 최소 공간을 할당하고 문자열 배치(우측정렬), 남는 공간은 공백
- `%d` 정수
  - `%{숫자}d` -> 숫자 자리수만큼 최소 공간을 할당하고 숫자 배치(우측정렬), 남는 공간은 공백
  - `%{대체숫자}{숫자}d}` -> 남는 공백을 대체숫자로 교체
- `%f` 실수
  - `%.2f` 소수점 2자리까지 끊기
  - `%.10.2f` 정수부 8자리, 소수점 2자리 끊기, 우측정렬

```python
message = "나의 이름은 %s이고, 나이는 %d입니다."
print(message % ("철수", 20))

# 고급 옵션
print("너비 지정 (5칸 우측 정렬): [%5s]" % "heh") # [  heh]
print("너비 지정 (5칸 우측 정렬): [%5d]" % 123) # [  123]
print("너비 지정 (5칸 우측 정렬): [%05d]" % 123) # [00123]
print("소수점 지정 (소수점 둘째 자리): %.2f" % 3.14159) # 3.14
print("소수점 지정 (소수점 둘째 자리): %10.2f" % 3.14159) #       3.14
```

#### 2세대: `str.format()` 메서드 방식
- 순서 지정 교체: `{0} {1} {2}`
  - 항상 `0`부터 시작해야 함
- 단어 지정 교체: `{단어} {단어}`

```python
# 순서 지정 교체
message = "내 이름은 {0}이고, 나이는 {1}입니다"
print(message.format("철수", 20))

message = "내 이름은 {1}이고, 나이는 {2}입니다"
print(message.format("철수", 20))

# 단어 지정 교체
message = "내 이름은 {name}이고, 나이는 {age}입니다."
print(message.format(name="철수", age=20))

# 순서 지정 교체 + 단어 지정 교체
# 단, format 작성 시 '순서'에 해당하는 값부터 먼저 입력해야 한다.
message = "내 이름은 {name}이고, 나이는 {0}입니다."
print(message.format(20, name="철수")) # OK
print(message.format(name="철수", 20)) # ERROR
```


#### 3세대: f-string 방식
- 가장 직관적이고 간편한 형태

```python
# f-string 기본
name = "영희"
age = 20

message = f"내 이름은 {name}이고, 나이는 {age}입니다."

print(message)
```
```python
# f-string 고급
price = 10000

# 숫자 자릿수 콤마 자동으로 넣기
print(f"가격: {price:,}") # 가격: 10,000

# 콤마 찍고 10칸 우측정렬하기
print(f"가격: {price:>10,}원") # 가격:     10,000원

# 콤마 찍고 10칸 좌측정렬하기
print(f"가격: {price:<10,}원") # 가격: 10,000    원

# 디버깅 조절자 (=)
print(f"{price=}") # price=10000
```


## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음



----------------
