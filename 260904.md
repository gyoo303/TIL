# 26.09.04
KANT 24일차, ML


## 오늘 느낀 점
- 데이터베이스에서의 정규화가 좀 애매했는데, 튜터님이 한 번 더 다뤄주셔서 어느정도 머리에 들어온 것 같다.
- N:M 관계 유형에서 junction table을 삽입하는 형태는 꽤 많이 사용하는 패턴이 될 것 같다. 기억해 두자.
- DDL은 어제 오늘 실습을 하면서 많이 익숙해진 느낌이다. 프로그래밍 언어에 비해 직관적이고 단순해서 원하는 작업을 실행하는 것까지는 수월한데, 결국 중요한 것은 전체 데이터베이스 구조 설계인 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
- PK/FK 개념과 제약 규칙을 설명할 수 있다
- 참조 무결성의 의미를 이해한다
- 정규화(1NF~3NF)의 개념과 목적을 설명할 수 있다
- 1:1 / 1:N / N:M 관계 유형을 구분하고 예시를 들 수 있다
- ERD 기호를 읽을 수 있다
- DDL과 DML의 차이를 구분할 수 있다
- CREATE TABLE 문법으로 테이블을 생성할 수 있다
- INSERT INTO 문법으로 데이터를 입력할 수 있다
- 여러 행을 한 번에 입력할 수 있다
```

## 목차

- [26.09.04](#260904)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [정보의 관계성](#정보의-관계성)
      - [\[N-\>M 관계\] 글(N) \<-\> 태그(M)](#n-m-관계-글n---태그m)
      - [N:N 관계 (다대다) 테이블 구성](#nn-관계-다대다-테이블-구성)
    - [DML (데이터 조작어)](#dml-데이터-조작어)
  - [메모할 만한 것들](#메모할-만한-것들)
    - [1정규형 (1NF)조건: 하나의 칸에 여러 값](#1정규형-1nf조건-하나의-칸에-여러-값)
    - [2정규형 (2NF)조건: 2개의 기본키](#2정규형-2nf조건-2개의-기본키)
    - [3정규형 (3NF)조건: 일반 컬럼에서 종속관계 발생](#3정규형-3nf조건-일반-컬럼에서-종속관계-발생)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


PostgreSQL 따옴표
- 작은따옴표 ' ' : 문자열(dtype) 리터럴
- 큰 따옴표 " " : 구분식별자 -> 컬럼, 테이블명
  - *특수문자가 포함된 경우
  - *대소문자 섞어쓰는 경우
  - *예약어

DDL -> 데이터 삭제시 복구 불가 (ROLLBACK 불가)
1. CREATE 생성
   1. CREATE [TABLE, INDEX, TYPE ... ]
2. DROP 삭제
   1. DROP [TABLE, INDEX, TYPE ... ]
3. TRUNCATE TABLE 테이블명;
   1. 테이블의 데이터를 비운다 (초기화)
4. ALTER 수정
   1. 컬럼 추가: ALTER TABLE {table_name} ADD COLUMN {column_name} {data_type} [제약조건(optional)];
   2. 컬럼명 변경: ALTER TABLE {table_name} RENAME COLUMN {column_name} TO {new_column_name}
   3. 컬럼 삭제: ALTER TABLE {table_name} DROP COLUMN {column_name};
   4. 컬럼 자료형 변경:
      1. ALTER TABLE {table_name} ALTER COLUMN {column_name} TYPE {data_type};
      2. (데이터가 존재하고 형변환이 필요한 경우) ALTER TABLE {table_name} ALTER COLUMN {column_name} TYPE {data_type} USING {column_name::data_type}
   5. ALTER TABLE {table_name} ALTER COLUMN {column_name}
      1. SET [제약조건명];
      2. SET DEFAULT 기본값;
   6. ALTER TABLE {table_name}
      1. ADD CONSTRAINT [제약조건 구분이름]
         1.  PRIMARY KEY(column_name);
         2.  UNIQUE(column_name);
         3.  CHECK(조건식);
         4.  FOREIGN KEY (column_name) REFERENCES {column_name};
       2. DROP CONSTRAINT [제약조건 구분이름];

### 정보의 관계성
- 회원 -> 주문 1:N 관계
  - (N은 0개 이상) - 필수X
  - (N은 1개 이상) - 필수
- 회원 -> 회원상세 1:1 관계

#### [N->M 관계] 글(N) <-> 태그(M)
- 게시글 -> 태그
  - 게시글1 - #쇼핑 #여름 #맛집
  - 게시글2 - #맛집 #여름
  - 게시글3 - #여름 #스포츠 #등산
- 태그 -> 게시글
  - #여름 - 게시글1, 게시글2, 게시글3
  - #맛집 - 게시글1, 게시글2

#### N:N 관계 (다대다) 테이블 구성
- 게시글 테이블 (게시글번호PK, 제목, 내용, ...)
- 게시글-태그 KEY 테이블 (게시글번호PK, 태그명PK)
- 태그 테이블 (태그명PK, 태그설명)


### DML (데이터 조작어)

1. INSERT - 추가
   1. INSERT INTO {table_name} ({column_name1}, {column_name2}, ...) VALUES ({value1}, {value2}, ...)
2. UPDATE - 수정
   1. UPDATE {table_name} SET {column_name1} = {data}, {column_name2} = {data}, ... [WHERE 조건식]
   2. 조건식 비교연산(>,<,=) / 논리연산(AND, OR, NOT)
3. DELETE - 삭제
   1. DELETE FROM {table_name} [WHERE 조건식]


## 메모할 만한 것들

데이터베이스 제약조건 보충
1. 개체 무결성 제약조건
2. 도메인 무결성 제약조건
3. 참조 무결성 제약조건


데이터베이스 정규화 보충
- 목적 1. 중복 방지
- 목적 2. 이상현상 방지 
- -> 3단계 단계별 접근한다.
  - 2형, 3형은 테이블 분리를 해야 한다는 것이 논리이지만, 언제나 좋은 것은 아니다 (반정규화) 

### 1정규형 (1NF)조건: 하나의 칸에 여러 값
- 테이블의 모든 속성(컬럼) 값이 원자 값(Atomic Value)이어야 합니다
- 설명: 하나의 칸에 여러 개의 값이 들어 있거나, 반복되는 그룹(예: 전화번호1, 전화번호2)이 있으면 안 됩니다.
- 해결 방법: 값을 하나씩만 가지도록 행을 늘리거나 테이블을 쪼갭니다.

### 2정규형 (2NF)조건: 2개의 기본키
- 제1정규형을 만족하고, 기본키가 아닌 모든 속성이 기본키에 완전 함수 종속되어야 합니다.
- 설명: 기본키가 2개 이상의 컬럼(복합키)으로 이뤄진 경우, 복합키의 일부만으로도 다른 컬럼이 결정되는 부분 함수 종속이 없어야 합니다.
- 해결 방법: 부분 함수 종속을 유발하는 컬럼들을 별도의 테이블로 분리합니다.

회원번호|회원명|과목코드
---|---|---
1|김철수|A0
2|이영희|B1

과목코드|과목명
---|---
A0|파이썬
B1|데이터베이스


### 3정규형 (3NF)조건: 일반 컬럼에서 종속관계 발생
- 제2정규형을 만족하고, 기본키가 아닌 일반 속성들 간의 이행적 함수 종속(Transitive Dependency)이 없어야 합니다.
- 설명: A -> B이고 B -> C일 때, A -> C가 성립하는 이행 종속이 존재하면 안 됩니다. 
  - 즉, 기본키가 아닌 일반 열이 다른 일반 열을 결정하면 안 됩니다.
- 해결 방법: 종속 관계에 있는 일반 속성들끼리 새로운 테이블로 분리합니다.



## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


