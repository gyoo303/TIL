# 26.09.14
KANT 30일차, 프론트엔드 / LLM


## 오늘 느낀 점
- 네이버 웹사이트의 구조를 직접 보며 HTML 구조를 이해할 수 있어서 재미있었다.
- LLM을 잘 활용하기 위해서 텍스트 데이터를 처리하는 정규표현식이 매우 유용해 보여서, 특강이 기대된다. 또한, 다양한 Message 클래스를 활용하여 LLM에게 의도를 주입하는 방법론을 앞으로 계속 배워나가면 재미있을 것 같다.
- LLM 민감 정보 유출 방지를 위해서 마스킹하는 기법은 실제 프로젝트 때에 반드시 적용해야 할 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
Frontend (HTML)
- 웹 표준의 개념을 이해하고 HTML 필수 구성 요소의 역할을 설명할 수 있다.
- 웹 요소의 두 가지 배치 방식(블록, 인라인)의 차이점을 설명할 수 있다.
- 제목 태그와 본문 서식 태그를 활용하여 구조적 위계가 명확한 웹 문서를 마크업할 수 있다.
- 하이퍼링크와 멀티미디어 태그의 삽입 방식 및 필수 속성을 설명할 수 있다.
- 다양한 폼(Form) 서식 요소를 활용하여 사용자 데이터 입력 화면을 배치할 수 있다.

LLM
- 환각, 지식 컷오프 등 대규모 언어 모델(LLM)의 주요 한계 4가지를 구분하고 설명할 수 있다.
- 민감 정보 유출 방지를 위한 데이터 마스킹 함수와 환각 완화 프롬프트를 코드로 구현할 수 있다.
- Streamlit 환경에서 위험 요소를 사전에 확인하고 안전하게 AI를 호출하는 웹 인터페이스를 완성할 수 있다.
- 시스템(System), 사용자(User), AI(Assistant) 메시지의 역할을 명확히 구분할 수 있다.
- 애플리케이션 목적에 맞는 다중 메시지 구조를 설계하고 코드로 구현할 수 있다.
```

## 목차

- [26.09.14](#260914)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [\[FrontEnd\_HTML\] Web Document](#frontend_html-web-document)
      - [HTML 웹표준 (web standard)](#html-웹표준-web-standard)
      - [하이퍼링크 - 페이지 이동](#하이퍼링크---페이지-이동)
      - [양식(Form) 태그](#양식form-태그)
    - [\[LLM\] LLM의 한계와 위험 요소](#llm-llm의-한계와-위험-요소)
      - [LLM의 4대 한계와 발생 원인](#llm의-4대-한계와-발생-원인)
      - [파이썬 실습: 환각 방지 프롬프트 및 temperature 제어](#파이썬-실습-환각-방지-프롬프트-및-temperature-제어)
    - [\[LLM\] 민감 정보 유출 위험과 데이터 전처리](#llm-민감-정보-유출-위험과-데이터-전처리)
      - [민감 정보 -\> 정규표현식 기반 마스킹](#민감-정보---정규표현식-기반-마스킹)
    - [\[LLM\] System-User-Assistant 메시지 구조](#llm-system-user-assistant-메시지-구조)
      - [역할별 메시지 템플릿](#역할별-메시지-템플릿)
    - [\[LLM\] 대화 이력(memory) 구조 설계하기](#llm-대화-이력memory-구조-설계하기)
      - [Assistant 메시지 기반 문맥 유지](#assistant-메시지-기반-문맥-유지)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


### [FrontEnd_HTML] Web Document
- 뼈대: HTML (HyperText Markup Language)
  - HyperText - 페이지이동
  - Markup - 태그로 구성 `<tag> ... </tag>`
    - 태그 내부의 content 없으면 단일 태그 사용 `<tag/>` or `<tag>` ex. `<meta ...>`
  - Language - 언어
  - 파일 확장자 `.html`, `.htm`
- 스타일링: CSS (Cascading Style Sheet)
  - Cascading - 상위 요소의 스타일이 하위 요소에 전달되는 방식
  - 파일 확장자 `.css`
- 동적요소: JavaScript

#### HTML 웹표준 (web standard)
- 어떤 브라우저로 사용해도 동일화면, 동일기능
- 기본 구성
  - `<!DOCTYPE html>` HTML 버전 태그 (일반적으로 HTML 5)
  - `<html>` HTML 문서 시작 태그
    - `<head> ... </head>` => 문서 정보(title, meta, ...), 문서 자원(CSS, JS)에 대한 정의
    - `<body> ... </body>` => 문서의 출력 부분
  - `<html>` HTML 문서 종료 태그
- 태그 (문서와 관련성이 높다)
  - 제목 태그 (Heading)
  - `<h1> ~ <h6>`
  - `<h1>` 주제목
  - `<h2>` 하위제목
  - ...
- 본문 서식 태그
  - `<p>` 문단 태그
  - `<b>` `<strong>` 텍스트 두껍게
  - `<i>` 기울임
  - `<u>` 밑줄
  - `<del>` `<strike>` 취소선
  - `<br>` 줄 개행
- 주석
  - 설명
  - 출력 배제
  - `<-- -->`
- **문단 태그(Block 요소)**
  - 줄 개행
  - p, div, h1~h6, form, ...
  - 영역을 가진다 (너비, 높이 설정 가능)
- **글 태그(Inline 요소)**
  - 줄 개행 X
  - i, b, strong, u, a, span
  - 영역을 갖지 않는다 (너비, 높이 X)


#### 하이퍼링크 - 페이지 이동
- `<a>` : 하이퍼링크 태그
  - 태그 기능 수행하기 위한 추가 정보: 속성(attribute)
  - `href` : 이동할 경로
    - 절대경로 - 전체URL / 전체 파일경로
    - 상대경로 - `./`, `../`
  - `target` : 출력할 창의 위치 
    - `target = _self`(기본값) -> 현재 창
    - `target = _blank` -> 새로운 창
    - `target = _parent` -> 부모 창
      - `<iframe>` - 자식 창
        - `src` : 창에 출력할 경로(URL)
        - `name` : 창의 이름


#### 양식(Form) 태그
- input : 사용자 입력을 위한 태그
  - action : 제출 처리 URL
  - type : 입력 유형
    - text (기본값)
    - password 비밀번호
    - radio 여러개 중 1개 선택
    - checkbox 여러개 중 여러개 선택
    - submit 제출 (reset-입력항목 초기화, button-버튼)



### [LLM] LLM의 한계와 위험 요소

#### LLM의 4대 한계와 발생 원인
1. 환각 (hallucination)
   1. LLM은 분류 모델이다 (토큰에 대한 분류)
2. 지식 컷오프 (knowledge cutoff)
   1. 학습한 범위까지만 알고, 학습하지 않은 최신 뉴스는 모른다
3. 비결정성 (non-determinism)
   1. 같은 질문에 대해서도 다른 답변이 나올 수 있다
4. 편향 (bias)


#### 파이썬 실습: 환각 방지 프롬프트 및 temperature 제어
- top-k, top-p, temperature
- top-k : 가장 높은 확률의 k개 토큰을 선택, 선택된 토큰을 응답으로 사용한다
  - ex. top-k = 100 -> 가장 높은 확률인 토큰 100개 중 선택
- top-p : p 확률 이상의 토큰에서 선택
- top-p + top-k
- temperature : 확률 분포를 조정 (0에 가까울 수록 정확한 답변, 1에 가까울 수록 창의적인 답변)
  - 0에 가깝게 설정: 도구선택, 형식의 고정


```python
from langchain.core.prompts import ChatPromptTemplate
from langchain_ollama import ChatOllama
from langchain.chat_models import init_chat_model
from langchain_core.output_parsers import StrOutputParser

prompt_template = ChatPromptTemplate.from_messages([
    ("system", "당신은 객관적 사실만을 전달하는 정보 검증관입니다. 제공된 지식 범위 밖의 내용이거나 사실 확인이 불가능하다면 '해당 정보는 확인할 수 없습니다'라고만 명확히 답변하세요. 절대 거짓 정보를 지어내지 마세요."),
    ("user", "{user_question}")
])

# 환경변수, 설정파일 등으로 설정하면 테스트-로컬모델, 배포-상용모델 교체가 용이하다.
model = init_chat_model(
    model_provider = 'ollama',
    model = 'mistral',
    temperature = 0
)

parser = StrOutputParser()

chain = prompt_template | model | parser

question = "2028년 한국에서 개최된 달 탐사 기념 축제의 주요 행사를 알려줘"
response = chain.invoke({"user_question": question})

response
```

### [LLM] 민감 정보 유출 위험과 데이터 전처리

#### 민감 정보 -> 정규표현식 기반 마스킹

```python
import re

# 정규 표현식을 활용하여 전화번호와 이메일을 익명화하는 함수를 정의합니다.
def mask_personal_information(text: str) -> str:
    # 전화번호 형식 (숫자 3자리-대쉬-4자리-대쉬-4자리) 감지 패턴
    phone_pattern = re.compile(r'\d{3}-\d{4}-\d{4}')

    # 이메일 형식 (예: user@example.com) 감지 패턴
    # email_pattern = re.compile(r'')

    # 일치하는 패턴을 대체 문구로 치환
    masked = phone_pattern.sub("[전화번호 보호]", text)
    # masked = email_pattern.sub("[이메일 보호]", masked)
    return masked

user_input = "연락처는 010-0000-0000이고, 메일은 yonggyo00@kakao.com 입니다."
masked_input = mask_personal_information(user_input)

print(masked_input)
```

### [LLM] System-User-Assistant 메시지 구조

#### 역할별 메시지 템플릿
- System: 모델의 역할(페르소나), 상황, 과업, 규칙 정의, 최우선순위
  - SystemMessage 클래스
- User: 실제 질의 응답에 대한 사용자 질문, 처리할 데이터의 전달, 가변적, 프롬프트 템플릿
  - HumanMessage 클래스
- Assistant: AI가 출력한 응답
  - AIMessage 클래스

User - Assistant
- 대화의 맥락 정보를 쌍으로 전달
- 1쌍 = 1턴 / 여러 쌍 = 멀티 턴
- BaseMessage의 하위 클래스
  - HumanMessage, SystemMessage, AIMessage, ToolMessage


```python
from langchain_core.prompts import ChatPromptTemplate

prompt_template = ChatPromptTemplate.from_messages([
    ("system", "당신은 재치있게 컴퓨터 개념을 설명하는 유쾌한 유치원 교사입니다."),
    ("user", "다음 개념을 설명하세요: {text}")
])

chain2 = prompt_template | model | parser
response2 = chain2.invoke({"text": "함수"})

print(response2)
```


### [LLM] 대화 이력(memory) 구조 설계하기

#### Assistant 메시지 기반 문맥 유지

```python
from langchain.chat_models import init_chat_model
from langchain_core.output_parsers import StrOutputParser
from langchain_core.messages import SystemMessage, HumanMessage, AIMessage, BaseMessage

model = init_chat_model(
    model_provider = "ollama",
    model = "mistral"
)

parser = StrOutputParser()
chain = model | parser

messages: list[BaseMessage] = [
    HumanMessage("내 이름은 김철수야. 만나서 반가워"),
    AIMessage("네. 반갑습니다, 철수님. 무엇을 도와드릴까요?"),
    HumanMessage("내 이름이 뭐라고 했지?")
]

response = chain.invoke(messages)
print(response)
```






## Daily Quest 퀴즈 오답
- 

## 진단 퀴즈 오답
- 없음
