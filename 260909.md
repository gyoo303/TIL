# 26.09.09
KANT 27일차, ML


## 오늘 느낀 점
- 데이터베이스 수정 작업을 안전하게 처리할 수 있는 TCL을 배웠다. 쿼리 조회가 아닌 중요한 변경이 필요할 때 반드시 사용하자.
- Supabase SDK를 통해 반복적인 DB 작업을 자동화하거나, 함수 등 모듈화로 편리하게 만들 수 있어 좋은 것 같다.
- Langchain으로 Ollama 모델을 로컬에서 동작시키고, 입력 템플릿과 파서와의 파이프라인도 구축해 보았다. gemma의 경우 성능이 많이 아쉬웠는데 Ollama는 재미있게 사용해볼 수 있을 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
- TCL의 역할을 설명할 수 있어요.
- BEGIN, COMMIT, ROLLBACK의 차이를 구분할 수 있어요.
- 여러 데이터 변경 작업을 하나의 작업 단위로 묶을 수 있어요.
- 트랜잭션의 ACID 원칙을 설명할 수 있어요.
- 오류 발생 시 ROLLBACK을 적용하여 데이터 정합성을 유지할 수 있어요.
- 일부 작업만 성공했을 때 발생하는 문제를 설명할 수 있어요.

- Supabase의 Database, API, SDK 역할을 구분할 수 있다
- 데이터베이스 테이블이 자동 API로 노출되는 구조를 설명할 수 있다
- SDK를 활용한 데이터 접근 흐름을 이해할 수 있다
- SDK 기반 개발 환경을 구성할 수 있다
- 환경변수를 활용해 연결 정보를 안전하게 관리할 수 있다
- Supabase Client를 초기화하고 연결 상태를 검증할 수 있다
- SDK를 활용해 애플리케이션 코드에서 데이터를 생성할 수 있다
- 입력값과 DB 컬럼 구조를 매핑할 수 있다
- 생성 결과와 오류 응답을 처리할 수 있다
- SDK를 활용해 조건 기반 데이터 조회를 구현할 수 있다
- 사용자별 데이터 조회 흐름을 구성할 수 있다
- 조회 결과를 리스트 형태로 처리할 수 있다
- SDK를 활용해 데이터를 수정하고 삭제할 수 있다
- 조건 기반 update/delete의 위험성과 검증 방법을 이해할 수 있다
- 수정·삭제 후 DB 상태를 검증할 수 있다
- Supabase Authentication의 역할을 설명할 수 있다
- 회원가입, 로그인, 로그아웃의 기본 흐름을 구현할 수 있다
- 인증된 사용자 정보를 데이터와 연결할 수 있다

- LLM 애플리케이션의 기본 입·출력 처리 구조와 동작 흐름을 설명할 수 있다.
- 순수 API 직접 호출 대비 LangChain 도입 이유와 LCEL의 장점을 설명할 수 있다.
- uv와 .env 설정을 완료하고 Jupyter Notebook 환경에서 LLM 모델을 연동할 수 있다.
- 프롬프트 템플릿, 모델 호출, 출력 파서를 조합하여 LCEL 파이프라인 코드를 단계별로 완성할 수 있다.
- AI 성능 개선 및 확장 방식(Prompt, RAG, Fine-tuning, Agent)의 특징과 차이점을 비교할 수 있다.
```

## 목차

- [26.09.09](#260909)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [TCL](#tcl)
    - [트랜잭션 = SQL 실행 단위](#트랜잭션--sql-실행-단위)
    - [Supabase SDK (Software Development Kit)](#supabase-sdk-software-development-kit)
      - [이메일 기반 회원가입 및 로그인](#이메일-기반-회원가입-및-로그인)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### TCL
- 트랜잭션 제어 명령어

### 트랜잭션 = SQL 실행 단위
트랜잭션 4가지 원칙 = ACID
- A (Atomicity) 원자성
  - 모두 성공 OR 모두 실패
- C (Consistency) 일관성
  - 규칙(제약조건) 강제
- I (Isolation) 격리성
  - 동시 트랜잭션이 서로 간섭되어서는 안된다
- D (Durability) 지속성
  - COMMIT -> DB에 영구 반영

트랜잭션
- BEGIN; -> 트랜잭션 시작
- (TRANSACTION 내용)
- COMMIT; -> 트랜잭션 종료 (DB 영구반영)


```sql
-- Atomicity

begin;

insert into users (email) values ('user03@test.org');

insert into users (id, email) values ('1', 'user04@test.org');

commit;
```

```sql
-- Rollback

begin;
delete from users where email = 'user02@test.org';
select * from users;
rollback; -- commit 이전 시점이기 때문에 롤백 가능
select * from users;
```

### Supabase SDK (Software Development Kit)

```python
# .env 파일의 값을 환경변수로 불러오기
import os
from supabase import create_client

# .env 환경변수 파일로부터 값 불러오기
url = os.getenv("SUPABASE_URL")
key = os.getenv("SUPABASE_PUBLISHABLE_KEY")

# Client 초기화
supabase = create_client(url, key)

# INSERT
response = supabase.table("users").insert({"email":"user03@test.org"}).execute()

# documents에 추가
response = supabase.table("documents").insert(
    {
        "title":"첫 번째 문서",
        "content":"본문 내용",
        "user_id":"df96c481-1161-4bf3-a283-d98ca96f54d7"
    }
).execute()

# SELECT
response = supabase.table("users").select("*").execute()

# SELECT + DATA FORMATTING
response = supabase.table("users").select("*").ilike("email", "user03%").execute()
print(response)
id, email, created_at = response.data[0].values()
print(f"ID: {id}, Email: {email}, Created At: {created_at}")

# 레코드 1개만
response = supabase.table("users").select("*").ilike("email", "user03%").single().execute()

# 레코드 0~1개, 0개이면 None 반환
response = supabase.table("users").select("*").ilike("email", "user03%").maybe_single().execute()

# 레코드 0~1개, 0개이면 None 반환
response = (supabase.table("users")
    .select("*")
    .ilike("email", "user03%")
    .limit(1) # 1개만 조회한다
    .offset(0) # 0번 인덱스에서 1개를 조회한다
    .maybe_single() # 0~1개 조회, 0개이면 None 반환
    .execute()
)

# SELECT + ORDER BY
response = (
    supabase.table("users")
        .select("*")
        .order("email", desc=True)
        .execute()
)

# TABLE JOIN
# 기본값 -> LEFT JOIN

response = (
    supabase.table("documents")
    .select("id, title, content, users!left(id, email)")
    .execute()
)

# INNER JOIN
response = (
    supabase.table("documents")
        .select("id, title, content, users!inner(id, email)")
        .ilike("users.email", "user%")
        .execute()
)

# UPDATE
response = (
    supabase.table("documents")
        .update({
            "title":"첫 번째 문서(수정)",
            ##########################,
            ##########################
        })
)

# DELETE
response = (
    supabase.table("users")
        .delete()
        .eq("id", "73b95b8c-0cea-4ac3-9a3a-b6bd893c68c5")
        .execute()
)
```


#### 이메일 기반 회원가입 및 로그인

```python
# 회원가입
# 최초 1회 이메일 인증 옵션 해제해야 정상 작동함
response = sb.auth.sign_up({
    "email":"user01@test.org",
    "password":"password1234",
    "phone":"01010001000"
})

# 로그인
response = sb.auth.sign_in_with_password({
    "email" : "user01@test.org",
    "password" : "password1234"
})

# 현재 로그인된 사용자 확인
user = sb.auth.get_user()
if user:
    print("logedg in", user)
else:
    print("not logged in")

# 로그아웃
sb.auth.sign_out()
```



## 메모할 만한 것들
  
```
irm https://ollama.com/install.ps1 | iex
ollama ls
ollama pull mistral
http://localhost:11434/
```

## Daily Quest 퀴즈 오답

Q. Supabase는 어떤 오픈소스 관계형 데이터베이스를 기반으로 백엔드 서비스를 제공하는가?
- (내가 쓴 답) 기억이 안납니다
- (정답) PostgreSQL
- (해설) Supabase는 PostgreSQL 데이터베이스를 기반으로 인증, 스토리지, 실시간 기능 등을 제공하는 백엔드 서비스 플랫폼입니다. 이 때문에 PostgreSQL의 기본 구조와 SQL 문법을 그대로 활용할 수 있습니다.
- (틀린 이유) 기억 이슈


## 진단 퀴즈 오답
- 없음
