# 26.08.12
KANT 8일차, python


## 오늘 느낀 점
- 함수가 동작할 때 구체적으로 메모리에서 어떤 방식으로 처리되는지 알 수 있어서 좋았다.
- 호출부터 리턴까지, 함수가 동작하는 동안 메모리가 할당되고 해제되는 방식을 학습함으로써, 파이썬의 GC가 얼마나 편리한 시스템인지 알 수 있었다.


## 오늘 배운 것

```
### [ 학습목표 ]
- 코드 재사용 관점에서 함수의 필요성을 설명하고 독립된 기능을 정의할 수 있다.
- 위치 인수, 기본값 인수, 가변 인수를 구분하여 함수 매개변수를 설계할 수 있다.
```

### 목차

- [26.08.12](#260812)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
    - [목차](#목차)
    - [함수의 기본 정의와 작동 방식](#함수의-기본-정의와-작동-방식)
    - [메모리 feat. 파이썬](#메모리-feat-파이썬)
    - [스택(Stack) vs. 큐(Cueue)](#스택stack-vs-큐cueue)
    - [왜 함수 실행영역은 Stack일까?](#왜-함수-실행영역은-stack일까)
    - [위치 인수(Positional Arguments)와 키워드 인수(Keyword Arguments)](#위치-인수positional-arguments와-키워드-인수keyword-arguments)
    - [기본값 매개변수(Default Parameter)](#기본값-매개변수default-parameter)
    - [가변 인수(\*args)와 키워드 가변 인수(\*\*kwargs)](#가변-인수args와-키워드-가변-인수kwargs)
    - [return 문의 역할과 복수 데이터 반환](#return-문의-역할과-복수-데이터-반환)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 함수의 기본 정의와 작동 방식
```
# 정의 방법
def 함수명(매개변수1, 매개변수2, ...): # 입력부분
    # 처리부분
    return 반환값 # 출력부분

# 호출
- 함수명(...) -> 엔터

# 목적
- 코드의 재활용

# return
- 반환값 정의
- 함수의 종료
```

```python
def calc(x):
    y = x * 2 + 1
    return y
```

### 메모리 feat. 파이썬
![stack-heap](https://teamsparta.notion.site/image/attachment%3Adc2c6d81-e12d-4bbe-9d6c-c46b2589d11a%3Aimage.png?table=block&id=3b72dc3e-f514-80fa-8ed4-d5aa9ad62dd8&spaceId=83c75a39-3aba-4ba4-a792-7aefe4b07895&width=1330&userId=&cache=v2&imgBuildSrc=requestProxiedImageUrl)


- 정적 메모리: 한번 기록되면 제어X
  - 파이썬 내장함수, 예약어 등
  - 전역변수
- 동적 메모리: 실행 중 생성과 소멸
  - 힙 영역 메모리 (객체전용)
    - `PyFunctionObject`
      - `PyCodeObject` - 함수 매개변수 정의, 수행코드 정의
      - *함수의 이름은 변수의 이름과 같으며, 마찬가지로 메모리 주소를 할당받는다.
      - 함수의 변수는 작업지시서(명세 객체)를 저장한 힙 영역의 주소를 가리킨다.
  - 스택 영역 메모리 (Call Stack)
    - 함수가 호출되고 실행될 때 할당되어 사용되는 메모리 = 임시메모리
    - 함수 호출 시 `PyFrameObject` 메모리 구조체를 Call Stack에 쌓는다
      - 함수 호출 -> 새 프레임 객체 생성 -> 스택 PUSH
      - 함수 리턴 -> 프레임 객체가 스택으로부터 POP -> 메모리 해제
    - `f_locals` 지역변수(함수내부정의)
      - x = 10, y = 21
      - 함수가 실행될 때만 유효하다
      - 실행 완료(`return`)하면 메모리에서 제거한다.
      - 지역변수(함수내부정의) <-> **전역변수(함수외부정의)

| **구분** | **힙(Heap) 영역** | **콜 스택(Call Stack) 영역** |
| --- | --- | --- |
| **저장 대상** | **함수 정의 객체** (PyCodeObject, PyFunctionObject) | **함수 실행 프레임 객체** (PyFrameObject) |
| **생성 시점** | 모듈 로드 및 def 문 해석 시점 | 함수 호출 시점 (func()) |
| **소멸 시점** | 프로그램 종료 또는 가비지 컬렉션(GC) 시점 | 함수 실행 완료 시 (return) 즉시 POP |
| **특징** | 함수를 여러 번 호출해도 **단 1개의 원본 객체를 재사용** | 호출할 때마다 **새로운 독립 프레임 생성** (재귀 호출 포함) |

**프레임 객체의 주요 구성 요소**
- 프레임 객체는 함수가 실행되는 동안 필요한 모든 내부 정보(지역 변수, 전역 변수, 호출자 정보 등)을 담고 있습니다.
   
| **속성명** | **역할 및 설명** |
| --- | --- |
| f_locals | 현재 함수 내부의 지역 변수(Local Variables) 및 매개변수를 저장하는 딕셔너리 |
| f_globals | 현재 함수가 속한 모듈의 전역 변수(Global Variables)를 참조하는 딕셔너리 |
| f_builtins | 파이썬 내장 함수 및 예외 객체들(print, len 등)이 속한 스코프 딕셔너리 |
| f_back | 현재 프레임을 호출한 이전 프레임(부모 함수/호출자)을 가리키는 포인터 |
| f_code | 함수 바이트코드 및 상수 목록(co_consts), 변수명 목록(co_varnames) 등을 담은 코드 객체 |

```python
result = calc(10)

calc2 = calc # 함수 자체를 통째로 다른 변수에 저장하는 것도 가능
calc2(20) # 동일하게 함수 실행 가능

print(calc is calc2) # True
print(id(calc) == id(calc2)) # True
```

```python
def add(num1, num2): # 매개변수=지역변수
    num3 = 30
    result = num1 + num2 + num3 # 지역변수 result
    return result # 반환 후 스택메모리 제거

# 함수 내부에 정의된 지역변수 'result'에서 전역변수 'result'로 메모리 주소를 이사가는 것.
# 값 자체를 새로운 주소로 넘긴다!
result = add(10, 20) # 전역변수 result

print(result) # 60
```

### 스택(Stack) vs. 큐(Cueue)
![stack vs queue](https://hudi.blog/static/93b9a8dcb59391a52ed4ad442c5a1b62/dbf98/01.png)
- 스택 구조 = 원통
  - 원통에 하나씩 집어넣어서 쌓고, 꺼낸다.
  - LAST IN, FIRST OUT = FIRST IN, LAST OUT
- 큐 구조 = 대기열 (줄서기)
  - FIRST IN, FIRST OUT = LAST IN, LAST OUT


### 왜 함수 실행영역은 Stack일까?

![memory-load-process](https://teamsparta.notion.site/image/attachment%3A00d272fe-b3d4-4057-89c9-3a6f43a54e4d%3Aimage.png?table=block&id=3b72dc3e-f514-8048-9d89-c8dc66941197&spaceId=83c75a39-3aba-4ba4-a792-7aefe4b07895&width=1330&userId=&cache=v2&imgBuildSrc=requestProxiedImageUrl)

- 최종적으로 계산 결과를 바깥으로 내보내려면, 가장 먼저 실행된 함수 호출 스택이 가장 마지막에 종료되어야 한다.
- 최초 호출된 함수 호출 스택의 `f_locals`가 모든 연산이 끝날 때까지 마지막까지 대기한다.


```python
def func(num):
    result = add(10, 20) # add 함수 -> 정적 메모리
    result += num
    return result

# func 호출 -> 실행 시작 -> add 호출
result = func(40)

# 변수 재정의 시, 기존 할당된 메모리주소 날아감
# -> Python Garbage Collector(GC)
func = 3
```


```python
def add(num):
    if num == 999:
        print("종료")
        return # 반환값 없으면 생략 = return None

    result = num ** 2
    return result

print(add(10))
print(add(999))
```

### 위치 인수(Positional Arguments)와 키워드 인수(Keyword Arguments)

- 위치 인수 : 매개변수 순서대로 값 지정
- 키워드 인수 : 매개변수 명과 함께 값 지정, 순서는 지정하지 않음

```python
def add(num1, num2):
    # 변수 추가지정 없이 바로 리턴 -> 메모리 효율적
    return num1 + num2 

add(10, 20) # 위치 인수
add(num1=10, num2=20) # 키워드 인수
add(num2=20, num1=10) # 순서 바꿔도 ok
```
```python
def add(num1, num2, name, mobile):
    return f"{num1 + num2}, {name=}, {mobile=}"

add(10, 20, mobile = "010-1000-1000", name="김영희")

# SyntaxError : 키워드 인수는 뒤에 몰아주고, 위치 인수부터 앞에 와야 한다.
add(mobile = "010-1000-1000", 10, 20, name="김영희")
```

### 기본값 매개변수(Default Parameter)
- 매개변수에 값을 할당하지 않아도 기본적으로 가질 수 있는 값 설정
- 기본값이 있는 매개변수는 기본값 없는 매개변수 다음에 순서대로 정의

```python
def add(num1, num2, name, mobile = "010"):
    return f"{num1 + num2}, {name=}, {mobile=}"

add(10, 20, "김영희") # "30, name='김영희', mobile='010'"
```
```python
def add(num1, num2, mobile, name = None):
    return f"{num1 + num2}, {name=}, {mobile=}"

add(10, 20, "010-1000-1000") # "30, name=None, mobile='010-1000-1000'"
```

### 가변 인수(*args)와 키워드 가변 인수(**kwargs)

**마찬가지로, `*args`는 `**kwargs`보다 앞에 나와야 함.
- `*args` 
  - 가변적인 위치 인수
  - 매개변수 갯수 상관없이 지정 가능
  - 키워드 매개변수로는 사용 불가
- 

```python
def add(num1, num2, num3):
    return num1 + num2 + num3
add(1, 2, 3)

# 들어오는 변수의 갯수가 특정되어 있지 않다면?
def add(*nums):
    print(nums, type(nums))

add(10, 20, 30, 40, 50, 60, 70) # (10, 20, 30, 40, 50, 60, 70), <class 'tuple'>

```

```python
def add(*nums):
    #print(nums, type(nums))
    total = 0
    for num in nums:
        total += num
    return total

print(add(1,2,3,4,5)) # 15
print(add(*range(1,6))) # 15
print(add(*[1,2,3,4,5])) # 15
```

```python
def print_user_info(**info):
    print(info)

print_user_info(name="김철수", age=20) # {'name': '김철수', 'age': 20}

def print_user_info(name, age, **info):
    print(f"name: {name}, age: {age}, {info}")

print_user_info(*['김철수', 20], mobile="010") # name: 김철수, age: 20, {'mobile': '010'}

# 별표 
def print_user_info(name, age, *args, **kwargs):
    print(f"name: {name}, age: {age}, {args}, {kwargs}")


print_user_info("김철수", 20, 553, 212, mobile="010", nickname="ironwater")
print_user_info(*["김철수", 20], *[553, 212], mobile="010", nickname="ironwater")
# name: 김철수, age: 20, (553, 212), {'mobile': '010', 'nickname': 'ironwater'}
```


### return 문의 역할과 복수 데이터 반환

```python
def calc(num1, num2):
    return num1 + num2, num1 * num2, num1 - num2, num1 / num2

calc(10, 2)
```



## 메모할 만한 것들

파이썬은 sys 모듈의 `_getframe()` 함수를 통해 실행 중인 프레임 객체에 직접 접근할 수 있습니다.

```python
import sys

def outer_func():
    x = 10
    
    def inner_func():
        y = 20
        
        # 1. 현재 실행 중인 프레임 객체 가져오기
        frame = sys._getframe()
        
        print(f"1. 현재 실행 함수: {frame.f_code.co_name}")
        print(f"2. 현재 지역 변수(f_locals): {frame.f_locals}")
        print(f"3. 전역 스코프 참조 여부: {'outer_func' in frame.f_globals}")
        
        # 2. f_back을 통해 부모 프레임(outer_func)으로 이동
        parent_frame = frame.f_back
        print(f"4. 부모 함수 이름: {parent_frame.f_code.co_name}")
        print(f"5. 부모 함수의 지역 변수: {parent_frame.f_locals}")

    inner_func()

outer_func()
```

**명명 규칙과 메모리 로드**
- 함수의 이름은 변수와 마찬가지로 숫자로 시작할 수 없으며, 해당 함수가 어떤 기능을 하는지 명확히 알 수 있도록 동사 위주로 명명하는 것이 좋습니다. 
- 파이썬에서는 영문 소문자를 사용하고 **단어와 단어 사이를 밑줄(_)로 연결하는 '스네이크 케이스(Snake Case)' 방식**으로 함수 이름을 작성하는 것이 권장사항입니다. 
- 파이썬 인터프리터는 **def 키워드**를 식별하는 순간 해당 함수 블록을 메모리에 등록해 두었다가, 호출되는 시점에 비로소 실행 제어권을 넘겨 작동시킵니다.


## Daily Quest 퀴즈 오답
다음 코드의 출력 결과로 옳은 것은? def get_average(*scores): if len(scores) == 0: return 0 total = sum(scores) avg = total / len(scores) return avg result = get_average() print(result)

- 내가 선택한 답: B. 오류(ZeroDivisionError)가 발생한다
- 정답: 0
- 해설
  - 인수 없이 호출하면 scores는 빈 튜플이 되어 len(scores)==0 조건이 참이 되므로 정수 0이 그대로 반환됩니다. 이 방어 코드가 없다면 0으로 나누는 ZeroDivisionError가 발생했겠지만, 조건문 덕분에 안전하게 처리됩니다.

틀린 이유
- 아무것도 입력하지 않으면 0 또는 None이 매개변수에 입력되어 오류가 발생할 것으로 생각했지만, 튜플로 받아서 처리하므로 입력값이 없을 때 빈 튜플`(,)`이 되는 것인지 몰랐다.

## 진단 퀴즈 오답
- 없음


