# 26.09.08
KANT 26일차, ML


## 오늘 느낀 점
- 메인 쿼리 작성 중 단계에서의 중간 체크, 필터링, 데이터 유무 확인 작업에 유용하며, JOIN에 비하여 상대적으로 가벼운 목적의 서브쿼리를 배웠다. 파이썬으로 치면 lambda 함수와 조금 비슷한 접근이 아닐까?
- IN/NOT IN에 비하여 EXISTS/NOT EXISTS는 거의 단점이 없어 보인다. 어지간하면 EXISTS를 사용하자.
- Supabase 계정을 만들고, 키를 안전하게 `.env` 파일로 저장하는 세팅을 진행했다. docker + dbeaver에 비해 접근성이 좋고 가벼운 느낌이다.


## 오늘 배운 것

```
### [ 학습목표 ]
- 서브쿼리의 정의와 필요한 상황을 설명할 수 있다
- 위치에 따른 서브쿼리 종류를 구분할 수 있다
- WHERE절 서브쿼리를 작성할 수 있다
- IN / NOT IN + 서브쿼리를 활용할 수 있다
- EXISTS / NOT EXISTS의 동작 원리를 이해한다
- FROM절 서브쿼리(인라인 뷰)를 작성할 수 있다
- SELECT절 서브쿼리(스칼라 서브쿼리)를 작성할 수 있다
- 서브쿼리와 JOIN의 차이를 비교하고 적절한 방법을 선택할 수 있다

- 데이터베이스가 웹·AI 서비스에서 필요한 이유를 설명할 수 있어요.
- 애플리케이션과 DB가 SDK 또는 API를 통해 통신하는 구조를 이해할 수 있어요.
- 연결 정보, 인증, 권한 관리가 필요한 이유를 설명할 수 있어요.
- DDL의 역할을 설명할 수 있어요.
- 요구사항에 맞는 테이블과 컬럼을 정의할 수 있어요.
- CREATE TABLE 문을 활용해 데이터베이스 객체를 생성할 수 있어요.
```

## 목차

- [26.09.08](#260908)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [SQL/DB - 서브쿼리](#sqldb---서브쿼리)
      - [왜 서브쿼리를 쓰나요](#왜-서브쿼리를-쓰나요)
      - [위치에 따른 서브쿼리 종류](#위치에-따른-서브쿼리-종류)
      - [서브쿼리 vs. JOIN](#서브쿼리-vs-join)
      - [WHERE 절 서브쿼리 -\> 동적 조건값 계산](#where-절-서브쿼리---동적-조건값-계산)
      - [SELECT 서브쿼리 -\> 스칼라 서브쿼리](#select-서브쿼리---스칼라-서브쿼리)
      - [FROM 절 서브쿼리](#from-절-서브쿼리)
    - [DB \& AI App](#db--ai-app)
      - [SUPABASE 세팅](#supabase-세팅)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### SQL/DB - 서브쿼리
- SELECT 문(외부쿼리)에 SELECT(내부쿼리)가 포함된 쿼리
- 내부쿼리의 결과를 외부쿼리가 이용한다.

#### 왜 서브쿼리를 쓰나요
- 서브쿼리는 "조건을 먼저 계산한 뒤 적용"하는 방식이라, 코드 읽기가 직관적이고 특정 상황에서는 더 간결해요.
- 어떤 방법이 더 적합한지는 다음 강의에서 비교하면서 배울 거예요.

#### 위치에 따른 서브쿼리 종류
| 위치 | 명칭 | 특징 |
| --- | --- | --- |
| WHERE절 | WHERE절 서브쿼리 | 조건값을 동적으로 계산 |
| FROM절 | 인라인 뷰 (Inline View) | 가상의 임시 테이블처럼 사용 |
| SELECT절 | 스칼라 서브쿼리 (Scalar Subquery) | 단일 값(하나의 열, 하나의 행)을 반환 |


#### 서브쿼리 vs. JOIN
- 기본적으로 가벼우며, 직관적인 로직
- 자유도는 낮은 편 (외부 쿼리 기준 열만 선택가능)
- 단순 조건 필터링, 데이터의 존재 여부 확인 용도

| 비교 항목 | 서브쿼리 | JOIN |
| --- | --- | --- |
| 가독성 | 조건/계산 로직이 명확하게 분리됨 | 여러 테이블 관계를 한눈에 파악 가능 |
| 반환 구조 | 외부 쿼리 기준의 열만 선택 가능 | 여러 테이블의 열을 자유롭게 선택 가능 |
| 성능 | **단순 조건 필터링에 유리** | 대용량 데이터 JOIN에 최적화된 경우 많음 |
| NULL 처리 | **EXISTS 사용 시 안전** | OUTER JOIN으로 NULL 제어 가능 |
| 적합한 상황 | **집계 결과를 조건으로 사용, 존재 여부 확인** | 두 테이블의 열을 함께 조회, 복잡한 관계 표현 |


#### WHERE 절 서브쿼리 -> 동적 조건값 계산

- WHERE절 서브쿼리는 외부 쿼리가 실행되기 전에 먼저 실행되고, 그 결과값을 외부 쿼리의 조건에 전달한다.

```sql
-- WHERE 절에도 사용 가능하다.
-- 값이 1개, 단일행 서브쿼리
select product_id, product_name, list_price
from products
where list_price > (SELECT AVG(list_price) FROM products)


-- 값이 여러개, 다중행 서브쿼리 (IN, NOT IN) -> 서브쿼리로 쓴다
SELECT DISTINCT customer_id FROM orders;

-- 다중행 서브쿼리
-- 주문을 한 기록이 있는 고객 목록
select customer_id, first_name, last_name
from customers
where customer_id in 
(SELECT DISTINCT customer_id FROM orders);


-- `NOT IN` 사용 시 서브쿼리 결과에 NULL이 포함되면 전체 결과가 빈 값으로 나올 수 있어요.
-- 이런 경우 `NOT EXISTS`를 사용하는 것이 더 안전해요.
-- 서브쿼리의 열에 NULL이 없는지 미리 확인하는 습관을 갖는 것이 좋아요.
-- **EXISTS는 NULL로부터 영향 받지 않음.


-- 재고가 등록된 상품 서브쿼리로 조회하기
-- SELECT * from stocks where product_id=

select product_id, product_name 
from products p
where exists (SELECT * from stocks s where s.product_id=p.product_id)

-- "존재하기만 하면 OK"
select product_id, product_name 
from products p
where exists (SELECT 1 from stocks s where s.product_id=p.product_id)

-- WHERE로도 할 수는 있지만
-- IN은 전체 행을 연산하고, EXISTS는 하나만 존재하면 연산을 끝낸다.
select product_id, product_name
from products
where product_id in (select product_id from stocks);
```



#### SELECT 서브쿼리 -> 스칼라 서브쿼리
스칼라 = 단일값. 스칼라 서브쿼리는 반드시 하나의 행, 하나의 열만 반환한다.
- 각 행마다 계산된 값을 새 열로 추가할 때 유용하다.
- 스칼라 서브쿼리를 작성할 때는 항상 단일 값이 반환되도록
  - WHERE 조건이나 집계 함수(AVG, COUNT, SUM 등)를 사용한다.

```sql
-- 스칼라 서브쿼리
select product_id, product_name,
(select sum(list_price) from order_items oi
where oi.product_id = p.product_id) "총 판매금액"
from products p
limit 10;

-- 각 고객의 이름과 함께 전체 평균 주문 금액을 나란히 조회
SELECT
    customer_id,
    name,
    (SELECT AVG(amount) FROM orders) AS avg_order_amount
FROM customers;
```


스칼라 서브쿼리를 활용한 상관 서브쿼리
```sql
-- 끌어다 쓴다!
-- 외부 쿼리의 각 행 정보(o.customer_id)를 서브쿼리 안에서 참조하는 방식
-- -> 상관 서브쿼리(Correlated Subquery)
select
    o.order_id,
    o.customer_id,
    o.amount,
    (
        select count(*)
        from orders o2
        where o2.customer_id = o.customer_id
    ) as total_orders_by_customer
from orders o;
```


#### FROM 절 서브쿼리
인라인 뷰 (가상 테이블) -> 실제 물리 테이블X, 조회 결과를 테이블처럼 활용한다.
- 반드시 AS 별칭을 붙인다.
- view -> 일반적으로 조회 용도
  - 데이터 업데이트 목적으로는 사용하지 않는다.
- HAVING을 쓰면 되는 거 아닌가요? 왜 굳이 인라인 뷰를 써요?
    - `HAVING`으로 해결할 수 있는 경우도 많아요. 하지만 인라인 뷰는 집계 결과를 다시 JOIN하거나, 집계된 값을 기준으로 추가 계산을 해야 할 때 더 유연해요.
    - 예를 들어, 집계 결과를 다른 테이블과 JOIN해야 한다면 인라인 뷰 없이는 구현이 어려워요.

```sql
select total_stock from ( 
	select store_id, product_id, sum(quantity) total_stock
	from stocks
	group by store_id, product_id
);


-- 총 재고가 평균 이상인 store만 조회하기
select store_id, total_stock from ( 
	select store_id, sum(quantity) total_stock
	from stocks
	group by store_id
) store_stock
where total_stock >= (
	select avg(total_stock) from (
		select sum(quantity) total_stock 
		from stocks
		group by store_id
	)
);

-- 총 재고가 평균 이상인 store의 이름까지 포함해서 조회하기
select store_stock.store_id, total_stock, s.store_name from ( 
	select store_id, sum(quantity) total_stock
	from stocks
	group by store_id
) store_stock
	inner join stores s
	on store_stock.store_id = s.store_id
where total_stock >= (
	select avg(total_stock) from (
		select sum(quantity) total_stock 
		from stocks
		group by store_id
	)
);


-- 비효율적인 쿼리 : 꼭 필요할 때만 사용
-- avg값을 위해 products 테이블 전체를 11번 조회한다
select product_id, product_name, list_price,
	(select avg(list_price) 평균가 from products)
from products
limit 10;

```


### DB & AI App

#### SUPABASE 세팅

supabase `.env` 구성파일
- SUPABASE 키값을 정의하는 파일
- `.gitignore`에 `.env*` 추가

환경변수 등록 *시스템 변수 등록
* SUPABASE_URL
* SUPABASE_PUBLISHABLE_KEY
* SUPABASE_ANON_KEY
  * Settings - API Keys - Legacy



## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


