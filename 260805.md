# 26.08.05
KANT 3일차, Git

## 오늘 배운 것
- Git 현재까지 배웠던 명령어 / 프로세스 리뷰
- Git 팀 협업 과제 수행: 원격 저장소 협업하기

## 메모할 만한 것들
- Git 협업 시 발생하기 쉬운 실수 -> main 브랜치 변경사항이 있을 수 있으므로 자주 git pull을 수행해야 함.
- `git pull --rebase` -> 꼭 기억 (브랜치에 변경사항이 추가되었고, 나도 추가할 변경사항이 존재할 때 수행)

## Daily Quest 퀴즈
틀린 문제
- Q. GitHub Flow와 Git Flow를 비교한 설명 중 옳은 것은?
- A. GitHub Flow는 develop, release, hotfix 브랜치를 두어 복잡한 버전 관리를 지원한다

해설
- GitHub Flow는 main과 기능 브랜치만으로 동작하는 단순한 구조로, 소규모 팀이나 빠른 배포 주기에 적합합니다. 반면 Git Flow는 main, develop, feature, release, hotfix 등 복잡한 브랜치 구조를 가지며 중대형 팀의 버전 관리에 적합하고, 대형 팀의 짧은 주기 CI/CD 통합에는 Trunk-based Development가 더 적합합니다.

틀린/헷갈린 이유
- Git Flow / Github Flow 개념/용어 혼동

----------------

### Git 전체 프로세스 오버뷰
1. `git status` - 현재까지의 변경사항을 확인
2. `git init` - 워킹디렉토리 등록
3. `git add` <경로>
    - `git add .` 전체 스테이징
    - `git add -p` 부분 스테이징
    - `git diff --staged` 스테이징 상태의 변경사항을 출력
4. `git commit`
    - 버전을 기록하는 단계
    - 관리를 위해 식별가능한 메시지를 반드시 함께 입력한다.
    - `git commit -a -m "메시지"` 이미 존재하는 파일에 대해 add-commit 한번에 수행한다. (신규 생성된 파일은 불가능)
    - `git commit --amend -m "메시지"` 마지막에 수행된 커밋의 메시지 수정
5. `git log`
    - 버전(커밋) 히스토리 확인 + 커밋ID
    - `--oneline` 한줄씩
    - `--graph` 그래프로
    - `--decorate` 보기좋게
    - `--all` 모든 기록 (되돌린 커밋 내역까지)
    - `-n갯수` 최근 갯수만큼 (ex. -n3)
6. `git hash-object <파일경로>`
    - .git/objects/ 폴더 내부에 파일로 기록된 해시값을 출력한다 (앞 2글자 폴더명 + 나머지 글자 파일명)
7. `git checkout <커밋ID>`
    - 특정 커밋 시점으로 HEAD 위치 변경
    - 실제 로컬 파일의 내용도 변경됨
    - `git checkout -` 직전 커밋 시점으로 돌아가기
    - `git checkout -b <브랜치명>` 브랜치를 생성 후 이동 (*혼동 방지를 위해 브랜치 간 이동에는 `git switch`를 주로 사용)
8. `git branch`
    - git branch 브랜치 조회 (현재 위치한 브랜치는 * 표시)
    - git branch -d(D) 브랜치 삭제 (다른 브랜치에 위치해야 함)
    - git branch -m(M) 변경하고자 하는 브랜치로 이동해서 변경
    - 브랜치 네이밍 컨벤션 예시: `feature/___` `fix/___` `bugfix/___` `hotfix/___` `release/___` `test/___` `chore/___` `docs/___` `dev/___`
9. `git switch <브랜치명>`
    - 브랜치 간 이동
    - `git switch -c <새브랜치이름>` 새로 만들고 이동
    - `git switch -` 직전 브랜치 이동
    - **main 브랜치 생성은 언제? -> `git init` 후 최초 커밋과 함께 main 브랜치가 생성된다.
    - `git branch -d(D) 브랜치명` - 브랜치 삭제(다른 브랜치에서 실행)
    - `git branch -m(M) 브랜치명` - 브랜치 이름 변경
10. `git merge <브랜치명>` 
    - 현재 브랜치A를 타깃 브랜치B와 병합한다.
    - 병합 성공 시, 결과는 현재 브랜치A에 반영된다.
    - 병합 방식/결과는 다음 3가지를 따른다.
      - Merge : 변경사항이 A,B 브랜치에 모두 존재하며, 중복되지 않을 때 신규 커밋의 형태로 병합 수행 
      - Fast-forward : 변경사항이 B에만 존재할 때 커서만 옮기는 방식으로 병합 수행 (수정이 필요없으므로 커밋X)
      - Conflict : 변경사항이 A,B 브랜치에 모두 존재하며, 중복될 때 충돌 발생 -> 작업자 간 소통/협업으로 변경사항 통일 후 git add + commit 수행한다.
11. `git rebase`
    - 두 개의 브랜치에서 진행된 커밋을 재배치한다.
    - [재배치 하고자 하는 브랜치] 위치에서 [붙이려는 브랜치]에 대해서 git rebase를 실행한다.
    - 공유되지 않은 개인 로컬 브랜치에서 사용한다.
    - rebase 충돌 시, 
      - 로컬 파일 조정 후 `git add ...` -> `git rebase --continue` (*신규 커밋으로 실행됨)
      - 작업 건너뛰기 `git rebase --skip`
      - 작업 중단 `git rebase --abort`
    - rebase 성공 후의 merge -> fast-forward으로 수행된다.
12. `git stash`
    - 로컬 변경 후 add 없이 브랜치 변경 시, 오류 발생
    - 변경사항을 일단 임시로 저장해 두고 브랜치 변경을 할 때 -> `git stash`
    - `git stash`는 스테이징(add)되지 않은 로컬 변경을 취소하고 임시보관함에 저장
    - `git stash list` : 임시보관함 확인
    - `git stash pop`
        - 임시 저장된 내용이 원복되고 임시 저장된 내용을 보관함에서 지운다
        - 원복되면 add-commit 해서 처리하면 됨

### 변경사항 되돌리기 / 취소하기
1. `git rm --cached <경로>` -> 스테이징된 파일을 삭제한다.
2. `git restore --staged <경로>` -> 스테이징 취소 (add 취소)
3. `git restore <경로>` -> 워킹디렉토리 변경 취소 (마지막 커밋 상태로 복원)
4. `git reset <커밋ID or HEAD~n>`
    - target으로 돌아간다.
    - `git reset --soft` : 커밋만 취소/ 커밋이력만 삭제/ 로컬소스 변경X
    - `git reset --mixed` : 커밋, 애드 둘다 취소/ 커밋이력만 삭제/ 로컬소스 변경X
    - `git reset --hard` : 커밋, 애드 둘다 취소/ 커밋이력 및 로컬소스(원본파일) 삭제
        - `git reflog`를 통해 커밋ID를 찾아서 `git reset`으로 복구 가능 -> 대신 추가로 `git add` + `git commit` 해줘야 함.
5. `git reflog`
    - commit, checkout, reset 히스토리 출력 + 커밋ID
    - `git reset`으로 삭제한 커밋ID로 다시 돌아갈 수 있다.
6. `git revert HEAD~n`
    - HEAD -> 현재 포인터 위치
    - HEAD~ -> 직전 단계
    - HEAD~n -> n(숫자)만큼 전 단계
    - `git reset`과 달리 **커밋이력을 삭제하지 않고 모두 기록한다**
    - **신규 커밋의 형태로 수행한다**


