# 26.08.13
KANT 9일차, python


## 오늘 느낀 점
- 콜백 함수와 팩토리 함수는 **파이썬에서 함수 기능의 확장성**을 높이는 데 유용하다. 
  - 핵심 기능을 기반으로, 상황에 따라 유연한 기능 확장을 가능하게 한다.
  - 콜백 형태는 각 함수가 객체로 완전 독립되어 있고 더 모듈화된 느낌이 들어서 더 깔끔한 것 같다.
  - 함수 내부에 또 다른 함수를 정의하는 것과의 차이점은 확장성과 재사용성.
  - 콜백 함수가 한번 쓰고 버리는 용도라면 lambda 함수를 사용한다.
- 파이썬 함수는 무조건 콜스택 메모리에 할당되지 않는다.
  - 리턴 시점에서 완전 연산이 되는 경우 콜스택 메모리에 할당
  - 외부? 다른 객체와의 참조/연결고리가 남는 불완전 연산(ex. 팩토리 함수)이 될 경우 힙 메모리에 할당
  - 파이썬 만든 사람은 머리가 엄청 좋은 것 같다.


## 오늘 배운 것

```
### [ 학습목표 ]
- 지역 변수와 전역 변수의 유효 범위(Scope)를 구분하고 은닉 법칙을 설명할 수 있다.
- 익명 함수(lambda) 문법을 익혀 한 줄 형태의 데이터 가공 구문을 작성할 수 있다.
- 파이썬 함수의 일급 객체 특성과 클로저 동작방식을 설명할 수 있다.
- 데코레이터 구문을 작성하여 기존 소스코드를 수정하지 않고 유연하게 기능을 확장할 수 있다.
```

## 목차

- [26.08.13](#260813)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [1. 전역 변수 vs 지역 변수](#1-전역-변수-vs-지역-변수)
    - [2. 파이썬의 변수 검색 규칙: LEGB 스코프](#2-파이썬의-변수-검색-규칙-legb-스코프)
    - [3. global 키워드](#3-global-키워드)
    - [4. 일급 함수 (First-class Function)](#4-일급-함수-first-class-function)
      - [팩토리 함수 `outer(num1)`가 `inner(num1, num2)` 함수를 리턴한다면, 콜스택의 PyFrameObject가 제거된다. **그런데 어떻게 num1이 살아서 inner 함수에서 사용될 수 있을까?**](#팩토리-함수-outernum1가-innernum1-num2-함수를-리턴한다면-콜스택의-pyframeobject가-제거된다-그런데-어떻게-num1이-살아서-inner-함수에서-사용될-수-있을까)
      - [콜백함수 사용 예시 - 원하는 연산을 수행하기 위해 함수에서 다른 함수를 끌어다 쓴다](#콜백함수-사용-예시---원하는-연산을-수행하기-위해-함수에서-다른-함수를-끌어다-쓴다)
      - [클로져(Closure)](#클로져closure)
    - [5. lambda 함수](#5-lambda-함수)
    - [6. lambda와 map(), filter()를 활용한 데이터 일괄 가공](#6-lambda와-map-filter를-활용한-데이터-일괄-가공)
    - [7. 데코레이터](#7-데코레이터)
      - [팩토리얼 연산 시, for 루프와 재귀함수 중 무엇이 빠를까?](#팩토리얼-연산-시-for-루프와-재귀함수-중-무엇이-빠를까)
      - [데코레이션 함수를 콜백 함수 인자 갯수와 상관없이 일반적으로 동작하도록 만드려면?](#데코레이션-함수를-콜백-함수-인자-갯수와-상관없이-일반적으로-동작하도록-만드려면)
  - [메모할 만한 것들](#메모할-만한-것들)
    - [복습노트 - 내 말로 설명해 보기](#복습노트---내-말로-설명해-보기)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 1. 전역 변수 vs 지역 변수

```python
a = 10 # 전역 변수

def func():
    b = 20 # 지역 변수
    print(b)
    print(f"{a=}, {b=}")

func()
```

```python
# 상위 스코프 변수에서 하위 스코프 변수에 접근할 수 없다
# 하위 스코프 변수에서 상위 스코프 변수에 접근할 수 있다

def outer():
    a = 10
    # print(b) # error - undefined

    def inner():
        b = 20
```


### 2. 파이썬의 변수 검색 규칙: LEGB 스코프
접근 우선순위 - L > E > G > B (*동일 변수명이 존재할 때, 스코프에 따른 우선순위)
- L : Local - 지역 변수
- E : Enclosing - 외부 함수 변수
- G : Global - 전역 변수
- B : Built-in - 내장 변수

```python
a = 'Global'
def outer():
    a = 'Enclosing'
    print("outer", a) # outer Enclosing
    def inner():
        a = 'Local'
        print("inner", a) # inner Local
    inner()

outer()
```

### 3. global 키워드
- 스코프와 상관없이, 전역변수로 선언된 변수를 현재 스코프에서 사용하겠다 -> `glboal` 키워드 사용
- 순수함수 vs. 비순수함수
  - 순수함수: 함수 외부 스코프의 변수에 접근하거나 변경하지 않음
  - 비순수함수: 함수 외부 스코프의 변수에 접근하거나 변경함
  - **가급적 global 키워드를 사용하지 않는 순수함수 형태를 사용하는 이유**

```python
a = 'Global'
def outer():
    a = 'Enclosing'
    print("outer", a) # outer Enclosing
    def inner():
        global a # a라는 변수는 전역변수를 찾겠다.
        a = not a # 전역변수 a를 새로 정의한다.
        print("inner", a) # inner False
    inner()

outer()
```


### 4. 일급 함수 (First-class Function)
"함수와 변수를 동일하게 취급한다."
- 함수의 매개변수로 함수를 사용할 수 있다! -> **콜백 함수**
  - 함수가 부르는(call) 함수니까 콜백 함수
- 함수의 반환값으로 함수를 사용할 수 있다! -> **팩토리 함수**, 클로져(Closure)
  - 팩토리 함수: 팩토리 함수의 일부 로직 / 매개변수 정보를 이어받으면서, 새로운 추가 로직 / 매개변수를 갖는 함수를 다양하게 '생산'할 수 있다.

```python
# 콜백 함수
# 함수 자체를 다른 함수의 매개변수로 입력한다.

def callback2():
    print("callback()")

def func(callback):
    print("func()")
    callback()

func(callback2) # func()\ncallback()
```

```python
# 팩토리 함수
# 입력된 매개변수 정보와 함께 새로운 함수 자체를 반환
# -> 새 함수에 새 매개변수를 입력하여 기존 매개변수와 연산 가능하다.
def outer(num1):
    def inner(num2):
        return num1 + num2
    return inner # 함수 자체를 반환

print(outer(5)(10)) # 15 
```

#### 팩토리 함수 `outer(num1)`가 `inner(num1, num2)` 함수를 리턴한다면, 콜스택의 PyFrameObject가 제거된다. **그런데 어떻게 num1이 살아서 inner 함수에서 사용될 수 있을까?**

- `outer` 함수가 리턴하더라도 `num1`에 대한 `inner` 함수와의 참조 관계가 계속해서 존재한다.
  - 이러한 상태를 클로져(Closure) 상태라고 부른다.
- 따라서, `outer`는 콜스택 메모리의 `PyFrameObject`가 아닌 힙 메모리 영역의 `PyCellObject`에 할당된다 (=리턴하더라도 메모리 영역이 사라지지 않는다)



#### 콜백함수 사용 예시 - 원하는 연산을 수행하기 위해 함수에서 다른 함수를 끌어다 쓴다
```python
def change_numbers(*args, callback):
    numbers = list(args)

    for i in range(len(numbs)):
        numbers[i] = callback(numbers[i])

    return numbers

def square(num):
    return num ** 2

change_numbers(*[10,20,30], callback=square) # [100, 400, 900]

```

#### 클로져(Closure)
- 함수 내부에서 변수를 캡쳐하여 유지하는 함수
- 외부 함수의 변수를 기억하고 사용하는 내부 함수

고차 함수(Higher Order Function)
  - 다른 함수를 전달인자(매개변수)로 받거나 함수 실행 결과로 반환하는 함수
  - 대표적인 고차 함수
    - `map`, `filter`, `reduce`

```python
def outer():
    num1 = 10

    def inner():
        num2 = 20
        print(num1+num2)
        
    return inner()

inner = outer() # num1은 inner에 의해서 사용되므로 힙 영역에 생성되어 있음
inner() # 10 + 20
```

```python
def add(num1): # 1차

    def _add(num2): # 2차

        def __add(num3): # 3차
            return num1 + num2 + num3
        
        return __add

    return _add

result1 = add(10)(20)(30)

add10 = add(10)
add10(30)(40)


```


### 5. lambda 함수

- 간단하게 일회용으로 사용하는 경우 한줄의 식으로 간단하게 함수 정의
```
lambda 매개변수, 매개변수, ... : 처리부분
```

```python
def change_numbers(*args, callback):
    numbers = list(args)

    for i in range(len(numbs)):
        numbers[i] = callback(numbers[i])

    return numbers

# square = lambda x: x ** 2

change_numbers(10, 20, 30, callback = lambda x: x ** 2) # [100, 400, 900] 
```


### 6. lambda와 map(), filter()를 활용한 데이터 일괄 가공

- map() : 변환 작업
- filter() : 특정 조건에서 데이터 선별

![map-pydocs](https://i.ytimg.com/vi/QftP0y1VlWs/maxresdefault.jpg)

```python
fruits = ['사과', '오렌지', '망고']

list(map(lambda x: f"**{x}**, fruits"), fruits)
# ['**사과**', '**오렌지**', '**망고**']
```


![filter-py](https://i2tutorials.com/wp-content/media/2020/09/Filter-method-in-Python-Built-in-Function-1.png)

```python
# filter -> 조건식을 lambda 처리부분에 바로 작성한다.
numbers = [*range(1,11)] # 1~10
list(filter(lambda x: x % 2 == 1, numbers)) # [1, 3, 5, 7, 9]

data = [1, 2, None, 4, 5, None, 7]
processed_data = list(filter(None, data)) # [1, 2, 4, 5, 7]
```


### 7. 데코레이터
- 핵심 기능과 공통 기능을 구분하여 구현하는 디자인 패턴

```python
def factorial1(num):
    result = 1
    for i in range(1, num + 1):
        result *= i
    return result

factorial1(10) # 3628800
```

같은 함수 내에서 같은 함수를 반복해서 호출하는 방법 -> 재귀함수(Recursive)
```python
def factorial2(num):
    if num < 1:
        return 1
    return num * factorial2(num - 1)

factorial2(10) # 3628800
```

#### 팩토리얼 연산 시, for 루프와 재귀함수 중 무엇이 빠를까?
- for 루프는 콜스택 1회만 할당되며 메모리 점유가 적고 연산 속도가 빠르다.
- 재귀함수는 호출되는 만큼 콜스택이 쌓이고, 메모리 점유와 함께 연산 속도도 느려진다.

```python

import time

def func_timer(func, param):
    start = time.time() # 공통 기능
    result = func(param) # 핵심 기능
    print(result)
    end = time.time() # 공통 기능
    print(f"걸린시간: {end - start}")
    return end - start

fac1 = func_timer(factorial1, 10)
fac2 = func_timer(factorial2, 10)

print(fac1 > fac2) # True
print(fac1 / fac2) # 16.3 -> factorial1 함수가 16배 빠르다.
```

```python
import time

def func_timer2(func):
    def wrapper(param):
        start = time.time() # 공통기능
        result = func(param) # 핵심기능
        end = time.time() # 공통기능
        print(f"걸린시간: {end-start}")
        return result
    return wrapper

@func_timer2
def factorial2(num):
    if num < 1:
        return 1
    return num * factorial2(num - 1)

factorial2(10)
```

#### 데코레이션 함수를 콜백 함수 인자 갯수와 상관없이 일반적으로 동작하도록 만드려면?
```python
def func_timer2(func):
    def wrapper(*args, **kwargs):
        start = time.time() # 공통기능
        result = func(*args, **kwargs) # 핵심기능
        end = time.time() # 공통기능
        print(f"걸린시간: {end-start}")
        return result
    return wrapper

@func_timer2
def add2(num1, num2):
    return num1 + num2

add2(10, 15)

add2(num1=10, num2=15) # *args만 적용하면 오류발생, **kwargs까지 포함해야 한다.
```



## 메모할 만한 것들

### 복습노트 - 내 말로 설명해 보기
Q. 파이썬에서 변수에 값을 할당할 때 타입이 자동으로 결정되는 이유와, 그 과정에서 기본 자료형들이 어떤 역할을 하는지 설명해 볼 수 있을까요?

A. 파이썬은 변수가 데이터를 저장하는 메모리 주소를 가리키는 역할을 수행하며, 변수 정의 시 자료형이 자동으로 캐스팅된다.

[보충]
- 동적 타이핑의 구체적인 동작 방식이 빠져 있는데, 타입이 '자동으로 캐스팅'되는 것이 아니라 값을 할당하는 순간 그 값의 형태를 보고 파이썬이 자동으로 타입을 결정하는 과정이에요.
- 기본 자료형(int, float, str, bool 등)이 정확히 무슨 역할을 하는지 명시되지 않았는데, 이들은 어떤 종류의 값인지를 구분하는 분류 틀로 작동한다는 점을 함께 설명하면 더 완전해요.
- 파이썬은 동적 타이핑 언어라서 변수를 선언할 때 타입을 미리 정하지 않아요. 대신 값을 할당하는 순간 파이썬이 그 값의 형태를 보고 자동으로 타입을 결정해요.
- 예를 들어 x = 5는 정수니까 int, x = 3.14는 실수니까 float로 인식하는 식이에요. 이때 int, float, str, bool 같은 기본 자료형들은 어떤 종류의 값인지 구분하는 틀 역할을 해요. 만약 서로 다른 타입끼리 연산하거나 처리해야 할 땐 형변환을 사용해서 한쪽의 타입을 다른 쪽에 맞춰 주는 거고요.

## Daily Quest 퀴즈 오답
Q. LEGB 스코프 구조에서 변수 검색 우선순위가 가장 높은 영역과 가장 낮은 영역을 올바르게 짝지은 것은 무엇인가요?
- 내가 선택한 답: `B. 지역 스코프(Local) — 전역 스코프(Global)`
- 정답 : `지역 스코프 - 빌트인`
- 해설 : 파이썬은 변수명을 찾을 때 가장 가까운 현재 함수 내부인 L(Local)을 1순위로 식별하고, 가장 마지막 단계로 파이썬이 기본 탑재한 B(Built-in) 영역을 비교합니다. 나머지 선지들은 순서가 뒤바뀌었거나 중간 단계를 포함하여 최우선/최후순위 짝으로 옳지 않습니다.


Q. def 키워드 없이 lambda 매개변수: 표현식 형태로 작성하며, 이름이 없는 한 줄짜리 함수를 무엇이라고 부르나요?
- 내가 작성한 답 : 임시함수
- 정답 : 익명 함수

## 진단 퀴즈 오답
- 없음


