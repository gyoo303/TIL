# 26.08.21
KANT 14일차, python


## 오늘 느낀 점
- 파이썬 기초과정 진도가 오늘로 끝났다. 공부가 끝났다고 생각하지 않고, 주기적으로 파이썬 기초 교안을 보면서 복습하자.
- AI 리터러시는 대학교 교양 과목을 듣는 느낌으로 재밌었다.

## 오늘 배운 것

```
### [ 학습목표 ]
- asyncio.gather()를 사용하여 다중 비동기 태스크를 동시에 처리하여 실행 성능을 높일 수 있다.
- 비동기 루프 안에서 동기 블로킹 함수를 사용했을 때의 위협 요소를 파악하고 대처할 수 있다.
- AI, 머신러닝(ML), 딥러닝(DL), 생성형 AI, LLM의 개념적 차이와 계층적 구조를 명확히 설명할 수 있다.
- 기술 발전의 흐름 속에서 LLM이 가지는 패러다임 변화를 설명할 수 있다.
- 생성형 AI가 답변과 데이터를 생성하는 확률 기반 단어 예측 및 토큰화 및 임베딩 메커니즘을 이해할 수 있다.
- 프롬프트 입력에 따른 모델의 추론 방식과 컨텍스트 윈도우의 개념적 작동 방식을 구분하고 식별할 수 있다.
- Temperature를 통한 출력 제어와 환각 발생 원리, 컨텍스트 엔지니어링 개념을 설명할 수 있다.
```

## 목차

- [26.08.21](#260821)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [](#)
      - [asyncio.gather()](#asynciogather)
      - [HTTPX](#httpx)
    - [AI 리터러시](#ai-리터러시)
      - [LLM (Large Language Model)](#llm-large-language-model)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 

```python
import asyncio

async def work():
    await asyncio.sleep(2)
    print("작업 완료")

await work() # 파이썬에서는 동작하지 않는다
asyncio.run(work()) # 이벤트 루프 생성 -> 실행
```

#### asyncio.gather()
한꺼번에 비동기 함수를 실행하고 -> 결과를 모아준다.

```python
import asyncio

async def work(title, seconds):
    await asyncio.sleep(seconds)
    print(title, "완료!")
```

```python
async def main():
    await work("작업A", 2)
    await work("작업B", 3)
    await work("작업C", 1)
```

```python
async def main():
    results = await asyncio.gather(work("작업A", 2), work("작업B", 3), work("작업C", 1))
    print(results)
```

#### HTTPX

```python
import httpx
import asyncio

async def get_post(num):
    async with httpx.AsyncClient() as client:
        url = f"https://jsonplaceholder.typicode.com/posts/{num}"
        res = await client.get(url)
        result = res.json()

        # print(type(result)) # <class 'dict'>
        # print(result['id'], result['title'])
        return result['id'], result['title']

# async def main():
#     for i in range(1, 11):
#         result = await get_post(i)
#         print(i, result)

async def main():
    pp = get_post(1)
    print(type(pp), pp)
    # 비동기 함수는 그냥 선언할때 바로 실행되지 않고,
    # 코루틴 오브젝트 형태를 리턴한다.

    get_posts = [get_post(i) for i in range(1, 11)]
    print(get_posts) # [<coroutine object get_post at 0x...>, ... ]

    results = await asyncio.gather(*get_posts)
    print(results)

asyncio.run(main())
```

### AI 리터러시

#### LLM (Large Language Model)
- 토큰(Token) : AI가 읽고 쓰는 최소 단위
  - 영어 : 단어 1개
  - 한글 : 형태소 기준 (단어 1개 -> 토큰 2~3개, 유동적)
- 다음 토큰에 대해서 예측하는 방식으로 문장 생성
    - "확률적 토큰 선택"
    - 확률 계산은 '유사도' 기반으로
- 앞에 있는 문장/토큰을 기반으로 "유사도" 판단
  - 문자열 텍스트를 숫자(실수 벡터 / 임베딩 벡터)로 변환하여 벡터 간 거리 계산 (거리 가깝다 = 유사하다)
- "유사도"의 기준
  - 유클리디언 (피타고라스 정의)
  - 코사인 유사도 (-1 ~ 1)
    - 1에 가까울수록 유사
    - 0에 가까울수록 상관X
    - -1에 가까울수록 반대의미



## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
Q. 실행 도중 필요한 경우 제어권을 잠시 양보하고, 다시 자기 차례가 되면 멈췄던 지점부터 이어서 작업을 수행할 수 있는 파이썬의 특수 함수를 무엇이라고 하나요?

- A. 코루틴(Coroutine) 함수
- 해설: 코루틴(Coroutine)은 일반 함수(서브루틴)와 달리 실행 중간에 멈췄다가 나중에 멈춘 지점부터 다시 이어갈 수 있는 협력형 함수로, async def로 정의되고 await 지점에서 제어권을 양보합니다.
- 내가 쓴 답: `await`

틀린 이유
- async def, await은 개별적인 코루틴 함수의 구현 요소이고, '실행 중간에 멈췄다가 그 지점부터 다시 이어갈 수 있는 협력형 함수'를 가리키는 큰 개념은 코루틴인데, 개념 이해가 모자랐다.

## 진단 퀴즈 오답
- 없음


