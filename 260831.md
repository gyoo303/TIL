# 26.08.31
KANT 20일차, ML


## 오늘 느낀 점
- 챗지피티를 사용할 때, 1의 내용으로 10을 만들어내는 능력이 신기했는데, 입력된 텍스트의 전체 맥락을 은닉 흔적으로 학습하는 RNN의 학습 방법론을 배우고 나니 약간 이해가 되는 느낌이었다.
  - (튜터님) 방법론은 다르겠지만 RNN의 접근법으로부터 발전해 나간 것이 현재의 LLM이다.
- 임베딩이 그냥 언어의 최소 단위를 엄청나게 거대한 벡터 공간에 유사도 기준으로 수치화하여 모아 놓은 것이라 이해하고 있었는데, 구체적으로 어떠한 방법론으로 임베딩을 구축하는지 알 수 있어서 좋았다.
- 은닉층 차원을 줄이거나 늘리는 것, dropout 등의 규제기법 등은 활성화 함수와 함께 선형적인 순전파 연산 흐름에 랜덤한 변수를 지속적으로 추가하는 것으로 이해했다. 결국, 손실 기반 역전파 방법론이 메커니즘의 가장 핵심이 되고, 순전파의 경우 수많은 데이터와 수많은 시행횟수로 우연을 반복해서 패턴 찾기를 시도하는 것으로 이해할 수 있을것 같다.

## 오늘 배운 것

```
### [ 학습목표 ]
- 복잡한 계산 그래프에서의 다층 역전파(Backpropagation) 연쇄법칙 원리를 설명할 수 있다.
- 인공신경망 성능을 제어하는 다양한 활성화 함수와 고도화된 옵티마이저의 종류를 나열할 수 있다.
- PyTorch를 사용하여 심층 신경망(DNN) 구조를 설계하고 커스텀 학습 루프를 작성할 수 있다.
```

## 목차

- [26.08.31](#260831)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [학습 메커니즘: 역전파와 옵티마이저](#학습-메커니즘-역전파와-옵티마이저)
      - [Dropout - 딥러닝 과적합 방지 목적의 규제(Regularizatoin) 기법](#dropout---딥러닝-과적합-방지-목적의-규제regularizatoin-기법)
    - [PyTorch 기반 FashionMNIST 모델 실습](#pytorch-기반-fashionmnist-모델-실습)
    - [순환신경망 (Recurrent Neural Network)](#순환신경망-recurrent-neural-network)
      - [임베딩 (Embedding)](#임베딩-embedding)
      - [time-step](#time-step)
      - [RNN 활성화 함수 - 하이퍼볼릭 탄센트](#rnn-활성화-함수---하이퍼볼릭-탄센트)
    - [RNN의 장기의존성 문제 -\> LSTM (Long Short-Term Memory)](#rnn의-장기의존성-문제---lstm-long-short-term-memory)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


### 학습 메커니즘: 역전파와 옵티마이저
* 역전파: 예측 오류(오차)를 분석하여 각 가중치의 오차 기여도(gradient)를 계산하는 과정
  * "어느 방향으로 얼만큼 가파른지(편미분)" 정보만 컴퓨터에게 알려준다
* 옵티마이저: 역전파에서 계산된 값을 바탕으로 가중치 수정
  * 역전파로 계산한 gradient 정보를 기반으로 실제 가중치 업데이트를 수행한다



```math

W \leftarrow W - \eta \cdot \frac{\partial L}{\partial W}
```
* 가중치 업데이트 양 = Loss에 대해 가중치로 편미분한 값 (=gradient)

옵티마이저가 바뀌면 무엇이 달라질까?
- 기본 SGD : "역전파로 계산된 편미분 값에 내가 가진 학습률만 곱해서 뺄게요."
- Momentum : "역전파로 계산된 편미분 값에 학습률을 곱하되, 직전 이동했던 관성(속도)을 계산해서 더해준 뒤에 뺄게요"
- RMSprop : 역전파로 계산된 편미분 값을 보고, 자주 변하는 가중치는 학습률을 낮추고, 가끔 변하는 가중치는 학습률을 높여서(Adaptive) 곱한 뒤에 뺄게요"
- Adam : Momentum과 RMSprop의 장점을 결합한 하이브리드 알고리즘 -> 대부분의 딥러닝 학습에서 우수한 성능 (기본 국룰 옵티마이저)


#### Dropout - 딥러닝 과적합 방지 목적의 규제(Regularizatoin) 기법

동작 원리: 학습과 추론의 차이
- 학습 시: 무작위 단선
  - 은닉층에 있는 뉴런들을 설정한 확률(보통 0.2-0.5)에 따라 무작위로 출력값을 0으로 만들며 학습을 진행
  - Batch가 바뀔 때마다 비활성화되는 뉴런의 조합은 변경되며, 비활성화된 뉴런으로는 역전파도 진행되지 않는다.
- 추론 시: 전원 복귀
  - 모든 뉴런을 전부 사용한다
  - 단, PyTorch 같은 현대 프레임워크에서는 추론 시 연산 효율을 높이기 위해 학습 단계에서 미리 값을 보정하는 Inverted Dropout을 사용하므로, 추론 시에는 별도의 스케일링 없이 model.eval() 호출만으로 드롭아웃을 비활성화합니다.

왜 과적합이 방지될까?
- 특정 뉴런에 대한 의존성 탈피 (Co-adaptation 방지)
  - 특정 뉴런 몇 개가 너무 강력하게 학습을 주도하면, 나머지 뉴런들은 강력한 뉴런을 따라가는 '동조화(Co-adaptation)' 현상 발생
  - 드롭아웃은 랜덤으로 강력한 뉴런을 강제 비활성화 -> 나머지 뉴런들도 독자적으로 데이터의 유용한 특징을 찾도록 노력함

앙상블 효과
- 매 배치의 스텝마다 뉴런들이 무작위로 꺼지기 때문에, 사실상 매 순간 서로 다른 형태의 수많은 가벼운 서브네트워크(Sub-network)를 학습시키는 꼴
- 테스트할 때 이 수많은 네트워크가 협력하여 하나의 결과를 내는 일종의 앙상블 효과를 하나의 모델로 발휘

Dropout 적용 위치
- 일반적으로 연산량이 많고 파라미터가 밀집된 Fully Connected Layer (Dense Layer) 뒤에 주로 배치


### PyTorch 기반 FashionMNIST 모델 실습
* 은닉 뉴런 128개와 드롭아웃(20%) 규제, 그리고 과적합을 방지하기 위한 조기 종료(Early Stopping) 기능을 접목한 학습 루프를 구현합니다.

```python
import torch
from torchvision import datasets
from torchvision.transforms import ToTensor
from torch.utils.data import DataLoader, random_split
import matplotlib.pyplot as plt
import platform
from matplotlib import rc

# matplotlib 한글 깨짐 방지 설정 
if platform.system() == 'Darwin':      # macOS 환경
    rc('font', family='AppleGothic')
elif platform.system() == 'Windows':   # Windows 환경
    rc('font', family='Malgun Gothic')
    
plt.rcParams['axes.unicode_minus'] = False  # 마이너스 기호 깨짐 방지

device = torch.device(
    "cuda" if torch.cuda.is_available() else
    "mps" if torch.backends.mps.is_available() else
    "cpu"
)

print(f"현재 사용 중인 연산 디바이스: {device}")

```

```python
# Fashion MNIST 28x28 (2차원) (0~255) -> 표준 점수 -> 특성은 1차원변환
# 60,000 -> 훈련 데이터 50,000장, 검증 (10,000)

# 훈련 데이터 셋
raw_train_data = datasets.FashionMNIST(
    root = "data", train=True, download=True,
    transform=ToTensor() # 기본 numpy arr
)

# 테스트 데이터 셋
test_data = datasets.FashionMNIST(
    root = "data", train = False,download = True,
    transform = ToTensor()
)

# 검증 데이터셋 분리
train_size = 50000
val_size = 10000

train_data, val_data = random_split(
    raw_train_data, [train_size, val_size],
    generator=torch.Generator().manual_seed(42)
)

print("훈련 세트:", len(train_data))
print("검증 세트:", len(val_data))
print("테스트 세트:", len(test_data))

# 데이터 세트 별 DataLoader 생성
# batch_size -> 한 에포크에서 전체 데이터셋을 32개의 배치로 분할해서 보겠다
train_loader = DataLoader(train_data, batch_size=32, shuffle=True)
val_loader = DataLoader(val_data, batch_size=32, shuffle=False)
test_loader = DataLoader(test_data, batch_size=32, shuffle=False)

print(train_data[0]) # 0~1 사이로 스케일링 처리된 상태
```

```python
import torch.nn as nn
import torch.optim as optim

class AdvancedFashionClassifier(nn.Module):
    # 사용할 레이어를 정의한다
    def __init__(self):
        super().__init__()

        # (28, 28) -> 특성 1차원으로 변경 -> (28*28) -> (784,)
        # nn.Flatten() -> 2차원 텐서 -> 1차원 텐서 -> 입력층
        self.flatten = nn.Flatten()

        # 입력층 (784 -> 128)
        self.hidden_layer = nn.Linear(784, 128)

        # 활성화 함수 (ReLU)
        self.relu = nn.ReLU()

        # Dropout - [0,1] 0.1~0.5
        self.dropout = nn.Dropout(p=0.2)

        # 출력층 128 -> 10
        self.output_layer = nn.Linear(128, 10)

    # 순전파(Feed Forward)
    def forward(self, x):
        out = self.flatten(x) # (28,28) -> (784,)
        out = self.hidden_layer(out) # (784,) -> (128,) # 은닉층 처리
        out = self.dropout(out) # 훈련시에만 사용, 과대적합 방지
        out = self.relu(out) # 활성화 함수 (선형적 -> 비선형적)
        out = self.output_layer(out) # (128,) -> (10,)

        return out

```

```python
# 모델 인스턴스 생성 -> 디바이스 할당
model = AdvancedFashionClassifier().to(device)

tot_params = sum([p.numel() for p in model.parameters() if p.requires_grad])
print(f"학습가능한 모델 파라미터 총 갯수: {tot_params:,}")

# 손실 함수 및 옵티마이저 정의
criterion = nn.CrossEntropyLoss() # 기본적으로 Sparse Categorical Cross Entropy 적용
optimizer = optim.Adam(model.parameters(), lr=0.005)

# 조기 종료 학습 및 검증 루프
epochs = 20 # 최대 에포크 수
patience = 3 # 검증 로스가 더 이상 떨어지지 않는 횟수, 3회 이상이면 학습 종료(조기종료)
patience_count = 0
best_val_loss = float('inf')

# 측정되는 loss 값을 저장할 리스트
train_losses = []
val_losses = []

for epoch in range(epochs):
    running_loss = 0.0 # 한 에포크(각 배치의 모든 손실)의 손실
    model.train() # 학습 모드 전환 : 학습에만 사용하는 것들 활성화 ex. dropout

    for images, labels in train_loader: # 미니배치 (32개 배치로 분할)
        # 처리할 데이터를 device로 보내준다 (연산 수행할 하드웨어 선택)
        images = images.to(device)
        labels = labels.to(device)

        optimizer.zero_grad() # 이전 그래디언트(기울기) 초기화

        # 예측
        outputs = model(images) # forward 메소드 실행 (feed-forward 순방향)

        # 손실 계산
        loss = criterion(outputs, labels)

        # 오차 역전파 수행 --- 가중치에 대한 편미분(gradient=기울기) 계산
        loss.backward() 

        # 옵티마이저를 통한 가중치 업데이트
        optimizer.step()

        running_loss += loss.item() # 각 배치의 로스를 기록 (1에포크)

    epoch_loss = running_loss / len(train_loader) # 1에포크 로스 (배치 로스의 평균)
    train_losses.append(epoch_loss)


    # 검증 모드
    model.eval() # 학습에만 필요한 부분 dropout 등을 배제한다.
    val_loss = 0.0

    with torch.no_grad(): # 역전파-옵티마이즈 안한다.
        for images, labels in val_loader:
            images = images.to(device)
            labels = labels.to(device)

            # 검증 예측 (순전파)
            outputs = model(images)
            loss = criterion(outputs, labels)
            val_loss += loss.item()

    epoch_val_loss = val_loss / len(val_loader)
    val_losses.append(epoch_val_loss)

    print(f"Epoch {epoch+1}/{epochs} | 훈련 loss: {epoch_loss:.4f} | 검증 loss: {epoch_val_loss:.4f}")

    # 조기 종료
    if epoch_val_loss < best_val_loss:
        # 기존 검증 로스보다 개선되었다
        best_val_loss = epoch_val_loss
        patience_count = 0

        # 손실이 개선된 모델 저장
        torch.save(model.state_dict(), f"best_fashion_model_val_loss_{epoch_val_loss:.4f}.pth")
    else:
        patience_count += 1
        print(f"== 검증 손실 미개선: {patience_count}/{patience} ==")

        # patience 한계를 초과하면 학습을 중단 (조기종료)
        if patience_count >= patience:
            print(f"조기 종료: {epoch+1} 에포크에서 학습 종료")
            break

```

```python
# 최종 테스트셋 기반 정확도 측정 및 시각화
# 가장 좋은 가중치 모델을 가져오기
model.load_state_dict(torch.load("best_fashion_model_val_loss_0.3796.pth"))
model.to(device)
print(model)

# 테스트 셋으로 평가
model.eval()
correct = 0
total = 0

with torch.no_grad():
    for images, labels in test_loader:
        images = images.to(device)
        labels = labels.to(device)

        outputs = model(images)
        _, predicted = torch.max(outputs.data, 1) # True, False, ...

        print("predicted:", predicted)
        total += labels.size(0)
        correct += (predicted == labels).sum().item()

test_accuracy = correct / total * 100
print(test_accuracy)

# 3. 에포크별 훈련 및 검증 손실 곡선 시각화
plt.figure(figsize=(8, 4))
plt.plot(train_losses, marker='o', color='darkviolet', linewidth=2.5, label='훈련 손실 (Train Loss)')
plt.plot(val_losses, marker='s', color='orange', linewidth=2.5, label='검증 손실 (Val Loss)')
plt.title("에포크별 훈련 및 검증 손실(Loss) 추이 (조기 종료 적용)")
plt.xlabel("학습 횟수 (Epoch)")
plt.ylabel("손실 크기 (Loss)")
plt.legend()
plt.grid(True, linestyle=':')
plt.show()
```


### 순환신경망 (Recurrent Neural Network)

1. 시계열 데이터에 특화된 모델
2. 각 순서를 기억
3. 문장(자연어) - "어순에 따라 의미 완성"



#### 임베딩 (Embedding)
* One-Hot Encoding -> 차원의 저주 -> Embedding
* 지정된 작은 차원으로 실수값 압축 + 단어와 단어 사이의 의미관계성 반영
* 어떻게 계산? -> "임베딩 가중치 행렬" $W_{emb}$
  * 최초 무작위 초기화 -> loss -> 역전파 -> 옵티마이즈

**예시: 원핫인코딩 1000개 -> (1000,1000) -> (1000,3) 가중치 행렬곱 -> (1000, 3)
- 1000개의 정보에 대한 1000차원짜리 원핫인코딩 값을 3차원으로 축소하였음 = Embedding


#### time-step
- 연속된 단어의 조합(ex. "나는 너를 사랑해")에 대해, 각 단어에 대해 순차적으로 feed-forward (가중치 행렬곱 + 활성화 함수 통과)
- 각 단어에 대해 feed-forward 할 때, 다음 단어(정답)는 은닉 상태로 감춘다
- 활성화 함수 -> 하이퍼볼릭 탄센트 tanh (-1 ~ 1)
  - 그래디언트 배니싱 문제 동일 발생

#### RNN 활성화 함수 - 하이퍼볼릭 탄센트
- -1 ~ 1 범위
- 스텝이 많을수록 값이 사라짐 (그래디언트 소실)
- 오랜 단계의 스텝은 기억X (장기 의존성 문제)
  - 굉장히 긴 문장이 입력되어 마지막 스텝이 10스텝 이상 진행될 경우 -> 첫 타임스텝 때 계산했던 단어에 대한 정보(gradient)는 점점 작아진다
    - [Q] 점점 작아지는 시점이 순전파일때? 아니면 역전파일때?

![RNN-time-step](https://teamsparta.notion.site/image/attachment%3Aac443112-0387-49da-913f-4f9ededdf416%3Aimage.png?table=block&id=3ae2dc3e-f514-8163-ba94-cb330d04114b&spaceId=83c75a39-3aba-4ba4-a792-7aefe4b07895&width=1330&userId=&cache=v2&imgBuildSrc=requestProxiedImageUrl)



### RNN의 장기의존성 문제 -> LSTM (Long Short-Term Memory)
- 장기 단기기억 메모리
  - 단기기억(은닉상태)
  - 장기기억(셀상태)


## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


