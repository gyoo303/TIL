# 26.08.20
KANT 13일차, python


## 오늘 느낀 점
- 핵심 개념을 제외한 내장 함수, 라이브러리 등은 한 번에 외우려 하지 말고 필요할 때마다 쓰고 익숙해지면서 외우는게 좋을 것 같다.
- 비동기 개념은 굉장히 합리적이라고 느끼고, 재미있다. 다양한 클래스들을 만들고 비동기로 통신하는 방법이 대략 이해될 것 같다.
- 여러 비동기 함수를 묶어, 모든 비동기 함수의 처리가 종료되어야 제어권을 다음으로 넘기는 `await asyncio.gather`는 프로젝트를 할 때에도 굉장히 유용할 것 같다.

## 오늘 배운 것

```
### [ 학습목표 ]
- 파이썬 내장 함수를 적재적소에 조합하여, 복잡한 제어문 없이도 유효성 검증, 상태 체크 등 핵심 애플리케이션 기능을 쉽고 빠르게 구현할 수 있다.
- 시간·수학·직렬화·난수 등 핵심 파이썬 표준 라이브러리의 기능과 동작 원리를 이해할 수 있다.
- 개발 목적에 맞는 내장 메서드를 활용하여 데이터 변환, 시간 측정 등의 기능을 코드로 구현할 수 있다.
- 텍스트 및 CSV/JSON 파일을 자원 누수 없이 안전하게 읽고 쓸 수 있다.
- 구조화된 데이터를 파이썬 내장 자료구조와 상호 변환할 수 있다.
- Pandas로 데이터프레임을 생성하고 info()와 describe()로 구조와 기초 통계를 확인할 수 있다.
- Matplotlib으로 데이터를 선 그래프로 시각화하고 Jupyter Notebook 셀 안에 출력할 수 있다.
- CPU Bound와 I/O Bound 작업의 리소스 점유 특징을 구별할 수 있다.
- Blocking과 Non-blocking의 흐름 제어 차이 및 이벤트 루프(Event Loop)의 구동 메커니즘을 설명할 수 있다.
- async def 키워드로 진입점이 제어되는 코루틴 함수를 정의할 수 있다.
- await 키워드로 제어권 양보 지점을 명시하고, 실행 환경에 따라 적절한 방식(await 또는 asyncio.run())을 선택해 루프를 실행할 수 있다.
```

## 목차

- [26.08.20](#260820)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [내장 함수](#내장-함수)
      - [`map()`, `filter()`](#map-filter)
      - [`enumerate()`](#enumerate)
      - [`zip()`](#zip)
      - [`sorted()`](#sorted)
      - [`min()`, `max()`](#min-max)
    - [표준 라이브러리](#표준-라이브러리)
      - [\[날짜와 시간\] datetime \& time](#날짜와-시간-datetime--time)
      - [\[수학 연산\] math](#수학-연산-math)
      - [\[데이터 표준 변환\] json](#데이터-표준-변환-json)
      - [\[랜덤\] random](#랜덤-random)
      - [\[시스템\] sys](#시스템-sys)
    - [파일 입출력과 구조화된 데이터 변환](#파일-입출력과-구조화된-데이터-변환)
      - [1. `with open()`의 자원 자동 해제](#1-with-open의-자원-자동-해제)
      - [2. 텍스트 파일 읽기 함수 (read, readline, readlines)](#2-텍스트-파일-읽기-함수-read-readline-readlines)
      - [3. CSV 형식](#3-csv-형식)
      - [4. JSON 형식](#4-json-형식)
    - [동기(Sync) vs 비동기(Async)의 이해](#동기sync-vs-비동기async의-이해)
      - [동기식 함수 (서브 루틴 Subroutine)](#동기식-함수-서브-루틴-subroutine)
      - [비동기 함수 (코루틴 Coroutine)](#비동기-함수-코루틴-coroutine)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---

### 내장 함수

#### `map()`, `filter()`
* map() : 변환
* filter() : 판별 -> 정제
-> 리스트 컴프리헨션으로 대체 가능

```python
nums = [*range(1, 11)]
nums2 = [x**2 for x in nums]
print(nums2)

nums3 = [x for x in nums if x % 2 == 1]
print(nums3)
```

#### `enumerate()`
```python
fruits = ['사과', '오렌지', '멜론', '망고']

num = 1
for fruit in fruits:
    print(num, fruit)
    num += 1

for i, fruit in enumerate(fruits):
    print(i, fruit)
```


#### `zip()`
```python
persons = ["김철수", "김영희", "김민수"]
fruits = ["사과", "오렌지", "멜론"]

items = []
for i, person in enumerate(persons):
    items.append((person, fruits[i]))

print(items)
print(list(zip(persons, fruits)))
```

#### `sorted()`
- 리스트.sort(): 원본 데이터를 정렬
- sorted(Iterable): 원본 데이터는 유지, 새로운 객체를 생성해서 정렬 (불변객체)

```python
nums = [2, 8, 7, 6, 4, 5, 9, 10, 3, 1]

nums.sort() # inplace
nums_sorted = sorted(nums) # return 

print("nums_sorted:", nums_sorted)
print("nums:", nums)
```

#### `min()`, `max()`
print(min(nums), max(nums))
fruits = ['사과', '오렌지', '멜론', '바나나']
print(min(fruits), max(fruits))


### 표준 라이브러리

#### [날짜와 시간] datetime & time
```
- datetime : 모듈명
    - 날짜와 시간 : datetime
    - 날짜 : date
    - 시간 : time
    - 차이 : timeDelta
    ...
```

#### [수학 연산] math
```
math.ceil()
math.floor()
math.trunc()
math.fabs()
...
```

#### [데이터 표준 변환] json
JSON = JavaScript Object Notation (자바스크립트 객체 표기법)

1. 다른 플랫폼(파이썬, 자바, C#...) 간의 소통을 위한 표준 데이터 형식
2. 자바스크립트의 객체를 정의하는 형식을 빌려온 것 -> 자바스크립트가 아니다! (문자열)
3. {"이름":"값", "이름":"값", ...}

```
json.dumps(obj) - 파이썬 객체를 JSON 형식의 문자열로 반환 (메모리 상의 반환)
json.loads(s) - JSON 형식의 문자열을 파이썬 객체로 역변환

json.dumps(obj, fp) - 파이썬 객체를 JSON 파일로 저장
json.loads(fp) - JSON 파일을 파이썬 객체로 역변환
# fp -> 파일 포인터 오브젝트 (open()으로 생성된 객체)
```

```python
import json

# 파이썬 객체 -> JSON 문자열
user = {
    "name" : "김철수",
    "age" : 20
}

json_string = json.dumps(user)
print("json_string:", json_string, "type:", type(json_string))
# json_string: {"name": "\uae40\ucca0\uc218", "age": 20} type: <class 'str'>

json_string = json.dumps(user, ensure_ascii=False)
print("json_string:", json_string, "type:", type(json_string))
# json_string: {"name": "김철수", "age": 20} type: <class 'str'>
```

```python
# JSON 문자열
nums_string = "[1, 2, 3, 4, 5]"
user_string = '{"name": "김철수", "age": 20}'

# JSON 문자열 -> 파이썬 객체
nums = json.loads(nums_string)
user2 = json.loads(user_string)

print("nums:", nums, "type:", type(nums)) # <class 'list'>
print("user2:", user2, "type:", type(user2)) # <class 'dict'>
```


#### [랜덤] random
```python
# 03.py
import random
```


#### [시스템] sys
```python
import sys
print(sys.argv)

args = sys.argv[1:]
print(int(args[0]) + int(args[1]))
```
```bash
PS C:\Users\dance\Documents\ax_study\02_python\chapter09> uv run 03.py 100 300 
['03.py', '100', '300']
400
```


### 파일 입출력과 구조화된 데이터 변환
- 입력(input) -> 데이터를 읽기
  - read(...)
- 출력(output) -> 데이터를 쓰기 [파일로 작성]
  - write(...)
- BUFFER
  - 파일 데이터를 담는 큰 바구니, 일정 용량마다 가득 차면 메모리 해제하면서 데이터를 내보낸다.
  - `file.close()`를 해야 추가 입력사항들이 일시 업데이트된다.

```python
# open(filePath, mode, buffering, encoding, ...)
file = open("test.txt", "w", encoding="utf-8") # 빈 텍스트 파일 생성
file.write("안녕하세요.")
file.write("반갑습니다.")
file.close() # 현재까지 업데이트 된 버퍼를 파일에 쓰고, 메모리 해제한다.
```

#### 1. `with open()`의 자원 자동 해제 

- with 구문
  - 첫 라인에서 `__enter__` 메소드 실행, 마지막 라인 실행 후 `__exit__` 실행.

```python
class MyResource:
    # with 구문 시작시 호출
    def __enter__(self):
        print("__enter__ 호출")
        return self # 인스턴스 객체 반환

    # with 구문 종료 OR 예외 발생시 호출
    # 예외 발생시 exc_type - 예외 클래스 종류
    #           exc_val - 예외 메시지
    #           exc_tb - TraceBack
    def __exit__(self, exc_type, exc_val, exc_tb):
        print("__exit__호출")

    def __str__(self):
        return "MyResource"

with MyResource() as o: # __enter__ 호출 -> return self(instance) -> o
    print(o)

    # 종료시 __exit__ 호출

```


```python
# test.txt 파일을 쓰기 모드(w)로 새로 생성한다
# 이미 경로에 동일 파일이 존재할 경우, 삭제하고 새 파일을 생성한다
# 파일 포인터가 파일의 처음으로 돌아간다
with open("test.txt", "w", encoding="UTF-8") as fp: # fp for "file pointer"
    fp.write("123")
    fp.write("456")

# 파일 포인터를 이동하지 않고 파일 마지막부터 시작 (누적/추가)
with open("test.txt", "a", encoding="UTF-8") as fp:
    fp.write("\n333")
    fp.write("\n444")
```


#### 2. 텍스트 파일 읽기 함수 (read, readline, readlines)

- `read()` : 한꺼번에 전체 텍스트 읽어오기
- `readline()` : 한 라인 읽어오기
- `readlines()` : 라인별로 읽어서 리스트로 변환

```python
with open("test.txt", "r", encoding="utf-8") as fp:
    text = fp.read()
    print(text)
```

```python
with open("test.txt", "r", encoding="utf-8") as fp:
    text = fp.readline()
    print(text) # 첫번째 라인
```

```python
with open("test.txt", "r", encoding="utf-8") as fp:
    # text에 값이 들어오면 True, 안들어오면 False
    # while 연산에서 자주 사용된다
    while(text := fp.readline()):
        print(text)
```

```python
# 대용량 텍스트를 readlines로 다 받아오면 메모리가 터진다
with open("test.txt", "r", encoding="utf-8") as fp:
    lines = fp.readlines()
    print(lines) # 모든 라인
```

```python
# 가장 직관적이고 효율적인 호출 방식
with open("test.txt", "r", encoding="utf-8") as fp:
    print(type(fp))
    for line in fp:
        print(line)
```

#### 3. CSV 형식
Comma Seperated Value
  - writer (파일 포인터)
    - writerow(iterable)
  - reader (파일 포인터)

```python
import csv
with open("members.csv", "w", encoding="utf-8", newline="") as fp:
    writer = csv.writer(fp)
    writer.writerow(["이름", "나이", "전화번호"])
    writer.writerow(["김철수", 20, "010-0000-0000"])
```

```python
with open("members.csv", "r", encoding="utf-8") as fp:
    reader = csv.reader(fp)
    members = list(reader)

    print(members)
```


#### 4. JSON 형식

- 파이썬 객체 -> 전송 가능한 JSON 형식으로 변환 - 직렬화(Serialize)
- JSON 문자열(전송받은 문자열) -> 파이썬 객체 변환 - 역직렬화(Deserialize)

```python
import json

# Python dict
user = {
    "name": "김영희",
    "age": 20,
    "mobile": "010-0000-0000"
}

# Serialize (Python dict -> JSON)
with open("user.json", "w", encoding="utf-8") as fp:
    json.dump(user, fp, indent=4, ensure_ascii=False)

# Deserialize (JSON -> Python dict)
with open("user.json", "r", encoding="utf-8") as fp:
    user2 = json.load(fp)
    print(user2, "type:", type(user2))
```

### 동기(Sync) vs 비동기(Async)의 이해

#### 동기식 함수 (서브 루틴 Subroutine)
- 먼저 실행된 연산이 종료되기 전까지, 다음 연산이 실행되지 않는다.

```python
import time

def work(title, seconds):
    time.sleep(seconds)
    print(title, "완료!")

start = time.perf_counter() # 작업 시작시간

work("A작업", 2)
work("B작업", 3)
work("C작업", 1)

end = time.perf_counter() # 작업 종료 시간
print("걸린시간:", end - start)
```

#### 비동기 함수 (코루틴 Coroutine)
- 비동기 함수가 순차적으로 "시작"되고
- 다음 코드로 이동한다 (end = time.perf_counter())
  - -> Non-blocking (코드 진행을 막지 않는다.)
  - 비동기 함수는 실행되면 제어권을 다음 코드로 넘겨준다

```python
import asyncio

async def async_work(title, seconds):
    await asyncio.sleep(seconds)
    print(title, "완료!")

# 비동기 함수가 순차적으로 "시작"되고
# 다음 코드로 이동한다 (end = time.perf_counter())
# -> Non-blocking (코드 진행을 막지 않는다.)
async def main():
    start = time.perf_counter()

    asyncio.gather(*[
        async_work("A작업", 2),
        async_work("B작업", 3),
        async_work("C작업", 1)
    ])
    
    # asyncio.gather(
    #     async_work("A작업", 2),
    #     async_work("B작업", 3),
    #     async_work("C작업", 1)
    # )

    end = time.perf_counter()

    print("걸린시간:", end - start)

await main()
```



## 메모할 만한 것들
- async.gather는 비동기 함수 여러 개를 동시에 실행하는 함수로, 마찬가지로 await를 붙이지 않으면 실행과 동시에 제어권이 다음 라인의 코드로 넘어간다.

## Daily Quest 퀴즈 오답
- 없음


## 진단 퀴즈 오답
- 없음


