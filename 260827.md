# 26.08.27
KANT 18일차, ML


## 오늘 느낀 점
- 학습률의 경우에는 모델 자체의 특성과 함께 데이터에 대한 경험적 이해가 있어야 유효한 조정이 가능할 것 같다. 학습 성공 여부를 빠르게 판단할 수 없는 경우(큰 데이터셋) 최적 학습률을 찾아내는 로직이 있으면 좋을 것 같다.
- 데이터를 검증용, 테스트용으로 쪼개는 접근법은 오버피팅을 막는 데 효과적으로 보인다. 하지만 데이터 샘플 갯수가 너무 작을 경우, 전체 데이터를 학습시키되 규제를 활용하여 오버피팅을 막는 접근도 생각해 볼 수 있지 않을까?
- 특성 공학(피쳐 엔지니어링)은 매우 다양한 조합 가짓수가 나오는 만큼, 무작위성보다는 데이터 도메인에 대한 이해를 기반으로 실행되어야 효과적일 것 같다. 또한, 다중공선성으로 인한 오버피팅을 방지하기 위해서는 규제 회귀를 통해 특성 일부를 배제하는 접근도 필수로 보인다.

## 오늘 배운 것

```
### [ 학습목표 ]
- 학습률(Learning Rate)을 조정하며 가중치 최적화 경로를 시각화하고, 데이터를 훈련/검증/테스트 세트로 분할할 수 있다.
- 다중 회귀에서의 다중공선성 문제와 규제(Ridge, Lasso)의 오버피팅 억제 원리를 설명할 수 있다.
- 특성 공학을 적용하여 다항 변수를 생성하고 Ridge 및 Lasso 모델을 학습시킬 수 있다.
```

## 목차

- [26.08.27](#260827)
  - [오늘 느낀 점](#오늘-느낀-점)
  - [오늘 배운 것](#오늘-배운-것)
  - [목차](#목차)
    - [2.2 경사하강법, 옵티마이저 및 데이터 분할 (계속)](#22-경사하강법-옵티마이저-및-데이터-분할-계속)
      - [모의고사가 필요한 이유: 데이터 분할 (Data Split)](#모의고사가-필요한-이유-데이터-분할-data-split)
      - [scikit-learn 데이터 분할의 핵심 옵션](#scikit-learn-데이터-분할의-핵심-옵션)
      - [오버슈팅을 막는 데이터 스케일링(Data Scaling)](#오버슈팅을-막는-데이터-스케일링data-scaling)
    - [3.1 다중 회귀와 규제 모델](#31-다중-회귀와-규제-모델)
      - [다중 선형 회귀와 다중공선성](#다중-선형-회귀와-다중공선성)
      - [피쳐 엔지니어링과 규제](#피쳐-엔지니어링과-규제)
      - [Lasso가 가중치를 0으로 만드는 이유](#lasso가-가중치를-0으로-만드는-이유)
  - [메모할 만한 것들](#메모할-만한-것들)
  - [Daily Quest 퀴즈 오답](#daily-quest-퀴즈-오답)
  - [진단 퀴즈 오답](#진단-퀴즈-오답)

---


### 2.2 경사하강법, 옵티마이저 및 데이터 분할 (계속)

#### 모의고사가 필요한 이유: 데이터 분할 (Data Split)
데이터 분할을 왜 하나요?
* AI가 수능을 잘 보게 하려면 '연습문제', '모의고사', '본수능'용 문제를 사전에 엄격하게 나누어 제공해야 합니다.
* 수집한 데이터를 모두 학습시키면, 모델이 데이터의 법칙과 패턴을 이해하는 것이 아니라 문제은행의 질문과 답을 통째로 외워버리는 과적합(Overfitting) 현상이 일어납니다.
* 이를 방지하기 위해 전체 데이터를 scikit-learn 라이브러리의 train_test_split 기능을 활용하여 3개의 독립 공간으로 분리합니다.
  * train set 60% 평소 연습용
  * validation set 20% 중간 점검용
  * test set 20% 최종 평가용


#### scikit-learn 데이터 분할의 핵심 옵션

데이터를 분할할 때, 순서를 섞고 균형을 맞추는 처리가 필요합니다.

`train_test_split` 함수 인자
* test_size = 0.2 (test set의 비율)
* random_state = 42 (데이터를 섞을 때 사용하는 난수, 재현성을 위해 고정)
* shuffle = True (분할 전 데이터를 무작위로 섞어준다 - 모델이 편향된 데이터로 학습하는 것을 방지한다)
* stratify = y (정답 데이터의 비율이 쪼개진 훈련/테스트 세트 안에서도 동일하게 유지되도록 나눈다, 아주 강력한 효과/매우 중요)


```python
# 1차 분할: 전체 압축된 데이터를 [훈련+검증용 80%]와 [최종 테스트용 20%]로 나눕니다.
X_train_val, X_test, y_train_val, y_test = train_test_split(
    X_scaled, y_scaled, test_size=0.2, random_state=42, stratify=y_scaled
)

# 2차 분할: 남겨둔 [훈련+검증용 80%]를 다시 쪼개어 [진짜 훈련용 60%]와 [모의고사 검증용 20%]로 나눕니다.
X_train, X_valid, y_train, y_valid = train_test_split(
    X_train_val, y_train_val, test_size=0.25, random_state=42, stratify = y_train_val 
)
```


#### 오버슈팅을 막는 데이터 스케일링(Data Scaling)

왜 필요한가요?
* 집 크기(ex. 4000)나 집값(500,000)처럼 원본 숫자가 너무 크면, 인공지능의 gradient update 수치가 불안정해져서 오버슈팅이 발생할 수 있다.

MinMaxScaler - 데이터 평균을 0, 표준편차를 1로 맞추어 데이터가 평균으로부터 얼마나 떨어져 있는지에 대한 표준 점수(Z-Score)로 변환한다.

```math

X_{new} = \frac{X - \text{최솟값}}{\text{최댓값} - \text{최솟값}}
```

표준점수 Z -> 어떤 데이터가 평균으로부터 **표준편차의 몇 배만큼 떨어져 있는지**를 나타내는 상대위치 점수.
```math

Z = \frac{X - \mu}{\sigma}
```


```python
# 오버슈팅을 막기 위해 모든 숫자를 0에서 1 사이로 압축하는 MinMaxScaler를 준비합니다.
scaler_X = MinMaxScaler()
scaler_y = MinMaxScaler()

# 데이터를 압축기에 넣고 돌린 뒤, 계산하기 편하게 다시 1차원(한 줄)으로 쫙 펴줍니다(flatten).
X_scaled = scaler_X.fit_transform(X_sqft).flatten()
y_scaled = scaler_y.fit_transform(y_price).flatten()
print(X_scaled[:5], y_scaled[:5])
print(X_scaled.shape, y_scaled.shape)
```


### 3.1 다중 회귀와 규제 모델
너무 많은 변수를 설정하면 과적합을 일으키기 쉽다.


#### 다중 선형 회귀와 다중공선성

**다중 선형 회귀**
- 여러 개의 독립변수를 입체적으로 조합하여 하나의 결과(종속 변수)를 정밀 예측하는 알고리즘

**다중 선형 희귀 공식**
```math
y = w_1 x_1 + w_2 x_2 + w_3 x_3 + \dots + w_p x_p + b
```
- $y$ : 예측해 낼 최종 기말고사 시험 성적 (0점부터 20점 범위)
- $x_1$ : 주당 자습 시간 (1: 2시간 미만, 2: 2~5시간, 3: 5~10시간, 4: 10시간 초과)
- $x_2$ : 방과 후 자유 시간 (1: 매우 적음 ~ 5: 매우 많음)
- $x_3$ : 학기 중 학교 결석 일수
- $w_1$, $w_2$, $w_3$ : 각 정보가 최종 성적에 미치는 영향력/중요도 (가중치)
- $b$ : 기본적으로 깔고 가는 점수 (편향)

![다중회귀](https://teamsparta.notion.site/image/attachment%3Aced26f89-a60d-41d6-a269-b10891bd923c%3Aimage.png?table=block&id=3ae2dc3e-f514-8185-ae42-f8b2db4e0e0a&spaceId=83c75a39-3aba-4ba4-a792-7aefe4b07895&width=1330&userId=&cache=v2&imgBuildSrc=requestProxiedImageUrl)


**다중공선성**
* 상호 상관관계가 높은 독립 변수들이 지나치게 상관관계를 가져서 독립된 기여도를 파악하기 힘든 중복 간섭 현상
* 데이터가 아주 조금만 바뀌어도 가중치 계수가 크게 변화하여 모델 신뢰도가 떨어집니다.
* 모델의 오버피팅 가능성을 크게 높여 새로운 데이터에 대한 예측력이 상실됩니다.
* 분산팽창계수(VIF) - 다중공선성 여부를 진단하는 지표
  * VIF = 1	다중공선성이 전혀 없음	매우 안전한 변수이므로 그대로 모델 학습에 사용합니다.
  * 1 < VIF <= 5	가벼운 중복 존재	모델 성능에 주는 악영향이 미비하므로 허용 가능한 수준입니다.
  * VIF >= 10	심각한 다중공선성 존재	변수를 제거하거나, 하나의 중합 변수로 합치거나, 규제 모델을 걸어야 합니다.

#### 피쳐 엔지니어링과 규제

피쳐 엔지니어링(특성 공학, Feature Engineering)이란?
- 기존의 변수를 추가 가공해서 더 설명력이 높은 변수를 만들어내는 '전처리 과정'
  - 기존 변수인 자습시간과 자유시간 데이터를 곱해 '학습 대비 휴식 균형'과 같은 고차원 파생 변수를 추가
  - 이 과정에서 변수가 과도하게 늘어나 오버피팅이 발생할 수 있음 : 훈련용 데이터의 패턴과 함께 소음까지 암기해버리는 문제
  - 변수에 페널티를 부여해 과도한 영향력을 억누르는 "규제"가 필요한 배경

**릿지 회귀** (L2 규제)
* 예측 평균 오차를 정밀하게 조율하면서, 가중치들의 제곱합을 추가 페널티로 적용해 특정 변수가 과도한 영향력을 갖는 것을 방지

릿지 손실 함수
* 모델의 예측오차와 가중치들의 복잡도(크기)를 동시에 고려하여, 모델이 최소화해야 하는 종합 벌점
* 결과값의 최소지점을 찾는 것이 Ridge 회귀 학습 목표
```math
\text{Loss}_{\text{Ridge}} = \text{MSE} + \alpha \sum_{j=1}^{p} w_j^2
```

$\text{MSE}$ : 평균 제곱 오차 (Mean Squared Error)
* 모델의 전체적인 예측력 지표
* 모델의 데이터 적합도(Fit)을 담당하는 영역
* 오버피팅이 발생하면 낮아진다

$\alpha$ : 규제 강도 (Hyperparameter)
* $\text{MSE}$ 대비, 오버피팅을 막기 위한 모델 단순성(규제항)에 얼마나 많은 가중치를 줄 것인가
* $\alpha$가 커질수록:
  * 오차를 조금 더 허용하더라도 독립변수들의 가중치$w$를 강하게 압박하여 모델 오버피팅을 막는다

$\sum_{j=1}^{p} w_j^2$: L2 규제항 / 패널티항 (Regularization Term)
* 모델의 상수항(bias)를 제외한 모든 독립변수(feautre)들의 가중치($w$)를 제곱하여 더한 값
  * $p$ : 모델에 사용된 독립 변수(Feature)의 총 개수
  * $j$ : 각 독립 변수의 번호를 지정하는 인덱스(일련번호). 편향(상수항)을 규제에서 제외하기 위해 0번이 아닌 1번 변수부터 시작합니다. 
* 제곱을 적용하여 특정 변수의 가중치가 비대해질수록 크게 증가 = 페널티 크게 증가
  * 모델 오버피팅을 방지하는 브레이크 역할


**라쏘 회귀 (L1 규제)**

idge와 달리, "쓸모없는 힌트는 아예 보지 않겠다"며 가중치를 칼같이 0으로 만들어 버리는 단호함을 가진 알고리즘

라쏘 손실 함수
* 예측에 도움이 안 되는 불필요한 데이터(컬럼)는 모델이 스스로 판단해서 가중치를 0으로 지워버립니다. 덕분에 진짜 중요한 핵심 원인들만 쏙쏙 골라내어 남기는 효과(변수 선택)

```math

\text{Loss}_{\text{Lasso}} = \text{MSE} + \alpha \sum_{j=1}^{p} |w_j|
```

$\sum_{j=1}^{p} |w_j|$ (L1 규제항)
* 모델의 편향(Bias, 상수항)을 제외한  모든 독립 변수(컬럼) 가중치들의 '절대값 합'
  * $p$ : 모델이 학습하는 독립 변수(컬럼)의 총 개수
  * $j$ : 각 변수의 번호(인덱스). y절편 역할을 하는 상수항(편향)은 전체의 기본 바닥 선을 잡아주어야 하므로, Ridge와 마찬가지로 규제 대상에서 제외($j=1$부터 시작)합니다.

![릿지-라쏘-규제](https://teamsparta.notion.site/image/attachment%3Aa13f1263-26e9-452f-94f9-40af8908a180%3Aimage.png?table=block&id=3ae2dc3e-f514-8128-a21c-caad17d6405a&spaceId=83c75a39-3aba-4ba4-a792-7aefe4b07895&width=1410&userId=&cache=v2&imgBuildSrc=requestProxiedImageUrl)


#### Lasso가 가중치를 0으로 만드는 이유

* Ridge 회귀는 가중치들을 **둥근 원 모양**의 경계선 안으로 압박하는 반면, 절대값을 사용하는 Lasso 회귀는 가중치들을 **뾰족한 다이아몬드(마름모) 모양**의 경계선 안으로 밀어 넣습니다.
* 릿지의 **둥근 원(Ridge)은 완만한 곡선 위**에서 만나기 때문에 가중치들이 0.1, 0.05처럼 작아질 뿐 완벽한 0이 되기는 어렵습니다.
* 반면, 뾰족한 다이아몬드(Lasso)는 높은 확률로 '축 위에 있는 뾰족한 모서리'에서 처음 만나게 됩니다. 축 위에서 만난다는 것은 가중치 그래프의 좌표 중 하나가 **정확히 0이 된다는 뜻**입니다.



## 메모할 만한 것들
- 없음

## Daily Quest 퀴즈 오답
- 없음

## 진단 퀴즈 오답
- 없음


